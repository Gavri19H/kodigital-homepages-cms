# LeadGen CMS — Contract 00 · Repository Findings Evidence

**Status of this document:** PROOF of investigation, not summary. Every row cites concrete file paths + the functions/routes/tables/exports actually read. This is the evidence base the rest of the v2 contract is built on.

## Commits / branches inspected

| Repo | Ref | Tree commit observed | Role |
|---|---|---|---|
| `Gavri19H/kodigital-homepages-cms` | `main` | `6ff010277f51` | **Target** repo + Worker + the **Listicles** subsystem (primary fork template) |
| `Gavri19H/a2z-agent-demo` | `main` | `b484748e96dc` | Quote-funnel **reference** (`api/src/insureprimo/`) — auction, adapters, payload, funnel styles, postback |
| `Gavri19H/kodigital-dashboard` | `main` | `516252ad5bd0` | Media-buying / campaign **dashboard** — CH↔D1 mirror, revenue ingestion, alerting |

> Note on naming: the reference product surface lives under `api/src/insureprimo/` in `a2z-agent-demo`. That identifier is a **banned token** in the target repo (see §2 Guardrails). This contract names it only in prose/evidence; **generated LeadGen code must not contain it** — the mechanics are re-implemented fresh under `leadgen_`.

---

## Evidence table — Target CMS (`kodigital-homepages-cms@main`)

| Area | File(s) inspected | What exists (concrete) | What to reuse | What to avoid | LeadGen implication |
|---|---|---|---|---|---|
| Worker entry | `api/src/index.ts` | `Hono` app; `app.use("*")` structured request log (reads `cf.botManagement`); `app.use("*")` **ADMIN_HOST gate** (any `/admin*`/`/api/admin*` off `ADMIN_HOST` → 404 `no-store`+`noindex`); `app.get("/health")`; `app.get("/api/admin/auth/status", accessAuth, authStatusHandler)`; mounts `previewRouter, mediaRouter, adminRouter, newsletterRouter, analyticsRouter` **before** `publicRouter` (slug catch-all); `scheduled` cron runs `processScheduledArticles`, `driveInProgressProvisioning`, `syncListicleAnalytics`, `listicleDailyReconciliation`, `runListicleRevenueCron` — **each in its own try/catch**; `queue` runs `processProvisionMessage`. | The mount-order pattern (register `leadgenPublicRouter` before `publicRouter`), the per-task isolated cron (add `syncLeadgenAnalytics` + `runLeadgenRevenueCron`), the bot-signal request log. | Do not register `/lg/*` after the `/:slug` catch-all. Do not let a LeadGen cron task throw into the shared `scheduled`. | `/lg/*` mounts next to `analyticsRouter`; LeadGen cron tasks are additive + isolated. |
| Env / secrets | `api/src/env.ts` | `Env` interface (D1 `DB`, KV `CACHE`, R2 `MEDIA`, `PROVISION_QUEUE?`); optional secrets `OPENAI_API_KEY`, `CF_ACCESS_*`, `AWS_*`, `LISTICLE_EVENTS_FIREHOSE_STREAM`, `CH_URL/CH_USER/CH_PASSWORD`, `LISTICLE_PB_TOKEN_GENERIC/_CAPI`, `LISTICLE_S2S_TOKEN_FACEBOOK`; helpers `readEnvSecret(env,name)`, `parseBoolean`, `parseNumber`, `getAdminHost`. | `readEnvSecret` for dynamically-named per-provider/per-platform secrets; the "absent secret ⇒ no-op" discipline. | Never add a credential to `[vars]`; never type a secret as required. | LeadGen adds `LEADGEN_EVENTS_FIREHOSE_STREAM` (var) + `LEADGEN_PB_TOKEN_<PROVIDER>` / `LEADGEN_S2S_TOKEN_<PLATFORM>` / per-Offer token secrets (all optional). |
| Worker config | `api/wrangler.toml` | `name=kodigital-homepages-cms-worker`; `[vars]` incl. `AWS_REGION`, `EVENTS_FIREHOSE_STREAM="homepage-events"`, `LISTICLE_EVENTS_FIREHOSE_STREAM="listicle-events"`, `ADMIN_HOST`, `HTML_CACHE_TTL_SECONDS` (60 dev / 300 prod), `CACHE_API_ENABLED`; `[triggers] crons=["* * * * *"]`; D1 `kodigital-homepages-cms-db` (id `caa649b9…`), KV `CACHE`, R2 `MEDIA`, `PROVISION_QUEUE`; bindings **redeclared per `[env.staging]`/`[env.production]`**. | Add `LEADGEN_EVENTS_FIREHOSE_STREAM="leadgen-events"` as a `[vars]` line in all three env blocks. | Do not rely on env inheritance for bindings; redeclare per env. | LeadGen stream var is plaintext + per-env; creds stay secrets. |
| Auth | `api/src/auth/access-auth.ts` (via `index.ts` + `admin/router.ts`) | `accessAuth` middleware (CF Access JWT via JWKS-in-KV; identity **or** service-token; `DEV_BYPASS_AUTH` honored only when `APP_ENV!=production`); `authStatusHandler`; `AccessAuthVariables` context (`c.get("access")` → `{mode, email}`). | Mount LeadGen routers under the existing gate — inherit auth with zero new auth code. | Do not add a second login flow. | `/admin/leadgen*` + `/api/admin/leadgen/*` gated automatically. |
| Admin mount | `api/src/admin/router.ts` | `admin.use("/admin", gate)`, `admin.use("/admin/*", gate)`, `admin.use("/api/admin/*", gate)`; `admin.route("/", adminUi)`, `admin.route("/", listicleUi)`, `admin.route("/", api|wfApi|aiApi|listicleApi)`. | Add `admin.route("/", leadgenUi)` + `admin.route("/", leadgenApi)` right beside the listicles pair. | — | Two new sub-routers, same gate. |
| Listicles admin API | `api/src/admin/listicles/router.ts` | `routes.get/post("/offers"…)`, `/offers/search` **before** `/offers/:id`, `/offers/:id/usage`, `/offers/:id/analytics`; `/sections` + `/sections/preview` before `/sections/:id` + `/sections/:id/{usage,offers,analytics}`; `/articles` + experiments/structure/analytics/drilldown/publish; `/versions/:id` PUT/fork/new-revision/preview; `/pages/:id/validate`; `/media-platforms`; `/analytics/rebuild-range`; mounted `listicleApi.route("/api/admin/listicles", routes)`; `routes.use("*")` sets `private,no-store`+`nosniff`. | Clone the route shape 1:1 for `/api/admin/leadgen/*` (§10 route contract). | Do not put a param route before its static sibling. | LeadGen API mirrors this exact surface + auction/quote/payload additions. |
| Listicles admin UI | `api/src/admin/listicles/ui.ts` | `listicleUi.get("/admin/listicles")→302 "/admin/listicles/offers"`; `/offers` `/sections` `/articles` list pages + `/sections/new`, `/sections/:id/edit`, `/articles/new`, `/articles/:id/edit`; `apiJson(env,path)` **drives the JSON API in-process** (`listicleApi.request(...)`); `distinctOfferValues(db,col)` (fixed-literal DISTINCT); `resolveTimeframe(range)`; `branding(c)`; `shellNoStore` middleware on `/admin/listicles` + `/admin/listicles/*`. | The SSR-drives-JSON-in-process pattern, `resolveTimeframe`, `distinctOfferValues`, the `→ /offers` default redirect, the 4-tab shell. | Do not duplicate SQL between SSR + XHR — drive the API in-process. | LeadGen UI = same shell, 4 tabs (Offers/Sections/Quotes/Auction). |
| ID strategy | `api/src/listicles/ids.ts` | `PUBLIC_ID_PREFIXES` (`off_ sec_ art_ exp_ ver_ pg_ cand_ rule_ lnk_`); self-contained Crockford `ulid(now)` (26 chars, 48-bit time + 80-bit entropy, ASCII-sortable); `mintPublicId(kind)`; `isPublicId(kind,value)`. | Re-implement identically with `leadgen_` prefixes (`lgo_ lgp_ lgs_ lgm_ lgq_ lgx_ lgv_ lga_ lgc_ lgr_ lgl_`). | Do not use a random UUID (loses time-sort). | `public_id` = the macro/event/analytics key. |
| Macros | `api/src/listicles/macros.ts` | `CANONICAL_MACROS` (**32**: click_id, utm_*, traffic_source, placement, lander_v, offer_id, offer_name, page, device, os, os_version, browser, browser_version, country, state, city, ip, ua, sub1–5, url, referer, language, cpc, session_id, fbc, fbclid); `MACRO_ALIASES` (`clickid→click_id`); `normalizeTemplate`, `findUnknownMacros`, `resolveMacros` (encodeURIComponent each), `validateOfferUrlTemplate` (absolute http(s), **no macro in host/authority**, no C0/DEL control chars, unknown rejected). | Re-implement + **extend** with `{response:<dotted.path>}` (winner response) — the reference already ships `sub5={response:slug}` (see below). | Do not allow a macro in the host position; do not leave unknown macros unresolved. | LeadGen `macros.ts` = 32 canonical + `{response:*}`. |
| CH→D1 mirror | `api/src/listicles/mirror-sync.ts` | `MIRRORS: MirrorSpec[]` (5: offer/section/article/drilldown/link_instance) each `{name, chTable, d1Table, columns:ColumnMap[], pk, notNull}`; `buildUpsertSql` (`INSERT … ON CONFLICT(pk) DO UPDATE SET nonPk=excluded.…, synced_at=excluded.synced_at`); `chSelect` (`… FINAL WHERE dt BETWEEN toDate({from}) AND toDate({to})`); `mirrorOne` (own try/catch, coerce text/int/real, skip bad rows); `D1_BATCH_ROWS=80`; `rebuildRange(env,from,to)`; `syncListicleAnalytics(env)` (today+yesterday UTC); `readChCleanEventCount`. | Clone as `syncLeadgenAnalytics` + 9 `leadgen_analytics_*` mirror specs; keep the isolation + fail-open + `ON CONFLICT` upsert. | Do not use `INSERT OR REPLACE` (loses the `ON CONFLICT` synced_at semantics). | §24 mirror sync. |
| S2S dispatcher | `api/src/listicles/s2s-dispatch.ts` | `getEnabledPlatformByTrafficSource(db,ts)` (case-insensitive); `deriveFbc(fbclid,fbc,now)` → `fb.1.<now>.<fbclid>`; `dispatchMatchedConversionS2S(env,ctx,db,click,revenue)` → resolve `postback_url_template` macros (`{fbc}{fbclid}{click_id}{value}{currency}{event_name}{auth_token}`), `isFireableUrl` gate, fire on `ctx.waitUntil`, KV dedupe `lst_s2s:<platform>:<click>:<event>:<conv>`; `resolveClickContextFromCh` (offer_click + session lookup). | Clone as LeadGen `s2s-dispatch.ts` + `leadgen_media_platforms`. | Do not block ingestion on the pixel fire; do not double-fire (KV dedupe). | §26 S2S. |
| Migrations | `api/migrations/0032`–`0035` | `listicle_offers` (global, no site_id; `public_id`, `conversion_tracking_method` CHECK, `payout_method`, cap cols, `cap_fallback_offer_id/url`), `listicle_sections` + derived `listicle_section_offers` + `listicle_section_link_instances`, `listicle_articles` (per-site `site_id`), `listicle_article_experiments` (partial unique `WHERE status='running'`), `listicle_article_versions` (`public_id==lander_v`, immutable-while-running), `listicle_pages` + `listicle_page_section_candidates` + `listicle_page_rules` (typed `conditions_json`+`conditions_hash`), `listicle_offer_cap_counters` (PK `(offer_id,cap_date)`); 5 `listicle_analytics_*`; revenue infra (`media_platforms`, `postback_log` UNIQUE `(provider,external_txn_id)`, `revenue_raw`, `revenue_unmatched`, `event_dead_letter`, `fx_rates`); `listicle_conversion_log` UNIQUE `(click_id,dedupe_key)`. **Migration head = `0035`.** | The entire table taxonomy → `leadgen_` equivalents (§7); the cap-counter, dedupe-UNIQUE, derived-index, and immutable-running-version patterns. | Do not reuse `listicle_` names; do not create a Listicles Athena DB. | LeadGen ships `0036`–`0039` additive. |
| Layout registry | `api/src/public/listicle/layouts/registry.ts` + `default/tokens.ts` | `LAYOUTS: Record<string,ListicleLayout>`, `getLayout(id)` (unknown→default); `ListicleLayout {id,name,cssVars,renderShell(vm),renderPage,renderSection}`; `defaultListicleLayoutTokens` = **measured** senior-saving tokens (`status:"measured"`, `measuredAt`, drift register). | Clone as `getFunnelDesign(id)` + a `banner` design registry; adopt the **measured-token discipline** (measured overrides provisional; both recorded). | Do not hardcode design values outside the token file; do not allow arbitrary CSS. | §14 + §20/§22 design registries. |
| Cache | `docs/cache-strategy.md`, `api/src/cache/*` | Key shape `ns:{site_id}:{path}:{content_version}:{template_version}` (`site_id` first, versions suffix); `htmlKey/articleKey/…` formatters; `TEMPLATE_VERSION` const; `computeEtag` (SHA-256 of key parts, 16 hex); `publicHtmlCacheHeaders` (`public,max-age=300,swr=86400`), `adminCacheHeaders` (`private,no-store`); per-site `list+delete` invalidate. | New `lg-shell:`/`lg-config:`/`lg-section:` namespaces + `LEADGEN_TEMPLATE_VERSION`; same header helpers. | Do not put `site_id` late in the key; do not cache admin. | §28 caching. |
| Analytics ingest | `api/src/analytics/router.ts` (via `index.ts`) | `analyticsRouter` mounts `POST /api/track` public beacon → Firehose; `listicle-reconciliation.ts` (`listicleDailyReconciliation` self-gated 00:05), `revenue-recon.ts` (`runListicleRevenueCron`). | Clone `POST /lg/track` beacon + a LeadGen reconciliation task. | — | §22 tracking. |
| Testing | `api/test/*` (244 files), `api/test-ui/*` (Playwright: `listicles-offers/sections/articles/tracking/render/visual/perf-regression/analytics-mirror/manual-qa.spec.ts`), `api/vitest.config.ts`, `api/playwright.config.ts` | Vitest unit+integration (`admin.request`/`listicleApi.request`), Playwright UI + **visual regression** + perf regression; design-contract render specs. | Mirror the suite layout for `leadgen-*.test.ts` + `leadgen-*.spec.ts`. | — | §31 testing plan. |
| Guardrails | `docs/source-architecture.md`, `docs/no-touch-red-line.md`, `README.md`, `prd.json` acceptance | `verify:no-legacy-prod-refs` scanner (`api/scripts/verify/assert-no-legacy-prod-refs.ts`); **banned tokens**: the reference product name, `quotesRoutes`, `psychic-quiz`, `rental-booking`; five gates (`tsc`, `test`, `verify:no-legacy-prod-refs`, `verify:infra`, `verify:worker-config`); additive-only, forward-only migrations; agent never deploys. | Respect all; run all five gates green. | Do not emit any banned token in LeadGen source; do not mutate existing tables/routes. | §3 guardrails; auction re-implemented fresh. |

---

## Evidence table — Quote-funnel reference (`a2z-agent-demo@main`, `api/src/insureprimo/`)

| Area | File(s) inspected | What exists (concrete) | LeadGen implication |
|---|---|---|---|
| Auction engine | `handlers/listings.ts` | `PARTNERS: Partner[]` (`{name, fetch, normalize}`, one per provider); `filterPartnersByPlacement(partners, body)` (drops any whose `body['_placement_'+name]` is empty); `calculateAuctionMetrics(listings, floorPct)` → `{score=maxBid, floor=maxBid*floorPct, qualified≥floor, fallbacks<floor}`; `selectWinner(results, …)` sorts candidates by `score` desc → winner; **CPL merge** (`CPL_AGGREGATOR_NAMES`; merges fixed-bid CPL listings into winner, re-sorts by `cpc` desc); `dedupeCarrierConflicts` (same-carrier CPL↔CPC → keep higher bid, CPC wins ties); backfill (brands not shown, from `nextinsure`, sorted desc, XOR-obfuscated); `evaluateOverrideConditions(conditions, body, hourEst, cfRegion)` (AND across fields, OR within a field; ops `eq/gt/lt/neq/range`, fields age_range/homeowner/currently_insured/military/hour_est/utm_*/os/os_version/state/credit_score/cf_region/bundle); `matchesCarrierName` (substring, case-insensitive); `applyCarrierOverride` (include_only + `strictly_override`, else exclude); parallel fetch `Promise.race([fetch, timeout(ms)])` + `Promise.allSettled`; timeout default 2500, floor 0.1; non-blocking `ctx.waitUntil` writes `insertAuctionBid`/`insertAuctionWin`/`updateSessionOnListings`; `obfuscateData` (XOR+base64). | §18/§19/§21 auction — **re-implemented fresh** as `src/public/leadgen/auction/{engine,fetch,parse,winner,explain}.ts` + `leadgen/rules.ts` + `leadgen/auction-core.ts`. |
| Config load | `handlers/config.ts` | `handleConfig` → KV cache (`config:…`, 300s TTL) → `loadConfigFromD1` → hardcoded `DEFAULT_CONFIG`; `respondWithConfig` strips server-only fields (include/exclude aggregators, checkpoint), keeps `redirect_pct`/`redirect_target` (`?? 0`/`?? null`); ETag (djb2 hash) + `Cache-Control: public,max-age=300,s-maxage=1800,swr`. | §8 `/lg/config/:funnel_variant_id` — same KV+D1+default fallback + server-field-strip + ETag. |
| Payload builder | `adapters/insurify-payload.ts` | `buildInsurifyPayload(body,headers)` returns a **nested `{application:{…}}`** envelope; multi-source field fallbacks (`body.first_name ?? contact.first_name ?? body.fn`); transforms `formatDate/formatPhone/mapBoolean/mapMaritalStatus`; `collectDrivers`/`collectVehicles` (array **or** flat `driver_1_*` fields); `cleanObject` recursively drops undefined/null/empty (arrays, nested objects) = the **"no fabrication"** mechanism. | §11/§13 payload builder + the `cleanObject` runtime build; the multi-source + transform + array-collection model. |
| Response-derived URL | `adapters/insurify.ts` | `fetchInsurify` POSTs `{api_key, ...buildInsurifyPayload()}`; **validity triple** `slug && id && session_key` else `wrapListings([])`; `buildInsurifyBannerUrl(slug,fbc,utm_term,utm_content)` → `…?source_id=fb&sub1=<utm_term>&sub2=<utm_content>&sub3=<fbc>&sub4=<fbc>&sub5=<slug>` — i.e. **`sub5 = {response:slug}`**; logo = CDN copy (not hotlink); description = locked headline; `cpc = admin CPL bid`; `pricing_model:'cpl'`. | §16 `{response:<path>}` macro family + §20 banner field-map + the carrier validity-gate + admin-bid-as-cpc for CPL Offers. |
| Provider adapters | `adapters/{mediaalpha,adsbymoney,kissterra,nextinsure,px,farmers}*.ts`, `adapters/types.ts`, `adapters/utils.ts` | Each provider = a `fetch` + `normalize` pair + a `*-payload.ts` + `*-mappings.ts`; `utils.ts` has `cleanObject`, `formatDate`, `formatPhone`, `mapBoolean`, `mapMaritalStatus`, `wrapListings`; `NormalizedListing` = `{name, displayname, title, description, logo, clickurl, cpc, imp_pixel, source, pricing_model}`. | §15 provider response parser normalizes to a common `LeadgenCarrier` shape; the per-provider adapter hook is the escape hatch (OQ-5). |
| Admin config model | `admin/db-types.ts` | **Row vs API** split for `FunnelRule` (checkpoint/priority/conditions/action{funnel_type,feed_name,fb_multiplier,redirect_pct,redirect_target}/include+exclude_aggregators), `Placement` (per-provider placement ids), `AuctionOverride` v1 (force_winner/include/exclude aggregators), `AuctionOverrideV2` (aggregators, conditions, include/exclude **carriers**, `strictly_override`), `CplRule` (fixed `bid_amount`), `AuctionSettings` (`timeout_ms`, `floor_percentage`, defaults). | §21 the two rule levels (offer-level = v1-style, carrier-level = v2-style w/ `strictly_override`); §33 auction settings; the Row/API split for every `leadgen_*` table. |
| Funnel styles | `templates/funnel-styles.ts`, `funnel-styles-components.ts`, `funnel-styles-offers.ts`, `funnel-styles-extras.ts` | `renderCssTokens()` `:root` design tokens (see §21 audit): **navy** `--color-primary:#1B3A5C`, **orange** `--color-accent:#E85D26`, bg `#F5F7FA`, `--font-display:'Literata',serif`, `--font-body:'Sora',sans-serif`, radius 6/10/14/20/9999, shadow sm/md/lg/xl+glow; `.progress-bar` h8 navy-gradient fill; `.step-title` Literata 1.375rem/700 (≥640 1.75rem); `.btn` pad14/16 border2 radius-md **min-height52** cubic-bezier hover, `.selected` navy; `.btn-primary` max-width320 centered; `.card-grid` 3-col; `.card-item` min-height48; `.offer-card` radius-xl first-child=recommended (accent border/bg/glow, logo 160×72, cta accent). **No range slider, no green, no serif-question, no icon-only cards, no reassurance badge in the reference code.** | §21 **Reference Design Audit** — the reference funnel's REAL palette is navy+orange+Literata/Sora, which **diverges** from the green/blue LendingTree screenshots (BLOCKER, see below). |
| Postback / CAPI | `handlers/postback.ts`, `handlers/capi.ts`, `handlers/ingest-conversions.ts`, `handlers/ingest-cpl-capi.ts` | `handlePostback` GET, token-auth (`POSTBACK_SECRET`), `buildUserData` SHA-256-hashes PII (email/zip/country/externalId lowercased+trimmed; pre-hashed passthrough), `fbc = fb.1.<ts>.<fbclid>` when absent, POSTs `graph.facebook.com/{ver}/{pixelId}/events`, `event_id` dedupe (client eventId or `ip_<ts>_<rand>`), **redacts access token in error logs**, `value`/`currency` from `data` param, defensive `response.ok` + `json()` try/catch. | §26/§42 conversion → FB CAPI/S2S; PII hashing; token redaction in logs; event_id idempotency. |
| Google Maps | **grep `maps|autocomplete|zipcode|places|geocod` → 0 matches** | The reference does **NOT** use Google Places. It validates ZIP with `/^\d{5}$/` (`handlers/listings.ts`) and resolves city/state from `INSUREPRIMO_ZIP_KV` (KV lookup). | §27 Google-Maps address/ZIP autofill is **NET-NEW** for LeadGen — no reference implementation to port; design fresh + flag (BLOCKER for exact UX parity). |

---

## Evidence table — Dashboard (`kodigital-dashboard@main`)

| Area | File(s) inspected | What exists (concrete) | LeadGen implication |
|---|---|---|---|
| CH→D1 mirror | `api/src/lib/ch-d1-mirror.ts` | Generic `mirrorToD1(config)` — `MirrorConfig {chClient, d1, chQuery (must include FINAL), chParams, d1Table, d1Columns, chColumnMap, batchSize=100, notNullCols, preStatements}`; filters `notNullCols`, batched `INSERT OR REPLACE`, `preStatements` for atomic stale-row DELETE. **This is the pattern the Listicles `mirror-sync.ts` re-authored with `ON CONFLICT`.** | §24 confirms the mirror lineage; LeadGen uses the `ON CONFLICT` variant (Listicles) not `INSERT OR REPLACE`. |
| Revenue ingestion | `api/src/lib/revenue-ingestion.ts` | `pushAthenaRevenue` (Athena `events` `event_type='listing_click'` last ~1 day, `SUM(offer_cpc)` grouped by `dt,hr,utm_campaign,utm_term,utm_content` → CH `revenue_events_raw`), `pushKoStatsRevenue` (`ko_stats_parquet_v2` `action='/cp'`), `retryOnce` (5s), `recordIngestionMetrics` (D1 `revenue_ingestion_metrics`); bid revenue writes CH directly (ReplacingMergeTree(synced_at) clobber note). | §42 the dashboard joins revenue on `utm_campaign/utm_term/utm_content` — LeadGen CH columns must keep these names **stable**. |
| Revenue/auction routes + alerting | `api/src/routes/{auction,campaigns,ingest,revenue}.ts`, `api/src/lib/{auction-ingestion,clickhouse,today-mirror-guard}.ts`, `schema.sql` | Campaign management + BI over the same CH substrate; `alert_rules_config` seeds incl. `ingestion_failed` (P0) + `revenue_gap` (P0) + `mv_failure` (P1) + `stale_data` (P1). | LeadGen CH tables + revenue land into the same observability net; keep `traffic_source/utm_*/placement/click_id/offer_id` column names stable. |

---

## Reference basis + implementation inputs (status is v2.3.7 — READY TO BUILD)

The design is grounded in code that exists — there is no missing design input:

| # | Basis / input | Source | Status |
|---|---|---|---|
| **R1 — Default funnel design** | The insureprimo funnel's real stylesheet, measured 1:1 (navy `#1B3A5C` + orange `#E85D26`, Literata/Sora). | `a2z-agent-demo/api/src/insureprimo/templates/funnel-styles*.ts` → shipped in `designs/default-funnel/tokens.ts` + `docs/default-funnel-design-audit.md`. | **Resolved** — authoritative default. Alternate palettes are later registry additions. |
| **R2 — Admin/control-screen design** | The Listicles admin design language (nav, tabs, tables, toolbars, modals, analytics), applied to Offers/Sections/Quotes/Auction. insureprimo admin templates are the *logic* reference. | `kodigital-homepages-cms/api/src/admin/listicles/*` + insureprimo `admin/templates/*`. | **Resolved** — see `03`§9. |
| **R3 — Funnel + auction logic** | Re-implemented fresh from the insureprimo engine with the sharper logic in this contract (banned name never emitted). | insureprimo `handlers/listings.ts`, `adapters/*`, `handlers/config.ts`. | **Resolved** — see `07`. |
| **I1 — Address/ZIP auto-suggest** | Net-new (insureprimo validates ZIP via regex + KV city/state; no Places). Build a standard Google Places autocomplete; key is a deploy-time secret. | net-new | Implementation-time: needs a Google Maps key at deploy (not a design gap). |
| **I2 — ClickHouse feed** | The Athena→ClickHouse ingest is run by the data/ops team; `lg_*` tables fill once it runs (mirror-sync tested against seeded D1 + mocked CH meanwhile). | ops-owned | Implementation-time (same as Listicles). |
| **I3 — Per-provider payloads** | Each provider's exact request/response is captured with the built-in Test tool at onboarding; the generic builder covers the common case. | per-provider | Implementation-time. |

I1–I3 are implementation-time inputs. R1–R3 are resolved from existing code. Status: **v2.3.7 — READY TO BUILD.**
