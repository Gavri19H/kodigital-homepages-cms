# 02 · Issue Coverage Matrix

Every product-owner issue and every investigation finding appears here — nothing is folded into "polish". Columns are binding; "Tests" names the suite group in `11-testing-manual-qa-contract.md`; "Acceptance" is the per-issue pass condition (phase-level PASS criteria in `12`). Root causes RC-1…RC-6 are defined in `01` §1.2. Full per-issue blocks (current behavior, expected behavior, exact contract violation, implementation direction) live in the referenced section files.

| Issue ID | Severity | Area | Root cause | Required fix | Files/routes affected | Data impact | Runtime impact | UX impact | Tests | Phase | Acceptance |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | P0 | Funnel runtime | RC-1 | Server-render Sections via existing presets into shell + ≤40KB hydration engine (render/state/events/validation/deps/auction/banners/impressions) → `03` | `serve.ts`, new `runtime/*` (engine, render, state, events, validation, dependencies, maps, auction-client), `components/presets.ts`, `config-dto.ts`, `scripts/build-leadgen-runtime.ts`; routes `/lg/:slug`, `/lg/runtime/:v.js` | none (KV `lg-section:*` keys per 09 §28) | Live funnel renders & completes; 29 headless events gain producers | Users see and answer questions | G1 E2E, G2 unit, §11.6 anti-false-PASS | 1 | `/lg/:quote_slug` renders first question → answers → auction → banners → impressions → `/lg/lc` click, all asserted by G1 |
| R2 | P0 | Payload macros | RC-2 | `buildLeadgenRuntimeContext()` threads request/CF/session/traffic/offer macros into `buildPayload`, Test, click, banner URLs → `04` | new `leadgen/runtime-context.ts`; `auction/engine.ts:767–778`, `auction/fetch.ts:173`, `serve-auction.ts:61–80`, `payload-builder-handlers.ts:204–234`, `click.ts` | additive `macro_context_json` on `leadgen_auction_result_log` (mig 0040) | `source=macro` fields carry real ip/ua/geo/traffic values | Operators can trust macro fields | G2 per-macro table, G1 payload assert | 1 | Live auction payload contains real geo/ip/ua/utm per the 32-macro resolution table |
| R3 | P0 | Computed source | RC-2 | `COMPUTED_REGISTRY` (12 keys) + save-time `computed_unknown_key` rejection + runtime population + UI dropdown → `04` §4.4 | new `leadgen/computed.ts`; `payload.ts:402–404,760–761`; `ui-payload-builder.ts:122–125` | none | computed fields resolve at auction/Test | Dropdown replaces free-text key | G2 registry + rejection tests | 1 (UI: 2) | Unknown key fails save; every registry key resolves in live payload + Test |
| R4 | P0 | Auction gate | RC-3 | Wire `dynamicAuctionEligibility()` at auction-offers PUT, engine participation, Offer editor/list badges; remove silent EMPTY_SCHEMA participation → `05` §5.1 | `leadgen/validation.ts:570–590`, `auction/engine.ts:168–173`, `auctions-handlers.ts`, `ui-offers.ts`, `ui-auctions.ts` | none | Invalid/untested Offers excluded with typed reason | Editor shows eligibility status | G3 gates suite | 1 | Ineligible Offer never enters live auction; exclusion reason in explainability + simulate |
| R5 | P0 | Quote publish gate | RC-3 | Activation PUT + variant-save gate over existing `sectionValidationStatus`; 409 + per-Section/Offer missing-field report; UI preflight panel → `05` §5.2 | `quotes-handlers.ts:882–893,1588–1665`, `leadgen/sections.ts:538–577`, `sections-handlers.ts:1217–1352`, `ui-quotes.ts` | none | Invalid Quotes cannot go live | Actionable blocking report with fix links | G3 activation 409 tests | 1 | Activation of a Quote with an error Section → 409 report; clean Quote activates |
| R6 | P1 | Event versioning | RC-6 | Stamp `payload_schema_version`/`answer_mapping_version`/`section_mapping_version`/`auction_config_version` server-side on auction-path events; engine beacons carry versions; populate `answer_mapping_version` in config DTO → `05` §5.4, `10` | `analytics/leadgen-events.ts:139,369,529`, `serve-auction.ts`, `config-dto.ts`, `runtime/events.ts` | none (columns exist) | Events attributable to exact config versions | — | G2 event-shape tests | 1 | No auction-path event ships with empty version fields |
| R7 | P1 | Impressions | RC-1 | Return `impressions[]` + `banner_render_id` in `/lg/auction` response; engine fires IntersectionObserver viewability beacons to `/lg/track` → `03` §3.6 | `auction/banner.ts:418–426`, `auction/engine.ts:1078`, `serve-auction.ts:192–210`, `runtime/auction-client.ts` | none | `carrier_impression`/`offer_impression` land in ingest | Analytics tiles stop reading 0 | G1 impression E2E | 1 | Rendered banner produces exactly one impression pair per slot per page_view |
| R8 | P1 | Click macros | RC-2 | Persist session/traffic/offer macro snapshot at auction (result log column) + re-derive request-scoped macros at click; thread into `/lg/lc` template resolution → `04` §4.6 | `runtime-routes.ts:277`, `click.ts:242,271`, `serve-auction.ts`, mig 0040 | additive column (see R2) | Banner URL templates fully resolve | — | G2 unit + G1 redirect E2E | 1 | `/lg/lc` 302 resolves `{session_id}`, `{utm_source}`, `{response:*}` etc. per template |
| R9 | P2 | Anti-tamper | RC-2 | Extend signed tuple with `session_id` binding + include `answer_mapping_version`/`auction_config_version` in signed material; keep 422+`tampered` flow → `05` §5.3 | `attempt.ts`, `serve-auction.ts:145–178`, `runtime/auction-client.ts` | none | Forged session/version swaps rejected | — | G3 tamper matrix | 1 | Each tampered tuple field → 422 + `tampered` quality flag |
| A1 | P2 | Offers delete | RC-5 | Usage-guarded hard delete (409 + usage report when referenced; confirm modal + cascade when clean); Archive stays default; contract erratum §9.6 → `07` §7.2 | `offers-handlers.ts:1162–1227`, `router.ts:120–132`, `ui-offers.ts`; `DELETE /api/admin/leadgen/offers/:id?mode=hard` | deletes rows only when zero references (explicit-query guard; D1 FKs not enforced) | none | Operators can remove mistakes safely | G4 delete tests | 3 | Unused Offer hard-deletes with children; referenced Offer → 409 + full usage report |
| A2 | P2 | Offers duplicate | RC-5 | `POST /offers/:id/duplicate` batch-clone per Q3 copy-set; draft+untested; new public_ids; required new placement ID; copy checkboxes → `07` §7.3 | `offers-handlers.ts`, `ui-offers.ts`, `leadgen/ids.ts` (mint) | inserts only | none | Duplicate modal (name, placement, 3 copy toggles) | G4 duplicate tests | 3 | Duplicate = draft, new `lgo_`/`lgpl_`/`lgp_` ids, no counters/logs/analytics copied |
| A3 | P2 | Usage endpoint | RC-5 | Extend usage to the full reference inventory; same payload drives the delete guard + UI report → `07` §7.4 | `offers-handlers.ts:1177–1227`; `GET /offers/:id/usage` | none | none | “Where is this used” panel | G4 usage completeness | 3 | Every reference kind reported with counts + links |
| B1 | P1 | Payload editors | RC-4 | Visual value-map table, transform picker, chips, condition builder; raw JSON → Advanced drawer only → `06` §6.3/§6.10 | `ui-payload-builder.ts:109–170` | none | none | No JSON in normal mode | G5 zero-JSON authoring | 2 | Full schema authored without typing JSON |
| B2 | P1 | Field tree | RC-4 | Three-pane shell: visual field tree w/ nested object/array child editors; no hand-typed dotted paths → `06` §6.1/§6.8 | `ui-payload-builder.ts:640–685` | none | none | Tree + add-child affordances | G5 nested-schema test | 2 | Nested array-of-objects schema authored visually |
| B3 | P1 | placement_id | RC-2 | Populate `placement` macro from participating placement at auction / selected-or-default in Test; optional explicit source group in UI → `04` §4.5 | `payload.ts:605–613`, `engine.ts:190–206`, `fetch.ts:198–207,304`, `payload-builder-handlers.ts`, `ui-payload-builder.ts` | none | Multi-placement auctions send correct id | Placement source group visible | G2 placement resolution | 1 (UI: 2) | Two auctions with different placements send their respective ids |
| B4 | P1 | Test sample answers | RC-4 | Auto-generate editable sample-answer FORM from active schema (enums→dropdowns, booleans→presets, dates→date input, ZIP presets); JSON = Advanced → `06` §6.12 | `ui-payload-builder.ts:424–470,1054–1062`, `payload-builder-handlers.ts` | none | none | Form-first Test tab | G4/G5 test-tool tests | 2 | Operator runs a passing Test without editing raw JSON |
| B5 | P2 | Test macro sim | RC-2 | Simulated runtime-context inputs in Test tab (geo/ip/ua/traffic), defaults mirroring R2 runtime builder → `06` §6.12 | `payload-builder-handlers.ts:204–234`, `ui-payload-builder.ts` | none | Test parity with runtime | Context panel in Test tab | G4 sim test | 2 | Test payload matches runtime payload for identical simulated context |
| B6 | P1 | Validation UX | RC-4 | Error panel: count summary, per-error path+label+code+hint, click-to-focus (expand parents, scroll, highlight, inline badge) → `06` §6.11 | `ui-payload-builder.ts:698–711,748–769` (server shape already good) | none | none | “Schema has N issues” + jump | G5 focus test | 2 | 5-error schema shows “5 issues”; click focuses exact field |
| B7 | P2 | Test gating | RC-3 | Run `validatePayloadSchema` pre-test; typed 400 + `schema_errors` (same panel); never bare 500 → `06` §6.12 | `payload-builder-handlers.ts:147–199` | none | none | Blocked Test explains itself | G4 invalid-schema 400 | 2 | Invalid schema → 400 + error summary; Save/Test blocked until P0/P1 schema errors fixed |
| B8 | P2 | Computed UX | RC-2 | Dropdown from `COMPUTED_REGISTRY` with label/help/example (UI half of R3) → `06` §6.2 | `ui-payload-builder.ts:122–125` | none | none | No free-text computed keys | with R3 | 2 | Only registry keys selectable |
| B9 | P2 | Value lists / Other | RC-5 | `choiceDisplay{mainValues, otherGroupEnabled, otherGroupLabel, searchableOther}` metadata + value-map grouping + runtime Other renderer; never send literal “Other” → `06` §6.4 | `payload.ts:356–381`, `components/content-schema.ts`, `ui-payload-builder.ts`, `runtime/render.ts`, new `SearchableDropdown`/`OtherGroupSelector` presets | content_json metadata only | Hidden choice selected → real provider value sent | Main choices + searchable Other | G2 runtime + G5 authoring | 2 (runtime leg 1) | Carrier list with 8 main + 100 Other values authors visually and submits real values |
| B10 | P2 | Boolean presets | RC-5 | Preset select emitting value_map: true/false · "1"/"0" · 1/0 · "Y"/"N" · "yes"/"no" · custom → `06` §6.7 | `payload.ts:499–517,677–681`, `ui-payload-builder.ts:112` | none | none | One-click boolean outputs | G2 per-preset shapes | 2 | Each preset emits its exact output shape |
| B11 | P2 | Date handling | RC-5 | Date mode: display input type + provider output format picker (YYYY-MM-DD, MM/DD/YYYY, DD/MM/YYYY, ISO-8601, Unix, custom) emitting `formatDate` transform → `06` §6.6 | `payload.ts:73,521–534`, `ui-payload-builder.ts` | none | Runtime converts input→provider format | No transform JSON | G2 format matrix + G5 UI | 2 | Each format produces documented output; authored without JSON |
| B12 | P2 | Free text flag | RC-5 | Explicit “Free text” toggle disabling value map; keeps required/default/fallback; sanitize + type-coerce → `06` §6.5 | `payload.ts:35–43`, `answers.ts:114–121`, `ui-payload-builder.ts` | none | none | Clear free-text mode | G5 toggle test | 2 | Toggle disables map controls; string output sanitized |
| C1 | P1 | Test tool (owner cluster) | RC-4 | Umbrella: B4 (form generation) + B5 (context sim incl. placement pick) + B7 (validity gate) + staging/production select — Test tab operable with zero raw JSON → `06` §6.12 | `ui-payload-builder.ts`, `payload-builder-handlers.ts` | none | none | Non-technical Test flow | G4+G5 test-tool suite | 2 | Operator selects env + placement, edits generated form, runs Test, reads masked result — no JSON |
| D1 | P2 | Region actions | RC-5 | Two labeled behaviors: “Allow only these regions” / “Block these regions” (enum kept for compat; aliases collapsed in UI); help text separating provider region-blocks from Auction participation rules; contract erratum → `07` §7.5 | `leadgen/rules.ts:59,82–141`, `ui-offers.ts:1361–1368` | none | none (semantics unchanged) | No duplicate-seeming choices | G4 semantics regression + G5 labels | 3 | UI shows 2 actions; stored enums unchanged; behavior regression green |
| D2 | P3 | Priority field | RC-4 | Label “Evaluation order” + help “lower runs first; first block wins” → `07` §7.5 | `ui-offers.ts:1359–1368` | none | none | Labeled field | G5 snapshot | 3 | Label + tooltip render |
| D3 | P2 | Region values | RC-4 | Per-dimension validators: country ISO-3166-1 alpha-2 dropdown, state code dropdown, city chips, ZIP `/^\d{5}$/` chips, bulk paste with inline rejects → `07` §7.5 | `leadgen/validation.ts:411–471`, `ui-offers.ts` | none | Bad rules can’t be saved | Chips + validation | G4 validator units | 3 | `zip:"not-a-zip"` rejected with typed error |
| E1 | P1 | Activity/Vertical | RC-4 | Comboboxes fed by `/activities` + `/verticals` (Vertical filtered by Activity); allow-create affordance; zero-Offer pair warning → `08` §8.2 | `ui-sections.ts:403–410`, `sections.ts:123–126`, `offers-handlers.ts:1513–1526` | none | none | Typos impossible | G5 combobox tests | 4 | Free text gone; mismatched pair warns; Offer list derives from pair |
| E2 | P1 | Mapping grid | RC-4 | Offer dropdown (from `/sections/:id/offers`), schema-path picker w/ type+valid values, visual value-map modal, per-row Test w/ generated samples → `08` §8.7 | `ui-question-builder.ts:286–297,380–443,1035–1066`, `sections-handlers.ts:1045–1054` | none | none | Zero raw ids/JSON in mapping | G5 mapping flow | 4 | Complete mapping authored via pickers only |
| E3 | P2 | Picker/Inspector | RC-4 | Studio rebuild: thumbnail component library grouped by intent, drag-drop canvas, tabbed inspector (Content/Choices/Mapping/Design/Validation/Dependencies/Advanced) → `08` §§8.3–8.6 | new `ui-section-studio.ts` (replaces `ui-question-builder.ts` editor layer) | none | none | Operator-grade authoring | G5 authoring flows | 4 | All §8.12 Playwright authoring flows pass |
| E4 | P2 | Layout model | RC-5 | Tokenized layout containers: Stack, Grid, Columns, CardPanel, HeaderBar, FooterBar, BackgroundPanel, TrustBar (Q4 resolved: in scope) → `08` §8.5 | `components/content-schema.ts:90–109`, `components/presets.ts`, `registry.ts`, `runtime/render.ts` | content_json container nodes | Containers render in live funnel + preview | §8.11 patterns achievable | G2 serialization + G5 render | 4 | All 4 §8.11 patterns built without custom CSS |
| E5 | P1 | Preview | RC-4 | Preview endpoint accepts `{design_id, viewport, sim_state, answers, auto_advance}`; server-renders all §14.9 sims; honors variant design; reversible desktop/mobile at real widths → `09` §9.2 | `sections-handlers.ts:931–1007` (`:962`), `ui-question-builder.ts:473–802` → studio preview pane | none | none | 9/9 sims functional | G5 sim-difference tests | 4 | Each sim visibly distinct; design honored; viewport round-trip screenshot green |
| E6 | P2 | Maps config | RC-5 | Field-level Maps config on address/ZIP components (validate, autofill links, normalize); engine wiring; key-missing warning + manual-entry fallback; Q5: browser leg only → `08` §8.8 | `ui-sections.ts:390–392,422–424`, `components/presets.ts:425–471`, `leadgen/maps.ts`, `runtime/maps.ts` | content_json `maps` props | address_autofill/validation events fire | Linked-field chips UI | G6 Maps suite | 4 (runtime leg 1) | ZIP validate + city/state autofill work; missing key = graceful manual entry |
| E7 | P2 | Components | RC-5 | Add `NumberInputQuestion` + `CurrencyInputQuestion` presets (catalog, content-schema, render, style slots) → `08` §8.10 | `components/registry.ts:27–65`, `presets.ts`, `content-schema.ts` | none | New inputs render | Palette completeness | G2 render tests | 4 | Both types placeable, render, validate, map |
| E8 | P3 | Success state | RC-5 | Success/valid input state styling + `SuccessState` affordance → `08` §8.10 | `components/registry.ts`, `presets.ts`, designs token slots | none | Valid inputs show success | — | G2 render test | 4 | Success state renders on valid input |
| E9 | P3 | Empty states | RC-4 | Explicit “No Offers match Activity + Vertical” empty state with fix links → `08` §8.2 | `ui-question-builder.ts:428` → studio mapping panel | none | none | Actionable empty state | G5 copy test | 4 | Empty state names the pair + links to Offers |
| S1 | P2 | Simulate trace | RC-4 | Simulate response includes redacted per-offer generated payload + parser id + expected response fields; stays dry → `07` §7.6 | `auctions-handlers.ts:1591–1638`, `auction/explain.ts:37–41,99`, `leadgen/redact.ts` | none | none | Explainable dry runs | G4 simulate test | 3 | Simulate shows redacted payload per considered Offer + exclusion reasons |
| T1 | P1 | Test blind spot | RC-6 | Live `/lg` E2E + visual suites against the real runtime (not `page.setContent`); anti-false-PASS assertions → `11` §11.2/§11.6 | `test-ui/leadgen-visual.spec.ts:8–16,72–93`, `leadgen-perf.spec.ts:66`, `leadgen-runtime.spec.ts`, new `leadgen-live-funnel.spec.ts` | none | none | — | is itself the test work | 1+5 | Suites fail on empty mount / no question / no answer event / no auction call |
| T2 | P1 | Traceability claims | RC-6 | Re-audit all 52 v2.3.7 rows against LIVE behavior (runtime rows need runtime assertions); reclassify CP3/CP5; execute + sign manual QA → `11` §11.5/§11.7 | `docs/leadgen/traceability.md:141,324,478–488`, `docs/leadgen/manualQA.md:3–6,496–524` | none | none | — | matrix re-verification checklist | 5 | Updated traceability with honest evidence; signed QA table |
| M1 | P3 | referrer alias | RC-2 | Add `referrer→referer` to `MACRO_ALIASES` → `04` §4.3 | `leadgen/macros.ts:61–63` | none | Both spellings resolve | — | G2 unit | 1 | `{referrer}` normalizes + resolves |
| M2 | P3 | Macro grouping | RC-4 | Grouped source picker: Request/Cloudflare · Traffic/URL · Session · Offer/Auction (+ Computed, Secret token, Advanced macro) with help text → `06` §6.2 | `ui-payload-builder.ts:127–130` | none (storage enums unchanged) | none | Non-technical group names | G5 UI test | 2 | Flat 32-option select replaced by labeled groups |

**Severity totals:** P0 × 5 (R1–R5) · P1 × 13 (R6 R7 R8 B1 B2 B3 B4 B6 C1 E1 E2 E5 T1 T2 — C1 aggregates owner-raised Test-tool P1s; investigation counts 13 P1 findings) · P2 × 18 · P3 × 5. C1 is the product-owner Test-tool cluster kept as its own row so no owner issue is folded away; its normative requirements live in B4/B5/B7 + `06` §6.12.
