# 11 · Testing & Manual QA Contract (all phases · T1, T2; every P0/P1 gets a regression test)

## 11.1 Gates & harness

Five verify gates stay mandatory (`tsc`, `test`, `verify:no-legacy-prod-refs`, `verify:infra`, `verify:worker-config`) **plus new `verify:leadgen-runtime`** (rebuild hydration bundle, diff against committed generated file, assert ≤40KB — `03` §3.2). Vitest for unit/integration (in-process `admin.request`/API harness, mocked providers); Playwright for admin UI + live `/lg` E2E + visual/perf. New spec files: `test-ui/leadgen-live-funnel.spec.ts`, `test-ui/leadgen-studio.spec.ts`, `test-ui/leadgen-payload-builder.spec.ts`; new unit files: `test/leadgen-runtime-context.test.ts`, `test/leadgen-computed.test.ts`, `test/leadgen-runtime-engine.test.ts`, `test/leadgen-gates.test.ts`, `test/leadgen-offer-lifecycle.test.ts`, `test/leadgen-parity.test.ts`. Seeds: extend `test-ui/leadgen-p*-seed.ts` with a fully-mapped two-section Quote + mock dynamic Offer.

## 11.2 Group 1 — Live funnel E2E (Playwright; Phase 1 exit gate)

`/lg/:quote_slug` renders the first Section’s components server-side (JS disabled check: first question visible) · each catalog component type spot-checked rendered · answer click → selected state + `answer_click` beacon observed (network intercept) · auto-advance advances; Continue mode blocks until required answered (+`validation_error`) · Back restores prior answers + progress · dependency reveal/hide follow-up · defaults: `answer_default_applied` then `user_confirmed_default`/`user_selected` transitions · multi-section traversal → `/lg/auction` POST carries binding + answers + `answer_mapping_versions` · banners render from `banners_html` · `carrier_impression` + `offer_impression` fire once per slot (scroll-into-view trigger; duplicate-scroll fires none) · banner click → `/lg/lc` 302 with resolved macros (`{session_id}`, `{utm_source}`, `{response:slug}`) · GA4 events unaffected · CLS budget met WITH content · visual screenshot suite runs against `/lg` (NOT `page.setContent`) desktop+mobile · sessionStorage restore mid-funnel keeps A/B identity + answers.

## 11.3 Group 2 — Runtime payload correctness (Vitest unit/integration; Phases 1–2)

Per-macro resolution table: each of the 32 canonical macros → expected context source or documented-empty; `referrer` alias · computed registry: all 12 keys resolve (DST fixtures for EST hour/day); unknown key → save-time `computed_unknown_key` · placement resolution: participating in auction, selected/default in Test, multi-placement matrix · value_map miss→invalid→fallback regression · nested array build regression (numeric path segments) · conditional ops matrix (all 9 ops × edge values) · boolean preset output shapes (6) · date format matrix (6 formats + custom + invalid→fallback) · Other grouping: secondary selection emits real provider value; literal “Other” only when mapped so · free-text sanitize/coerce · default/fallback typed emission + looseJson normalization · version stamping non-empty on auction-path events (event-shape assertions) · click-time macro merge precedence + snapshot redaction (no raw ip/ua persisted) · runtime state transitions + serialization excludes dependency-hidden answers · beacon batching ≤20 + envelope completeness.

## 11.4 Groups 3–6 (phase-mapped)

**G3 Gates (Phase 1):** engine excludes ineligible Offer (invalid schema / untested / failed / missing parser / missing endpoint) with typed `carrier_filtered_reason` + simulate visibility; EMPTY_SCHEMA silent-participation removed (mock provider records ZERO calls for ineligible Offer); auction-offers PUT returns per-offer warnings; activation 409 with per-Section/per-Offer missing-required report (shape test) + fix links; clean Quote activates; variant-save verdict recompute; signed-token v2 mint/verify + per-field tamper matrix (each of 7 bound fields) → 422 `tampered`; unsigned rejected when `requireSigned`.
**G4 Admin API (Phases 2–3):** duplicate → new `lgo_/lgpl_/lgp_` public_ids, draft, single default placement required, untested, no counters/logs/analytics/revenue copied, checkbox matrix; hard-delete: clean → cascades own children (row-count proofs); referenced → 409 + usage report; usage completeness (fixture referencing all 12 kinds); region per-dimension validators + paste-multiple partial rejection + alias-semantics regression; Test tool: invalid schema → 400 + `schema_errors`, sample-answer generation round-trip, simulated-context parity with runtime builder, placement echo; simulate: redacted per-offer `payload_preview` + parser id + expected fields + exclusion reasons, zero provider calls in dry-run.
**G5 Admin UI (Playwright; Phases 2+4):** payload schema authored end-to-end with zero raw JSON (nested object + array of objects + value map w/ main+Other via CSV + date + boolean preset + condition + default/fallback); “Schema has 5 issues” + click-to-focus; mapping grid via pickers only; Activity/Vertical comboboxes prevent typos + zero-Offer empty state; Studio flows per `08` §8.12 incl. all four §8.11 patterns; preview sims all visibly differ + `design_id` honored + viewport round-trip; Offers list actions, duplicate modal placement requirement, delete flows, region labels.
**G6 Maps (Phases 1+4):** ZIP validate; autocomplete autofill into street/city/state/zip internal fields per field-level config; missing-key graceful no-op + Studio warning + manual entry works; `address_*` events beaconed with correct field ids.

## 11.5 Traceability re-audit (T2 — Phase 5)

Re-audit **all 52 v2.3.7 traceability rows** against LIVE behavior with the corrected evidence standard: *a runtime row requires a runtime assertion* (a unit test cannot satisfy an E2E claim). Reclassify CP3 (“rendered end-to-end” = an actual rendered, answered, auctioned funnel) and CP5 (no “ZERO code gaps” without engine evidence); document the P11 deferral history explicitly. Output: updated `docs/leadgen/traceability.md` + the v2.4 matrix (`13`) with per-row evidence links (test name + file). No row may cite a test that does not execute the claimed behavior.

## 11.6 Anti-false-PASS regression (permanent; may never be waived)

Add to `leadgen-live-funnel.spec.ts` (and keep forever): FAIL if — `[data-lg-mount]` is empty after `data-lg-ready="1"` · zero `[data-lg-question]` elements exist on a Quote with question Sections · no `answer_click` beacon is observed after a scripted answer · `/lg/auction` is never called after completing the final Section · the visual suite’s page source is `page.setContent` (static assertion that the suite navigates to `/lg/…`). Delete/replace the false-comfort assertions: `leadgen-perf.spec.ts:66` empty-mount comment/behavior (perf budget now measured WITH content), `leadgen-visual.spec.ts` setContent harness, and any traceability citation to them.

## 11.7 Manual QA (docs/leadgen/manualQA.md — update + EXECUTE)

Rewrite/extend scenarios so they catch this class of failure, minimum: **MQA-R1** live `/lg` renders + full funnel completion on a real activated site (desktop + mobile device) · **MQA-R2** auction banners render; impressions visible in analytics next sync · **MQA-R3** banner click → provider URL with resolved macros (inspect final URL) · **MQA-R4** payload contains real geo/ip/traffic + computed + placement (staging provider echo) · **MQA-R5** invalid/untested Offer blocked from auction (attempt + observe exclusion reason) · **MQA-R6** invalid Quote activation blocked with readable report · **MQA-R7** payload builder: author schema + run Test with zero JSON (operator, not engineer) · **MQA-R8** Section Studio: build pattern §8.11-(4) start-to-finish · **MQA-R9** Maps field-level config + missing-key fallback · **MQA-R10** desktop/mobile preview round-trip. **Sign-off table must be filled** (name/date/result per scenario) before Phase 5 PASS — a blank sign-off table is a FAIL state (the v2.3.7 lesson).

## 11.8 Acceptance

Vitest green · Playwright green (incl. live-funnel + studio + payload-builder suites) · `verify:all` green incl. `verify:leadgen-runtime` · anti-false-PASS assertions present and green · traceability re-audited with runtime evidence · manual QA executed and signed.
