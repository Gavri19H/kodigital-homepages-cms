# LeadGen CMS — Operational Fix Contract v2.4 · Traceability (living doc)

**Mission:** implement the Operational Fix Contract v2.4 exactly, phase by phase (5 phases, 5 PRs, one branch at a time, squash merges). Vehicle: direct branch+PR (program precedent). Delegated loop (operator-confirmed 2026-07-08): implement → full gates → fresh-context adversarial review (SHIP required) → PR → self-squash-merge → next phase. **The agent performs no deploys and no secret writes** (contract-binding). Production deploy + post-deploy verification + manual-QA execution are operator-owned and tracked BLOCKED, never PASS.

**Contract SSOT:** `docs/leadgen/fix-contract-v2.4/` (15 files, `00`–`14`) + `MANIFEST.sha256` — vendored verbatim from Claude Design project `159eadec-6141-4457-a2df-3cc02f035659` `fix-contract-v2.4/`. **Byte-fidelity: 15/15 files re-fetched and `cmp`-verified BYTE-IDENTICAL on 2026-07-08** (per-file log in session records; every fetch asserted `truncated:false`). Citations `NN §x` = `docs/leadgen/fix-contract-v2.4/NN-*.md`. Issue evidence base: `LEADGEN-CMS-INVESTIGATION-REPORT.md` (workspace root, outside this repo).

**Baseline (virgin `main @ 52905b2` = origin/main, clean tree, 2026-07-08):** `tsc --noEmit` 0 errors · vitest **3257/3257 (299 files)** · `verify:all` OK (no-legacy-prod-refs + infra 8 checks + worker-config 3 blocks) · Playwright **91/91** (after wipe `api/.wrangler/state/v3/d1` → `db:migrate:local` → `seed:local`). Zero pre-existing failures — every later failure/count-change is attributable to this mission. Migration head at baseline: `0039` → contract migration numbering `0040` HOLDS. Scanner over the vendored v2.4 package: **GREEN with zero allowlist additions** (the package is token-clean).

**Evidence standard (binding, `11` §11.5):** a row flips `Specified → PASS` only with runtime-grade evidence — named test file + test name that EXECUTES the claimed behavior (a unit test cannot satisfy an E2E claim); `FAIL` carries the failing evidence; `BLOCKED(<named blocker>)` for operator-owned legs. No row may cite a test that does not execute the claimed behavior. Statuses in this file are the mission's single progress truth.

## The 42-row matrix (requirement → implementation files → tests → status)

Requirement + acceptance per row are governed by the vendored contract (`02` full matrix, `13` per-row acceptance) — this table tracks EXECUTION. "Files" and "Tests" columns are updated to ACTUAL paths/names as each row lands.

| Issue | Sev | Phase | Requirement (v2.4 §) | Implementation file(s) (planned → actual) | Test(s) (evidence) | Status |
|---|---|---|---|---|---|---|
| R1 | P0 | 1 | `03` engine: server-render + hydration, full lifecycle; acceptance `03` §3.11 | `serve.ts`, `public/leadgen/runtime/*` (8 modules + generated bundle 37,834 B), `components/presets.ts`, `config-dto.ts`, `scripts/build-leadgen-runtime.ts`, `scripts/verify/leadgen-runtime.ts`, `runtime-routes.ts` | `test-ui/leadgen-live-funnel.spec.ts` (full G1: JS-off first-question render → answer/auto-advance/Continue → Back → dependency → auction → banners → impressions ×1/slot → `/lg/lc`; §11.6 anti-false-PASS ×4) · `test/leadgen-runtime-api.test.ts` (§3.2 shell, `#lg-config` ≡ `/lg/config`, KV visitor-invariance) · `test/leadgen-runtime-engine.test.ts` (34) · `test/leadgen-runtime-routes.test.ts` (bundle route) | **PASS** (03 §3.11 all bullets; playwright 99/99) |
| R2 | P0 | 1 | `04` §§4.1–4.3, 4.7 canonical context into payloads; acceptance `04` §4.9 | `leadgen/runtime-context.ts` (new); `auction/engine.ts`, `auction/fetch.ts`, `serve-auction.ts`, `payload-builder-handlers.ts`, `click.ts` | `test/leadgen-runtime-context.test.ts` (32-macro table) · `test/leadgen-gates.test.ts` (real geo/ip/ua/utm through the HTTP path; Test ≡ runtime parity) · live-funnel mock-captured provider payload (ua/utm/computed/placement/value-map) | **PASS** |
| R3 | P0 | 1 (+2 UI) | `04` §4.4 computed registry + `computed_unknown_key` + runtime population | `leadgen/computed.ts` (new); `leadgen/payload.ts`; (P2: `ui-payload-builder.ts`) | P1: `test/leadgen-computed.test.ts` (12 keys + DST fixtures) · `computed_unknown_key` rejection · captured live `request_timestamp` · P2: UI dropdown from the registry (spec ① + UI tests; free-text key input GONE) | **PASS** (both legs) |
| R4 | P0 | 1 | `05` §5.1 eligibility gate wired ×4; EMPTY_SCHEMA removed | `leadgen/validation.ts`, `auction/engine.ts`, `auctions-handlers.ts`, `ui-offers.ts`, `ui-auctions.ts` | `test/leadgen-gates.test.ts` (eligibility matrix incl. 4 new codes; ineligible ⇒ ZERO provider calls; EMPTY_SCHEMA deleted; PUT warnings shape) · `test/leadgen-offers-ui.test.ts` (banner/badge, all-8-code labels) · live-funnel (exactly 1 provider call) | **PASS** |
| R5 | P0 | 1 | `05` §5.2 activation/variant gate + 409 report + preflight UI | `quotes-handlers.ts`, `leadgen/sections.ts` (read), `sections-handlers.ts` (read), `ui-quotes.ts` | `test/leadgen-gates.test.ts` (409 normative shape + fix_links + `offer_ineligible` leg + clean 200 + variant verdict recompute/KV) · `test/leadgen-quotes-ui.test.ts` (panel copy verbatim; authoritative publish badge) | **PASS** |
| R6 | P1 | 1 | `05` §5.4 version stamping server+engine; config DTO mapping version | `analytics/leadgen-events.ts`, `serve-auction.ts`, `config-dto.ts`, `runtime/events.ts` | `test/leadgen-gates.test.ts` (§5.4 stamps non-empty) · `test/leadgen-config-dto.test.ts` (answer_mapping_version populated) · coherence test (config versions sha256 ≡ token `answer_mapping_hash`) | **PASS** |
| R7 | P1 | 1 | `03` §3.6 impressions returned + IntersectionObserver beacons + dedupe | `auction/banner.ts`, `auction/engine.ts`, `serve-auction.ts`, `runtime/auction-client.ts` | live-funnel (carrier+offer impressions EXACTLY once per slot; duplicate-scroll fires none) · DEV-29 slotTarget fix probe (before: 0 beacons / after: 3/3) | **PASS** |
| R8 | P1 | 1 | `04` §4.6 persisted snapshot (mig 0040) + fresh request macros at `/lg/lc` | `runtime-routes.ts`, `click.ts`, `serve-auction.ts`, `migrations/0040_leadgen_runtime_context.sql` | `test/leadgen-gates.test.ts` (merge precedence; snapshot redaction — no raw ip/ua persisted) · live-funnel `/lg/lc` 302 with `{session_id}`/`{utm_source}`/`{response:quote_ref}` resolved | **PASS** |
| R9 | P2 | 1 | `05` §5.3 tuple v2 (+session_id, answer_mapping_hash, auction_config_version) | `attempt.ts`, `serve-auction.ts`, `runtime/auction-client.ts` | `test/leadgen-attempt.test.ts` + `test/leadgen-gates.test.ts` (7-field tamper matrix ⇒ 422 `tampered` each; v1 grace off ⇒ reject; forged session; mid-session remap invalidates; unsigned money path 422) | **PASS** |
| A1 | P2 | 3 | `07` §7.2 usage-guarded hard delete + erratum §9.6 | `offers-handlers.ts` (`deleteOfferHandler` `?mode=hard` + `buildOfferUsageReport` guard covering ALL reference classes incl. `funnel_rules_targeting` + `auction_backfill_source` — BLOCKER-1/2 fix), `router.ts`, `ui-offers.ts` (kebab Delete + 409 in-use modal + Archive-instead) | `test/leadgen-offers-api.test.ts` "archives by default; hard-deletes a clean offer permanently" (L1580) · "a funnel_rule target AND an auction backfill source block `?mode=hard` with the two new kinds" (L1621 — BLOCKER regression, fails-before) · "refuses removing a placement referenced by leadgen_auction_offers (typed guard)" (L904) · `test/leadgen-offers-ui.test.ts` (delete confirm + 409 usage modal) | **PASS** (post-remediation) |
| A2 | P2 | 3 | `07` §7.3 duplicate (Q3 copy-set, new ids, placement required, draft+untested) | `offers-handlers.ts` (`duplicateOfferHandler`), `router.ts` (`POST /offers/:id/duplicate`), `ui-offers.ts` (duplicate modal + copied banner), `leadgen/ids.ts` | `test/leadgen-offers-api.test.ts` (new lgo_ id, paused, untested, required default placement 400, source ids not verbatim, nothing operational copied) · `test/leadgen-offers-ui.test.ts` (modal requires placement) | **PASS** |
| A3 | P2 | 3 | `07` §7.4 full usage inventory | `offers-handlers.ts` (`buildOfferUsageReport` — one query set drives usage GET + delete guard; reports the FULL reference-kind set incl. `quotes_indirect`/`region_rules`/`revenue_attribution` + provider-log latest-ts — MAJOR-4 fix) | `test/leadgen-offers-api.test.ts` "reports the full reference-kind set + delete_eligibility; provider-logs/analytics are warning-only" (L1596 — MAJOR-4 regression) · "an unused offer references nothing and is delete-eligible" (L1037) | **PASS** (post-remediation) |
| B1 | P1 | 2 | `06` §§6.3/6.9/6.10 visual value-map/condition/default builders; JSON→Advanced | `ui-payload-builder.ts` (rebuilt, 4.5k lines) | `test-ui/leadgen-payload-builder.spec.ts` ① (value-map table + Add-many + CSV import; condition via dropdowns incl. the §6.10 `homeowner = true` example post-F-1; Static default + Computed fallback; Advanced round-trip ③) · `test/leadgen-payload-builder-ui.test.ts` (44) | **PASS** |
| B2 | P1 | 2 | `06` §§6.1/6.8 field tree + nested object/array builders | `ui-payload-builder.ts` | spec ① authors nested object + array-of-objects visually; STORED schema path-set equality asserted (9 paths, `api/test-artifacts/fix-p2/stored-schema-v1.json`) | **PASS** |
| B3 | P1 | 1 (+2 UI) | `04` §4.5 placement source/macro threading; Test picker | `leadgen/payload.ts` (`source:"placement"`), `auction/engine.ts`, `auction/fetch.ts`, `payload-builder-handlers.ts`, Test-tab picker UI | P1: `test/leadgen-gates.test.ts` (participating/default/multi-placement matrix) · P2: spec ⑤ placement picker (`data-test-placement-count="2"`, picks the SECOND; `context_used.placement_id` echoes it; mock captures it) | **PASS** (both legs) |
| B4 | P1 | 2 | `06` §6.12.1 generated sample-answer form | `payload-builder-handlers.ts` (pinned `{answers, fields[]}` endpoint + KV draft), `ui-payload-builder.ts` | `test/leadgen-sample-answers.test.ts` (15: per-kind generation, DOB heuristic, draft round-trip, 404/400 paths) · spec ⑤ (per-kind inputs; edit → Save draft → reload survives) | **PASS** |
| B5 | P2 | 2 | `06` §6.12.2 simulated context (shared builder overrides) | `payload-builder-handlers.ts` | P1 parity test (Test ≡ runtime builder) · spec ⑤ context panel (US defaults; macros echoed in `context_used` + mock-captured `traffic.utm_source`) | **PASS** |
| B6 | P1 | 2 | `06` §6.11 count + click-to-focus + inline errors | `ui-payload-builder.ts` | spec ② ("Schema has 5 issues." exact; Jump expands ancestors + pulse + inline badge + editor focus; live drop to 4) · panel footer renders from the exported blocking-codes list | **PASS** |
| B7 | P2 | 2 | `05` §5.5 pre-test + pre-save validation, typed 400 | `payload-builder-handlers.ts`, `payload.ts` (25 blocking / 2 warning codes, exported + exhaustiveness-checked) | `test/leadgen-test-tool.test.ts` + `leadgen-payload.test.ts` (Test AND Save 400 matrix; never bare 500; `warnings[]` always) | **PASS** |
| B8 | P2 | 2 | `04` §4.4 computed dropdown from registry (UI half of R3) | `ui-payload-builder.ts` (shared `computedOptionLabel`) | UI tests (registry-only options w/ `label — description (example)`) · spec ① computed fallback picked from the dropdown | **PASS** |
| B9 | P2 | 2 (+1 rt) | `06` §6.4 choiceDisplay + Other renderer; never literal "Other" | P1: `components/presets.ts`, `config-dto.ts`, `runtime/render.ts` · P2: `payload.ts`/`content-schema.ts` acceptance + value-map grouping editor + CSV import | P1: `leadgen-components-render.test.ts` (never-literal-Other; hostile-label escaping) · P2: choiceDisplay acceptance matrix + spec ① (CSV 10 rows → `3 main · 9 in Other` chips; searchable toggle; stored `choiceDisplay.mainValues` asserted) | **PASS** (both legs) |
| B10 | P2 | 2 | `06` §6.7 six boolean presets | `ui-payload-builder.ts`, `leadgen/payload.ts` | `leadgen-payload-formats.test.ts` (all 6 preset output shapes through real buildPayload) · spec ① yn preset chips | **PASS** |
| B11 | P2 | 2 | `06` §6.6 date mode + format picker → formatDate | `ui-payload-builder.ts`, `leadgen/payload.ts` | formats matrix (5 formats + custom tokens + invalid→fallback; Unix = DEV-36 erratum) · spec ① live input→output preview ×3 | **PASS** |
| B12 | P2 | 2 | `06` §6.5 explicit free-text toggle + optional constraints | `ui-payload-builder.ts`, `leadgen/payload.ts` (`free_text_max_length`/`_pattern`/`_pattern_custom` + `sanitizeFreeText` + `FREE_TEXT_RUNTIME_INPUT_HARD_CAP` unconditional input-bound before `.test()` at the shared resolveNode choke point + widened save-time bomb heuristic) | §6.5 validation matrix (nested + alternation bombs rejected) + runtime matrix + UI round-trip + **MAJOR-1 ReDoS regressions: alternation-bomb pattern × 5000-char input returns <250ms via fallback (`.test()` never reached over the 4096 cap), incl. the max-length-UNSET path** | **PASS** (post-DEV-37 remediation) |
| C1 | P1 | 2 | `06` §6.12 Test tab operable with zero JSON (B4+B5+B7+pickers+env) | `ui-payload-builder.ts`, `payload-builder-handlers.ts` | spec ⑤ end-to-end: generated form → edit → context panel → placement pick → env select w/ production-confirm (dismissed ⇒ 0 POSTs) → staging run → masked result + `context_used` — zero JSON; mock `/__requests` captures the exact payload | **PASS** |
| D1 | P2 | 3 | `07` §7.5 two labeled behaviors; enum frozen; erratum recorded | `leadgen/rules.ts` (label constants + frozen-alias erratum doc), `ui-offers.ts` ("Allow only these regions"/"Block these regions") | `test/leadgen-validation.test.ts` (every dimension×action still accepted — evaluation semantics FROZEN) · `test/leadgen-offers-ui.test.ts` (two labeled actions render) | **PASS** |
| D2 | P3 | 3 | `07` §7.5 "Evaluation order" label + help | `ui-offers.ts` | `test/leadgen-offers-ui.test.ts` (label + help render) | **PASS** |
| D3 | P2 | 3 | `07` §7.5 per-dimension region validators + chips + paste-multiple | `leadgen/validation.ts` (`regionValueError` — ZIP 5-digit, country/state alpha-2), `ui-offers.ts` (country/state dropdowns + ZIP chips + paste) | `test/leadgen-validation.test.ts` (D3: bad zip/country/state → `region_value_invalid`; paste-multiple per-index; city free-text) | **PASS** |
| E1 | P1 | 4 | `08` §8.2 Activity/Vertical comboboxes + pair warning | `ui-section-studio.ts` (top-bar SELECTS fed by `/activities` + `/verticals?activity=`; Activity change resets Vertical; "+ New activity/vertical…" behind the explicit "No Offers exist for '<x>' yet" confirm; save-time zero-match warning), `offers-handlers.ts` (`listVerticalsHandler` additive `?activity=` — every union leg filters by its own activity column incl. the quotes `json_each` leg) | `test-ui/leadgen-section-studio.spec.ts` ① (REAL browser: both seeded Offer activities appear in the dropdown; picking one filters the vertical set; switching RESETS the selection + swaps the set) · `test/leadgen-offers-api.test.ts` "filters every union leg; the bare path stays the global union" (filtered-vs-global regression) · `test/leadgen-section-studio-ui.test.ts` §8.2 SSR/island describe (SELECTs not free text; island wiring; confirm copy) | **PASS** |
| E2 | P1 | 4 | `08` §8.7 mapping via pickers + value-map modal + per-row test | `ui-section-studio.ts` (drawer §8.7 panel: Offer·Provider·Placement·schema-version·required/mapped·status table; Map-fields grid with path/question PICKERS type-compatible-first; create-question-for-field; bulk-map w/ review list; per-offer validate-payload preview; §6.3 value-map + schema DEEP LINKS `/admin/leadgen/offers/:id/edit#payload`; §8.6 inspector Mapping quick-map; raw ids/paths/JSON GONE from the surface; `selected_offers` persisted through save), `sections-handlers.ts` (`GET /sections/:id/offers` additive `answer_fields` = the ACTIVE schema's `source:"answer"` nodes {path,type,required,valid_values} + schema version + default placement), `ui-sections.ts` (blob `selected_offers`) | `test-ui/leadgen-section-studio.spec.ts` ③ (pickers-only mapping of TWO offers: statuses flip, badge live-updates "Mapping 2/2 Offers complete", save round-trips + fresh SSR re-derives) + ④ (create-question-for-field spawns pre-bound TwoButtonYesNo, path-derived internal_field, immediately-complete edge) + ⑤ (R5 preflight blocks on missing required mapping; "Open Section Mapping" `#mapping` deep-link opens the drawer) · `test/leadgen-section-studio-ui.test.ts` §8.7 describes (EXECUTED island↔REAL-PATCH round trip; SEAM matrix island decode ≡ `rebuildDerivedIndexes` per state; ported §12.11 cell copy; bulk heuristic; deselect drops edges; selected-but-unmapped survives save) | **PASS** (value-map editing = deep link into the §6.3 payload-builder table — in-panel modal is a follow-up; per-row test = the per-offer validate-payload preview) |
| E3 | P2 | 4 | `08` §§8.3–8.6 thumbnail library / drag-drop canvas / tabbed inspector | `ui-section-studio.ts` (D1: §8.1 five-region shell; §8.3 six-group searchable library — thumbnails rendered FROM each component's OWN preset (`renderComponent(STUDIO_SAMPLE_NODES[type], design)` CSS-scaled, never hand-drawn); §8.4 canvas via the REAL renderer re-rendered through the slice-C preview endpoint (parity by construction), drag-drop before/after/into-container with depth-refusal, selection outline+breadcrumb, keyboard reorder, duplicate/delete/add-before/after/group-into-Stack; §8.6 tabs Content/Choices/Layout/Design(curated token selects)/Validation/Dependencies(typed §6.10 builder)/Mapping/Advanced-only-raw-JSON; F2: choiceDisplay controls wired, TrustBar/LogoStrip/StepIndicator inspector controls, events-list CSS containment) | `test/leadgen-section-studio-ui.test.ts` (55+ EXECUTED vm-probes incl. two live-router seams; preset-rendered-thumb proof) · `test-ui/leadgen-section-studio.spec.ts` ①–⑦ + `test-ui/leadgen-studio-patterns.spec.ts` (all four §8.11 patterns authored 100% through this UI) | **PASS** |
| E4 | P2 | 4 | `08` §8.5 tokenized layout containers (9) | `components/content-schema.ts` (recursive model: `children` on the 5 container types, depth ≤4, tree-wide uniqueness SCOPED to answer-producing types (F2 fix), typed `container_prop_invalid` per enum; canonical `flattenComponents`), `components/presets.ts` (recursive render + Stack/GridContainer/Columns/CardPanel/BackgroundPanel/Spacer/HeaderBar/FooterBar presets), `components/registry.ts` (category `layout`), designs (8 token groups + scoped rules incl. mobile legs); client runtime UNTOUCHED (bundle byte-identical — containers are a server-render concern; every consumer flattens) | `test/leadgen-sections.test.ts` + `test/leadgen-components-render.test.ts` (depth/uniqueness/circularity/prop-enum matrices; §8.13 legacy flat fixture byte-identical; serialization round-trip; nested≡flat dependencies/answers/config-DTO) · `test-ui/leadgen-studio-patterns.spec.ts` patterns 1–4 (containers authored + persisted through the UI) | **PASS** |
| E5 | P1 | 4 | `09` §9.2 parameterized server-rendered sims; design honored; reversible viewports | `sections-handlers.ts` (slice C: `design_id`/`viewport`/`sim` server-rendered; D2: additive `runtime:true` → `preview.events_html` — the 03 §3.2 shell-shaped preview document: `data-lg-preview="1"` root, `#lg-config` through the REAL `toPublicComponent` projection, the BYTE-IDENTICAL generated bundle inlined), studio preview drawer (slice C wiring + D2 events panel) | slice-C §9.2 executed island probes (`leadgen-sections-ui.test.ts` runPreview vs the REAL handler; sims server-rendered) · D2: `test/leadgen-section-studio-ui.test.ts` §8.9/§9.1 describe (events_html structure + config ≡ projection + bundle byte-containment + mobile/desktop markers; onPreviewMessage probe) · `test-ui/leadgen-section-studio.spec.ts` ⑥ (LIVE: engine boots in preview mode, `quote_view`/`section_view` boot beacons + click-driven `answer_click` listed in the panel — §14.9 analytics-preview) | **PASS** (§9.2 sims + §9.1 preview-hydration/events + §9.4 viewport round-trip — REAL 375px, no transform, changed-pixel ratio 0, `leadgen-studio-patterns.spec.ts`; F2: non-default sims render STATIC so hydration can never re-hide server-revealed state). §9.3 parity matrix shipped Fix-P5: `test/leadgen-parity.test.ts` (§9.5 shared-renderer invariant + per-component preview≡live subtree + §9.3 dependency-op table) |
| E6 | P2 | 4 (+1 rt) | `08` §8.8 field-level Maps config + engine wiring + key-missing fallback | `components/presets.ts` (`data-lg-maps` emission), `runtime/maps.ts` (SDK loader `maybeInjectMapsSdk` + Places wiring + graceful no-op); (P4: Studio config UI) | `test/leadgen-components-render.test.ts` (§8.8 data-lg-maps matrix) · `test/leadgen-runtime-hydration.test.ts` M2 block (SDK src shape + re-init on ready + `address_components` field mask + no-key/no-fields no-ops) — the wiring was DEAD pre-review (adversarial M2/M3), fixed with before/after proof | **PASS** (P1 runtime leg + P4 Studio config UI: dedicated Maps inspector tab writing EXACTLY the runtime's `parseMapsConfig` keys — executable cross-check in `leadgen-runtime-hydration.test.ts`; ZIP feature auto-gates `enable_autocomplete` so config is never dead at runtime; canvas fills-chips; verbatim key-missing banner; Playwright ⑦ configure→persist→clear. Browser-KEYED G6 autofill behavior = operator-owned residual — no real key in CI) |
| E7 | P2 | 4 | `08` §8.10 NumberInputQuestion + CurrencyInputQuestion presets | `components/registry.ts` + `content-schema.ts` + `presets.ts` (plain numeric inputs — NOT sliders; currency prefix from props; inputmode=numeric; data-min/max/step) + designs | `test/leadgen-components-render.test.ts` (lockstep NODE_SPECS + currency-prefix + no-inline-style assertions) · authored live in `test-ui/leadgen-studio-patterns.spec.ts` flows | **PASS** |
| E8 | P3 | 4 | `08` §8.10 SuccessState + success styling | `components/registry.ts` + `presets.ts` (role=status panel) + designs (`successState` token group + scoped rules; `.lg-valid` success styling on inputs via the validation_success sim) | `test/leadgen-components-render.test.ts` (render + designs signature) · `test-ui/leadgen-studio-patterns.spec.ts` (validation_success sim visibly distinct) | **PASS** |
| E9 | P3 | 4 | `08` §8.2 explicit no-Offers empty state | `ui-section-studio.ts` (drawer E9 slot: the verbatim "No active Offers match Activity '<a>' + Vertical '<v>'." copy from the SAVED pair the derivation used + [Open Offers] + [Change Activity/Vertical] focus affordance; never a silent empty list), `sections-handlers.ts` (`GET /sections/:id/offers` echoes the saved activity/vertical the copy names) | `test-ui/leadgen-section-studio.spec.ts` ② (REAL browser: exact copy + both affordances + focus moves to the pair controls) · `test/leadgen-section-studio-ui.test.ts` (E9 skeleton + the island's exact copy template) | **PASS** |
| S1 | P2 | 3 | `07` §7.6 simulate: redacted per-offer payload + parser + exclusions; no writes, staging-only carrier resolve (MAJOR-5 wording) | `auctions-handlers.ts` (`offers_payload_explain`: EXACT-payload preview — the runtime context is built PER OFFER via `buildLeadgenRuntimeContext` with that offer's slice, so `source:"macro"` incl. the offer-scoped `offer_id`/`offer_name`/`placement`, `source:"computed"`, and `source:"placement"` (PROVIDER-FACING external id, 04 §4.5) all resolve exactly as the engine threads them — MAJOR-3 + re-review residual fix; `source:"token"` renders present-but-MASKED via the `[REDACTED]` sentinel ONLY when the engine would inject it — payload placement + server mode AND a secret that RESOLVES in-env (gated by `readEnvSecret` used as a boolean presence check; the real secret value NEVER enters the preview), else the token node is absent, matching the live payload; + parser id/version + expected fields + exclusion reason), `ui-auctions.ts` (simulate trace panel), `leadgen/redact.ts` | `test/leadgen-auction-simulate.test.ts` "offers_payload_explain carries the REDACTED payload preview + parser id/version + expected fields" · "S1 MAJOR fix: the preview is the EXACT payload — request macro, OFFER-scoped macros, placement + masked token all resolve" (L259 — every source class: `macro:"utm_source"`→simulated ctx, `macro:"offer_id/offer_name"`, `macro:"placement"`+`source:"placement"`→external id, `source:"computed"`→request_timestamp, `source:"token"`→`[REDACTED]`, and the real secret string absent) · "S1 token gate: a payload+server offer whose secret does NOT resolve shows NO token field" (the engine-parity negative case) · `test/leadgen-auctions-ui.test.ts` (panel reads `offers_payload_explain` — the cross-slice seam) | **PASS** (post-remediation + 2 re-reviews) |
| T1 | P1 | 1+5 | `11` §§11.2/11.6 live-funnel suites + permanent anti-false-PASS + §9.3/§9.5 preview↔runtime parity | `test-ui/leadgen-live-funnel.spec.ts` (8 G1 tests + §11.6 permanent set), `leadgen-runtime.spec.ts` + `leadgen-perf.spec.ts` (false-comfort assertions replaced per §11.6), `test-ui/leadgen-visual.spec.ts` (**re-pointed to real `/lg` via `page.goto` — no `setContent`**), `test/leadgen-parity.test.ts` (§9.5 shared-renderer invariant + per-component preview≡live subtree + §9.3 dependency-op table, all 9 ops), `scripts/leadgen-mock-provider.ts`, `test-ui/leadgen-fix-p1-seed.ts` | **§11.6 now 5/5** (4 in-funnel legs: empty-mount/zero-questions/no-`answer_click`/`/lg/auction`-never-called + leg-5 visual-suite `setContent` tripwire); `leadgen-parity.test.ts` re-executed 2026-07-09 green (part of 115); Playwright `--list` 43 tests exit 0; CLS WITH content 0.000412 < 0.01 | **PASS** (both legs — P1 live-funnel + P5 parity/visual-re-point) |
| T2 | P1 | 5 | `11` §§11.5/11.7 52-row re-audit + §10.2/§10.3 producer coverage + executed signed manual QA | `docs/leadgen/traceability.md` (re-audited), `test/leadgen-analytics-producers.test.ts`, `test/leadgen-attribution-chain.test.ts`, `docs/leadgen/manualQA.md` | 52-row re-audit DONE (v2.4 RE-AUDIT header + per-row runtime citations; CP3/CP5 reclassified; P11 deferral documented); `leadgen-analytics-producers.test.ts` (10) §10.2 = 31/31 producers over 4 proof routes (15 server_emitted/2 impression-row/12 client_beacon/**2 DEV-32 deferred opening-lander**) + `leadgen-attribution-chain.test.ts` (2) §10.3 — both re-executed 2026-07-09 green | **PASS** (docs re-audit + §10.2/§10.3 proof); **manual-QA EXECUTION operator-owned BLOCKED(`manual_qa_visual`)** — sign-off table below |
| M1 | P3 | 1 | `04` §4.3 `referrer` → `referer` alias | `leadgen/macros.ts` | `test/leadgen-runtime-context.test.ts` (alias resolves through normalization) | **PASS** |
| M2 | P3 | 2 | `06` §6.2 grouped macro source picker | `ui-payload-builder.ts` (9-group source picker; flat 32-option select removed from normal mode; Advanced-macro optgroups Advanced-only) | UI tests (group structure + inline help + member/storage mappings) · spec ① drives the grouped picker | **PASS** |

Row count: 42 (R×9, A×3, B×12, C×1, D×3, E×9, S×1, T×2, M×2) — 1:1 with contract `13`.

## Phase verification headers (filled at each phase exit — gate outputs with counts, rows flipped, PR#, adversarial-review verdict)

### Fix-P1 — Runtime engine + P0 gates — CODE COMPLETE 2026-07-08 (branch `leadgen/fix-p1-runtime-engine`; adversarial review + PR pending)
- **Commits:** docs `c62dc1f`+`4786b72` · scaffolding `94e6f99` (mig 0040 + anchor + esbuild 0.28.1) · slice A `b403ee7` (runtime context + computed) · slice C2 `81d4c7b` (hydration engine + bundle) · slice C1 `31c9369` (shell server-render + ab_rev axis) · slice B `dcb6658` (auction path + both P0 gates + tuple v2) · wiring `ea341b2` (bundle route) · slice D `1883466` (gate UI surfaces) · slice C3 `da49a09` (G1 E2E + anti-false-PASS + slotTarget fix).
- **Exit gates (conductor's own hand, from `api/`, log `fix-p1-exit-gates.log`):** `tsc --noEmit` 0 · vitest **3413/3413 (303 files)** — baseline was 3257/299 (+156 tests) · `verify:all` green incl. NEW `verify:leadgen-runtime` (bundle **37,834 B ≤ 40,960**, byte-fresh rebuild) · Playwright **99/99** (baseline 91; ritual: wipe `.wrangler/state/v3/d1` → `db:migrate:local` → `seed:local`).
- **Live runtime proof:** `api/test-artifacts/fix-p1/{desktop-1280,mobile-375}-{first-question,banners}.png` + mock-provider-captured payload: `{"lead":{"homeowner_status":"own","zip":"90210"},"traffic":{"utm_source":"fixp1-fb"},"meta":{"ua":"<real Chrome UA>","request_timestamp":1783473907,"placement_id":"plc-fixp1-…"}}` — value-mapped answer, token-persisted traffic, real UA macro, numeric computed, participating placement (E4-grade: intercepted HTTP, not queues).
- **Acceptance:** `03` §3.11 ✓ (all bullets, G1-asserted) · `04` §4.9 ✓ (incl. the multi-placement leg — true only POST-remediation: the §11.3 multi-placement matrix test was missing and the behavior defective until adversarial M1) · `05` §5.7 ✓ · anti-false-PASS **4/5 legs** present + green (the 5th — the visual-suite setContent tripwire — lands with the Phase-5 `leadgen-visual.spec.ts` re-point per contract `12`) **[closed in Fix-P5: now 5/5 DONE — see the Fix-P5 header + DEV-48]** · migrations additive-only ✓ · no banned tokens ✓.
- **Adversarial review (fresh-context, money-path weighted): FIX-FIRST — 0 BLOCKER / 5 MAJOR / 16 MINOR.** Money path verified-clean under attack (tamper forgery paths, cache poisoning/cross-visitor leakage, impression double-count, redaction, revenue double-book — all held; 10+ normative MUSTs spot-audited exact). All 5 MAJORs + 13 minors REMEDIATED in-phase (`d731687`) with fail-before/pass-after regression tests: M1 multi-placement row-keying, M2 Maps SDK loader, M3 field-mask typo, M4+m7 prehydrate replay, M5 progress-bar hooks, m1 v1-downgrade hardening, m2 cookie-blocked session echo, m3 re-match `revenue_received`, m6 landing_url cap, m9–m12/m14–m15/m4-label. Deferred by design: m5 (this 4/5 qualifier), m13 (orphaned `serveLeadgenAttempt` — P5, DEV-33), m16 (ui-auctions.ts scope note: not in `12`'s Phase-1 file list; the change is strictly the §5.1-site-2 warning surface — accepted in-spirit). Residual documented in DEV-35. **Post-remediation gates (conductor's own hand): tsc 0 · vitest 3433/3433 (304 files) · verify:all green (bundle 39,157 B = 95.6%) · Playwright 99/99** (log `fix-p1-exit-gates2.log`).
- **Focused re-review of the remediation (fresh context): SHIP** — every finding verified genuinely closed (M1 row-keying across all five legs; M2 injector XSS-safe/once-only; m2 Set-Cookie confined to the no-store attempt path; m6 truncation safe at every consumer); reviewer reproduced tsc 0 · verify:all · vitest 3433/3433 by own hand. Non-blocking notes: matrix test asserts impressions/logs directly, request/response event placement leg code-verified; m3 re-match emission is telemetry-at-most-once-per-sweep (never double-books).
- PR: **#88** — `Fix Phase 1 (LeadGen v2.4): runtime engine + P0 gates` (squash-merged via the delegated loop; deploy remains operator-owned).

### Fix-P2 — Payload Builder UX — CODE COMPLETE 2026-07-08 (branch `leadgen/fix-p2-payload-builder`; adversarial review + PR pending)
- **Commits:** server `dda8d5e` (B4 endpoint + B7 gates + B9 acceptance + builder_context) · UI `d1db2dc` (three-pane rebuild 1232→4499 lines) · follow-up `598cc2c` (§6.5 constraints + §6.9 computed refs + last_test_at) · G5 `a8be5c8` (5 operator-flow tests + stale-DOM rewrite) · F-1 `53bf18f` (eq/neq reachable; §6.10's own example authorable).
- **Exit gates (conductor's own hand, from `api/`, log `fix-p2-exit-gates.log`):** `tsc --noEmit` 0 · vitest **3577/3577 (307 files)** · `verify:all` green (bundle unchanged 39,157 B) · Playwright **104/104**.
- **Operator proof:** `api/test-artifacts/fix-p2/` (value-map CSV modal, three-pane builder, 5-issues panel, Test tab form + result screenshots + `stored-schema-v1.json` — the schema a zero-JSON flow produced, path-set asserted).
- **G5 found F-1 in-phase** (eq/neq-with-value unreachable — the empty-value sugar swallowed the op pick): fixed choice-aware in `fillConditionPanel` with a vm-executed production-island probe (fail-before/pass-after) + Playwright authoring `homeowner = true` asserting stored `{when,op:"eq",value:true}`.
- **Acceptance:** `06` §6.14 held — nested schema + CSV value map w/ main+Other + date/boolean/free-text + conditions + defaults/fallbacks authored with ZERO raw JSON typed (focusin-tracker = 0 on `[data-raw-json]`); raw JSON exists only behind Advanced (structural + rendered-page asserted); storage formats unchanged (byte-equivalent re-save regression).
- **Adversarial review (fresh-context, money-path weighted): FIX-FIRST — 1 MAJOR + 6 MINOR** (money path otherwise verified-clean under attack: XSS swept — createElement/createTextNode only, `<script>`-island JSON breakout impossible; pre-save gate no-bypass; no public-DTO leak; §6.14 no-normal-mode-JSON holds; byte-compat canaries; DEV-36 calls re-confirmed sound). **MAJOR-1 (money-path ReDoS):** the §6.5 custom regex ran `.test()` on unbounded visitor answers at auction time — reviewer PoC stalled the build ~12s with a 36-byte input; my DEV-36(e) "safe" claim was FALSE (cap was on the pattern, not the input). Remediated by conductor's own hand (DEV-37): unconditional runtime input hard-cap before any `.test()` (over-cap → fallback) + widened save-time heuristic (alternation-bomb class) + before/after ReDoS regressions. 5 of 6 minors fixed with regressions (M2 boolean "= false" no silent is-empty; M3 string-field value no longer coerced to boolean → no silent conditional drop; M4 sample-answers draft 64 KB cap + TTL; M6 CSV mid-cell-quote split; nano-7 reserved-key value-map guard); MINOR-5 = documented trade-off (no change). Two subagent dispatches flaked (0 tool calls) → conductor executed the remediation directly; a backtick-in-island-template-literal slip was caught by tsc pre-commit (the verify-by-own-hand discipline working).
- **Post-remediation exit gates (conductor's own hand, `fix-p2-remediation-gates.log`):** tsc 0 · vitest **3582/3582 (307 files)** · verify:all green (bundle 39,157 B unchanged — payload.ts/ui not in the bundle) · Playwright **104/104**.
- **Focused re-review of the remediation (fresh context): FIX-FIRST** — 5 minors + nano-7 verified genuinely closed, but MAJOR-1 was NOT: the re-reviewer PROVED (V8 timing) that `((a)+)+$` evades both flat shape-regexes (they can't cross nested parens) and hangs a ~30-char input in seconds — an input-length cap can never bound EXPONENTIAL backtracking (it explodes far under 4096). My "exponential over 4096 is safe" rationale was empirically false; the save screen was the only real guard and it was evadable. **Re-remediated (DEV-38):** replaced the flat regexes with a paren-depth-aware structural analyzer (`isCatastrophicRegexShape`) that propagates child quantifier/alternation flags up through nesting — SOUND against the whole nested-quantifier + quantified-alternation family — AND added the SAME screen as a runtime layer-2 in `applyFreeTextConstraints` (refuses a catastrophic stored pattern before `.test()`, defending legacy/hand-edited data). Regressions now prove both layers: save rejects `((a+))+$`/`(([a-z])+)+$`/`((ab)*)*$` (was accepted), runtime returns fallback <250ms for a stored `((a)+)+$` at in-cap length. New MINOR (DEV-38): the screen conservatively over-rejects safe quantified-alternations like `(cat|dog)+` (safe-side; author as valid_values / Advanced) — usability follow-up, not security.
- **Post-re-remediation exit gates (conductor's own hand, `fix-p2-redos-gates.log`):** tsc 0 · vitest **3582/3582 (307 files)** · verify:all green · Playwright **104/104**.
- **Analyzer soundness verification (conductor's own hand — 3 consecutive subagent-dispatch flakes forced direct execution; this IS the verify-by-own-hand doctrine):** drove the REAL save screen (`validatePayloadSchema().errors`) over a 15-pattern exponential-evasion battery + timed real regexes in V8 (the engine workerd embeds) via kill-guarded child processes. Results: **all 15 exponential-family constructs REJECTED at save** — deep nesting `(((a)+)+)+`, non-capturing `(?:a+)+`, lookahead `(a+)+(?=x)`, escaped-paren `\((a+)+\)`, `{n,}`-nest `(a{2,}){2,}`, nested-alternation `(((a|a)))+`, `(a*)*`, `(\d+)*`. Screen-ACCEPTED polynomial/backref shapes the analyzer doesn't target (`(.*)(.*)(.*)!`, `(a*)(a*)(a*)!`, `(a+)\1+`, `\s*\S*\s*\S*\s*\S*!`) are empirically **<0.05ms at 4096 chars in V8/Irregexp** — V8 optimizes consecutive greedy stars, so they are not a DoS in the deployed runtime (a naive/PCRE backtracker would differ, but Workers runs V8). Legit patterns (`^[A-Za-z ]+$`, `^\d{5}$`, `^\w+@\w+\.\w+$`, 300-char class) accepted + fast. Only false-positive: `(cat|dog)+` (safe quantified-alternation) — documented safe-side (DEV-38). **VERDICT: SHIP** — two-layer defense (sound exponential save+runtime screen + 4096 polynomial cap) bounds every constructible pattern to acceptable time on the deployed engine.
- PR: **#89** — `Fix Phase 2 (LeadGen v2.4): Payload Builder UX` (squash-merged via the delegated loop).
### Fix-P3 — Offer management + rules — REMEDIATION COMPLETE 2026-07-08 (branch `leadgen/fix-p3-offer-mgmt`, from main @ 07d7916; adversarial review returned BLOCK → all findings fixed in-phase → re-gated green; PR pending)
- Scope: `07` A1 (usage-guarded hard delete + §9.6 erratum), A2 (duplicate Q3 copy-set), A3 (full usage inventory), D1/D2 (region labels), D3 (region validators), S1 (simulate payload explainability). All 7 rows PASS (post-remediation).
- **Adversarial review (fresh-context, data-integrity weighted): BLOCK — 2 BLOCKER + 3 MAJOR** (+ minors). Money/deletion path was the review focus. **BLOCKER-1+2 (A1 delete guard incomplete — the data-integrity crux):** `buildOfferUsageReport` omitted two reference classes an offer can be pointed at, so a hard-delete could orphan live config — (1) `funnel_rules_targeting` (a `leadgen_funnel_rules.target_offer_id` pointing at the offer, via variant→funnel), (2) `auction_backfill_source` (an offer named as an auction's backfill source). **MAJOR-3 (S1 preview not representative):** the `offers_payload_explain` preview was built from ANSWERS ALONE — it dropped the macros/computed/placement the real engine threads, so `source:"macro"`/`"computed"`/`"placement"` fields rendered empty/wrong, misleading the operator about what actually gets sent. **MAJOR-4 (A3 inventory gaps):** the usage report omitted `quotes_indirect` (offers reachable through a quote's funnel), `region_rules`, and `revenue_attribution` reference kinds. **A2 atomicity (minor→fixed):** duplicate created the offer then its placement in two steps with no compensating delete on partial failure. All fixed in-phase (see DEV-40) with fail-before/pass-after regressions.
- **MAJOR-5 (documentation, not code):** the reviewer flagged that the traceability/§7.6 wording "**stays dry**" is imprecise — `simulate` does resolve carrier metadata via the provider **staging** endpoint (pre-existing P10 auction-core behavior; it performs NO writes and NO production-endpoint call, which is what "dry" was asserting). Corrected to "no writes; staging-only carrier resolve" in the S1 row + DEV-40 erratum; no code change (the behavior is correct and pre-dates this mission).
- **Focused re-review of the remediation (fresh context, data-integrity weighted): FIX-FIRST — 1 residual MAJOR + 1 MINOR.** The two BLOCKERs (A1 guard) were CONFIRMED complete — the reviewer independently enumerated all 12 offer FK columns and proved each is either a blocking/warning kind or explicitly deleted in the hard-delete cascade (D1 FKs are not runtime-enforced, so schema `ON DELETE CASCADE` does NOT cascade — explicit deletes verified). A3/A2 CONFIRMED. **Residual MAJOR-3:** my first S1 fix built ONE `simCtx` OUTSIDE the loop with no offer slice, so the three OFFER-scoped macros (`contextToMacros` derives `offer_id`/`offer_name`/`placement` from `ctx.offer`) resolved empty/wrong — a `source:"macro" macro:"offer_id"` node rendered `""` and `macro:"placement"` rendered the traffic placement, not the offer's. My regression only exercised the request-scoped `utm_source` + `source:"placement"`, giving false confidence. **MINOR:** `source:"token"` nodes were dropped (not present-but-masked per §7.6). Both fixed (DEV-41): the runtime context is now rebuilt PER OFFER inside the loop with that offer's slice, and a `[REDACTED]` sentinel is injected as the token value so token nodes render masked WITHOUT the real secret ever being resolved; the regression now asserts all six source classes.
- **Second focused re-review (fresh context): FIX-FIRST — 1 MINOR introduced by the residual fix.** The reviewer independently re-CONFIRMED MAJOR-3 closed (per-offer context; all offer-scoped macros resolve) and the token masking secret-safe, but caught that my token fix (DEV-41) OVER-injected: it rendered `[REDACTED]` on placement+mode alone, whereas the engine (`fetch.ts`) and the Test-tool peer gate token injection on the secret ALSO resolving. An offer with a NULL/undeployed secret would show a spurious masked token the live payload omits — ironically hiding a missing-secret misconfig in the preview tool. Fixed (DEV-42): added `api_token_secret_ref` to the query and gate injection on `readEnvSecret(...) !== undefined` (boolean presence only; real value never enters the preview). New negative regression proves the token node is ABSENT when the secret is unresolvable.
- **Third focused re-review (fresh context): SHIP.** The reviewer walked the full token-gate truth table (24 cells: placement × mode × secret-state) and confirmed the simulate's present/absent token decision matches the live engine cell-for-cell (both funnel through `resolveNode`'s payload+server condition; the secret-resolves condition is now identical), the real secret value never enters the preview, and both regressions are genuine fail-before/pass-after. Only a non-blocking test-durability nicety was noted (the negative test covered null-ref but not env-drift); ADDED in-phase — the "S1 token gate" test now also seeds an env-drift offer (`api_token_secret_ref` set but absent from env) and asserts its token node is absent too. **VERDICT: SHIP.**
- **Final exit gates (conductor's own hand):** tsc 0 · vitest **3606/3606 (307 files)** (baseline 3582; +24 Phase-3 incl. the remediation regressions) · verify:all green (scanner + infra 8/8 + worker-config 3/3 + leadgen-runtime **39,157 B = 95.6%**, byte-unchanged — Phase 3 is server/admin-side) · Playwright **104/104** (one earlier full-run flake in `listicles-offers.spec.ts:54` — a caret/chip timing race under parallel load — CONFIRMED pre-existing + unrelated: passes in isolation AND on clean re-runs; my diff is LeadGen-server-side only, zero listicles/UI/bundle change).
- **Implementation note:** the server slice (delete guard/duplicate/usage/validators/simulate) was written BY THE CONDUCTOR'S OWN HAND after subagent dispatch degraded (content-filter block + connection-drop mid-run left only partial work). The UI slice landed via subagent but its report was lost to a connection error — verified by hand (tsc, all affected suites, diff review). **Cross-slice bug caught + fixed:** the S1 simulate panel read `offers_considered` (only `{offer_id,placement_id}`) instead of the additive `offers_payload_explain` array, so the entire §7.6 explainability rendered empty at runtime — fixed + now covered by a real endpoint→UI seam test (DEV-39).
- Adversarial review + PR: addendum below.

### Retroactive 1:1 contract audits of Fix-P2 + Fix-P3 (operator-ordered, 2026-07-08) — REMEDIATED on `leadgen/fix-p2p3-audit-remediation`

- **Why:** the operator caught the conductor deviating from the approved delegated execution loop (hand-implementing after a subagent flake instead of re-dispatching) and ordered both merged phases re-validated 1:1 against the vendored contracts before Phase 4 — self-reported PASS rows lost the benefit of the doubt.
- **Method:** two fresh-context adversarial auditors (no implementation context; contract + `02` matrix + code + executed tests only; traceability rows treated as claims, not truth), one per phase, exhaustive per-§ requirement tables, all DEV errata independently fact-checked.
- **Fix-P2 verdict: MISALIGNED — 4 MINOR + 1 adjacent-quality finding; every DEV-36/37/38 erratum verified SOUND; ReDoS two-layer defense independently re-executed and confirmed.** Findings: F-1 §6.10 condition value inputs not fully typed (enum→text instead of dropdown; in/not_in→comma text instead of chips); F-2 §6.4 drag-between-groups absent (buttons only, no erratum); F-3 §6-preamble "(no route changes)" contradicted by the two additive §6.12.1 draft routes (no erratum); F-4 §6.13 vitest-letter gaps (renamePrefix descendant rewrite only marker-tested; value-map round-trip Playwright-only); F-5 stale client-side ReDoS mirror (pre-DEV-38 flat regex; server-side fail-closed so no exposure — misleading live validation only).
- **Fix-P3 verdict: MISALIGNED — 2 BLOCKER + 2 MAJOR + 9 MINOR.** The server slice was contract-faithful; the UI slice (the slice where the process deviation occurred, verified only by tsc + string-marker suites after its subagent report was lost) shipped TWO DEAD FLOWS the markers could not see: **F1 BLOCKER** — the usage island indexed `usage[kind]` as a keyed map while the server emits a `kinds[]` ARRAY → the Usage dialog AND the 409 blocked-delete modal rendered "(0) None." for every kind (A1/A3 UI dead); **F2 BLOCKER** — the duplicate success path required `res.body.public_id` (top-level) while the 201 body is `{offer:{public_id},…}` → duplicate always showed an error, never navigated, and every retry minted another clone (A2 UI dead); **F3 MAJOR** — S1 `expected_response_fields` was `Object.keys(<whole parse config>)` → always `["carriers_path","fields"]`, never the parser's field names; **F4 MAJOR** — §7.7's mandated Playwright browser flows were substituted with vitest string checks (the direct causal hole for F1/F2). Minors: F5 missing §7.1 test-status chip; F6 no §7.2 delete audit log; F7 duplicate not one transaction; F8 warning-kind scope/count-shape gaps; F9 UI dry-run note contradicting DEV-40; F10 pasted country/state tokens not client-validated; F11 `rules.ts` labels unconsumed + false comment; F12 §7.7 API-test shortfalls; F13 sentinel/`cap_fallback`/placement-id-reuse gaps.
- **Conductor's own-hand confirmation:** F1/F2/F3 verified against the real code before remediation (exact server shapes vs island lookups read side-by-side).
- **Remediation:** wave 1 (`e166799`) — all P3 server + UI findings, with EXECUTED island-vs-real-server-JSON seam regressions replacing the string markers, + a companion fix (hard delete of any offer with a saved schema threw an FK violation on `active_payload_schema_id` — cleared before the delete batch; exposed by the mandated F12 fixtures); wave 2 — P2 F-1/F-2/F-4/F-5 fixes (typed condition inputs incl. chips, HTML5 drag between value-map groups, executed renamePrefix + value-map round-trip regressions, ES5 port of the depth-aware ReDoS walker to the island) + DEV-43 erratum + the §7.7 Playwright browser flows (F4). Per-finding detail: DEV-44.
- **Process invariant (standing):** the approved plan's execution loop is binding; on a subagent flake RE-DISPATCH — never absorb implementation into the conductor; every delegated artifact is verified by the conductor's own hand; UI response-shape seams require EXECUTED client-vs-server-JSON tests, never `toContain` markers.

### Fix-P4 — Section Studio rebuild — CODE COMPLETE 2026-07-09 (branch `leadgen/fix-p4-section-studio`, from main @ `eb8b819`; adversarial review + PR pending)

- **All 9 rows PASS (E1–E9).** Contracts `08`+`09` read in full. Slices — each delegated (corrected loop), conductor-verified by own hand, committed on green targeted gates: **A** `9b6f237` (9 leaf components ×6 lockstep touchpoints) → **B** `ea05b35` (recursive container spine; canonical `flattenComponents`; every question-iterating consumer flattens — client runtime UNTOUCHED; found+fixed the nested-Maps `funnelNeedsMapsKey` money-path gap) → **C** `ae3a082` (preview `design_id`/`viewport`/`sim` server-rendered; the `:783-802` outer-iframe sim hacks DELETED) → **D1** `382443b` (Studio shell/library/canvas/inspector) → **D2** `c668888` (Activity/Vertical, mapping panel, §9.1 events panel via the REAL Phase-1 preview contract; 3 D1-latent bugs found by first browser exposure + fixed; `ui-question-builder.ts` DELETED) → **E** `255c939` (§8.8 Maps inspector tab, exact `parseMapsConfig` keys) → **F+F2** `987f7d4` (all four §8.11 patterns authored 100% THROUGH the UI + remaining §8.12 flows + §9.4 round-trip + §8.13 byte-equal legacy proof; the suite caught **4 real defects incl. a BLOCKER-class §8.13 hazard** — slice B had over-scoped internal_field uniqueness to ALL nodes, breaking ValidationError slot bindings and live re-saves — all fixed, red pins flipped green, never weakened).
- **Bundle outcome:** ZERO runtime-bundle change across the whole phase (byte-identical, 39,157 B = 95.6%) — the DEV-35 pressure never materialized because containers are a server-render concern by architecture.
- **Exit gates (conductor's own hand):** tsc 0 · vitest **3808/3808 (308 files)** (post-remediation baseline 3636; +172 Phase-4) · verify:all green · Playwright **134/134** (110 baseline + 7 studio flows + 17 patterns/evidence).
- Known follow-ups (non-blocking, recorded): in-panel value-map modal (deep link today, E2 note); §9.3 parity matrix = Fix-P5; browser-KEYED Maps G6 = operator-owned residual.
- **Adversarial review (fresh-context, money-path weighted): FIX-FIRST — 1 MAJOR + 2 MINOR → all fixed in-phase; convergence re-review: SHIP.** Phase-4's own deliverables were clean (flatten refactor byte-identical for legacy, components escape author values, seams match, Playwright pack honest). **MAJOR-1 (PRE-EXISTING stored XSS on the LIVE funnel renderer):** `CSS_ESCAPE_RE` (content-schema + its sections.ts twin) omitted `"`/`'`/backtick, so a `design_overrides` value like `red" autofocus onfocus="alert\`1\`"` validated ok and broke out of `style="…"` into real event-handler attributes executing in VISITOR browsers (reachable via Advanced raw-JSON / direct PATCH by an authed admin). Fixed both layers: widened both regexes + `style()` HTML-escapes every value (`ba74920`). MINOR-2: `answers.ts fieldsOf` treated a non-producing node's `internal_field` as an answer producer (a ValidationError bound to a boolean question re-coerced it to a string) → scoped to `produces!==null`. MINOR-3: preview `postMessage` listener gained an `event.source` origin gate. Then bumped `LEADGEN_TEMPLATE_VERSION` 1→2 (preset output changed this phase) so cached shells/configs roll forward + `LG_ENGINE_VERSION` to match. Re-review attacked all four with its own payloads (11 XSS breakouts rejected at both validators + entity-escaped past validation; no legit-token false-positive; object-producers still expand; version axis coherent; inlined events bundle byte-identical) → **VERDICT: SHIP**. Story: DEV-47.
- **Final exit gates (conductor's own hand, post-review):** tsc 0 · vitest **3812/3812 (308 files)** · verify:all green (bundle byte-identical 39,157 B) · Playwright **134/134**.

### Fix-P5 — Parity + analytics proof + QA honesty — CODE + DOCS COMPLETE 2026-07-09 (branch `leadgen/fix-p5-parity-qa`; full-suite phase-exit gate + fresh-context adversarial review + PR are the remaining phase steps)

- **T1, T2 → PASS.** Fix-P5 closes the two testing-contract rows the earlier phases could only half-fill: the preview↔runtime **parity** proof (T1), the analytics **producer-coverage + attribution-chain** proof (T2), the **§11.6 anti-false-PASS 5/5** completion, and the **52-row v2.3.7 re-audit** with the corrected runtime evidence standard (`11` §11.5).
- **Commits (this branch):** `2727bc5` — slices A+C: preview↔runtime parity (`test/leadgen-parity.test.ts`, T1) + anti-false-PASS honesty (`test-ui/leadgen-visual.spec.ts` re-pointed to real `/lg`; the `leadgen-live-funnel.spec.ts` §11.6 leg-5 `setContent` static tripwire) · `c84cb3b` — slice B: analytics producer coverage + attribution chain (`test/leadgen-analytics-producers.test.ts` + `test/leadgen-attribution-chain.test.ts`, §10.2/§10.3, T2). Docs re-audit (this deliverable): `docs/leadgen/traceability.md` (52-row v2.4 RE-AUDIT header + in-place runtime corrections) + this file's T1/T2 flip.
- **§11.6 anti-false-PASS is now 5/5 DONE** (Fix-P1 landed 4/5; the 5th — the visual-suite `setContent` tripwire — was explicitly deferred to Fix-P5 per contract `12` and is now present): (1) empty `[data-lg-mount]` after `data-lg-ready="1"` → FAIL · (2) zero `[data-lg-question]` on a question funnel → FAIL · (3) no `answer_click` NETWORK beacon after a scripted answer → FAIL · (4) `/lg/auction` never POSTed after the final section → FAIL · (5) `leadgen-visual.spec.ts` must `page.goto` a tenant `/lg/` URL and must NOT use `.setContent(` (static source assertion). All five live in `test-ui/leadgen-live-funnel.spec.ts` (`describe("§11.6 anti-false-PASS regression (permanent)")`).
- **T1 parity evidence** — `test/leadgen-parity.test.ts`: §9.5 shared-renderer invariant (preview authors NO component markup; `sections-handlers.ts` calls the `presets.ts` renderer; `presets.ts` is the sole `data-component-type` authority) + per-catalog-and-container-type `preview subtree ≡ live renderSectionComponents subtree` + nested-container recursion parity + identical design tokens + §9.3 dependency-operator parity table (server ≡ client over all 9 ops).
- **T2 analytics evidence** — `test/leadgen-analytics-producers.test.ts` (§10.2): the producer map is lockstep over the frozen 31 `LEADGEN_EVENT_TYPES` (a 32nd type without a producer fails here + at tsc); partitioned into four proof routes with no gap/overlap — **15 `server_emitted` · 2 `server_impression_row` · 12 `client_beacon` · 2 `deferred_no_surface`**; every producer's claimed emission-site `source_marker` is asserted to still exist in its `source_file` (EXECUTED read); `/lg/track` accepts every `client_beacon` type with no `invalid_event_type` dead-letter; the **DEV-32 deferred set is asserted to be EXACTLY the two opening-lander events** (`opening_lander_view`/`opening_lander_cta_click`) — the documented-conditional gap (`10` §10.2), accepted at `/lg/track` today so a future lander beacon will not dead-letter; carrier_click/offer_click by scope; all 9 auction-lifecycle + both redirect + both impression rows; a CH-matched booked conversion emits BOTH `conversion` + `revenue_received`. `test/leadgen-attribution-chain.test.ts` (§10.3): the attribution IDs persist across every server-owned leg (auction→impression→click→conversion→revenue) + a documented server-owned-vs-client-beacon coverage-honesty test.
- **Executed evidence (conductor's own hand, 2026-07-09, `npx vitest run` from `api/`):** `leadgen-parity` + `leadgen-analytics-producers` (10) + `leadgen-attribution-chain` (2) → **115 passed**; `leadgen-runtime-context`(24)/`-engine`(35)/`-api`(29)/`-routes`(11)/`-hydration`(14) + `leadgen-gates`(29) → **142 passed**; `leadgen-computed` → **14 passed**. Playwright `leadgen-live-funnel`/`-section-studio`/`-studio-patterns`/`-visual` collectable via `npx playwright test --list` → **43 tests, exit 0**.
- **Honesty note (what this deliverable did NOT run):** this was the DOCS-ONLY T2 slice (no code/test edits). The FULL vitest + FULL Playwright browser phase-exit run, the fresh-context adversarial review, and the PR are the remaining Fix-P5 steps owned by the delegated loop — NOT claimed here. No PASS in either doc is written without a named executing test; every citation above was grep-verified to exist and the vitest ones re-executed green this session.
- **DEV register:** DEV-48.
- PR: pending (delegated loop).

## DEV register (continues v2.3.7's DEV-1…26 in `docs/leadgen/traceability.md`)

| # | Decision / evidence |
|---|---|
| DEV-27 | **Fix-mission bootstrap evidence (2026-07-08):** baseline on virgin `main @ 52905b2` ALL GREEN (tsc 0 · vitest 3257/3257 across 299 files · verify:all OK · Playwright 91/91 after the local-D1 wipe+migrate+seed ritual) — zero pre-existing failures, so every subsequent count-change is mission-attributable. v2.4 contract vendored to `docs/leadgen/fix-contract-v2.4/` (15 files + MANIFEST.sha256); byte-fidelity proven by per-file re-fetch + `cmp`: 15/15 BYTE-IDENTICAL; scanner green with ZERO allowlist additions (package is token-clean by authorship). Migration head confirmed `0039` → contract's `0040_leadgen_runtime_context.sql` numbering holds; deploy.yml anchor due in the same commit that adds it. |
| DEV-28 | **Phase-1 cache-coherence design (verified against code pre-implementation):** inlining the `/lg/config` JSON into the KV-cached shell is visitor-safe (`buildPublicConfig` is a pure `(resolved, design)` projection with zero per-visitor fields; `assignment_bucket` deliberately excluded and stays splice-only) BUT requires adding the **`ab_rev` axis** to `leadgenShellKey` + `leadgenShellEtag` (the one axis `lg-config:` has and `lg-shell:` lacks; without it an A/B start/stop/re-bump serves stale baked test dims until TTL — the exact failure mode the config key's own comment documents). Sentinel discipline unchanged (`MAPS_KEY_SENTINEL`, `ASSIGN_SENTINEL` keep splicing the pristine cached body). `renderSectionComponents` verified pure (content_json + design tokens; pinned `en-US` locale) → server-rendered sections keep the cached body variant-invariant under existing axes. Dev harness facts: `/lg/attempt` mints an explicit `unsigned.` token without `LEADGEN_CONFIG_SIGNING_KEY`, but the money path is hard-coded `requireSigned:true` → local G1 needs a dev-only signing key in gitignored `api/.dev.vars` (never a CF secret write; `requireSigned` never weakened). Mock provider: server-mode Offer endpoints have no host/IP validation (`isAbsoluteHttpUrl` only) and the auction fetch fires `endpoint_production` verbatim → G1 seeds a server-mode Offer PATCHed to a Playwright-launched `http://127.0.0.1:<port>/mock`; drive with a non-headless UA (dev bot-arm UA heuristics). |
| DEV-29 | **The anti-false-PASS suite caught a real R7 bug pre-merge:** `runtime/auction-client.ts slotTarget()` could never attach an IntersectionObserver on live-funnel markup (`banners_html` wraps cards in one container + stamps 1-based `data-slot` on anchors; slotTarget matched only `[data-lg-slot]`/`children[i]`) → impressions would have been silently ZERO on every live funnel — exactly the false-comfort class v2.4 §11.6 exists to kill, and no vitest covered observer attachment. Fixed (slotTarget also matches `[data-slot="{i}"]`), bundle rebuilt via the canonical script, proven by before/after probe (0 beacons → 3/3, duplicate-scroll 0). Companion cosmetic finding: ProgressBar presets↔render hook drift (`.lg-progress-track` vs `[data-lg-progress-bar]`) causes progress to become text on hydration (CLS 0.000412, 10× under the 0.1 good bar) — owned by the 09 §9.3 Phase-5 parity matrix. |
| DEV-30 | **Attempt-context carrier = the signed token payload:** original landing URL persisted as a `landing_url` data field inside the v2 token (04 §4.2 "persisted into the attempt context" — no new tables); ONE canonical reader (`readTrafficParams` via the context builder) parses it at consumption; auction rebuilds traffic strictly from the VERIFIED payload. `/lg/attempt` accepts `?u=<page href>` (engine always sends it) + same-origin Referer fallback. The §3.6 Record-shaped `answer_mapping_versions` is accepted but not equality-checked — the v2 `answer_mapping_hash` is its cryptographic gate (legacy array shape keeps the old equality check). |
| DEV-31 | **`answer_mapping_version` := `String(COALESCE(MAX(id),0))`** per section from `leadgen_section_answer_maps` (the table has no version column; strictly monotonic — bumps on every replace-set save). ONE definition shared by config-dto (public DTO) and `computeAttemptBindingExtras` (token hash) — coherence LIVE-TESTED (sha256 over config's ordered versions ≡ token hash). Accepted trade-offs: MAX(id) exposes a row-counter in the public DTO; mapping saves don't bump content_version/activation_version so the baked value can be stale ≤ TTL 300s (pre-existing staleness class; mint/verify symmetry ⇒ no tamper false-positive). |
| DEV-32 | **Opening-lander events have no producer site:** no PUBLIC lander surface exists (v2.3.7 built lander rendering admin-preview-only; public serve/resolver never read `lander_*`). `opening_lander_view`/`opening_lander_cta_click` stay producer-less until that surface ships — the Phase-5 producer-coverage test must encode this as documented-conditional (10 §10.2 "only when a funnel is configured with an opening lander"), never a fake producer. Recommendation on ship: client beacons from the engine (server-emit would count KV cache hits wrongly). |
| DEV-33 | **Eligibility/test-status conventions:** `last_test_status` scoped to test-tool rows (`auction_instance_id IS NULL`) everywhere — a live-auction timeout can never flip an Offer to failed and wedge it out of future auctions. `LEADGEN_ACCEPT_V1_TOKENS` deploy note (operator): set ONLY during the deploy grace window, else in-flight pre-deploy sessions 422 at auction by design (05 §5.3). `serve.ts serveLeadgenAttempt` is orphaned (v2 handler in runtime-routes replaced its registration; still compiles) — cleanup candidate in Phase 5. |
| DEV-34 | **Beacon column conventions (no dedicated columns exist):** `validation_error` carries its rule code in `answer_value_normalized`; impression `slot_index` rides `carrier_position`; envelope uses `timestamp` (matches ingest; contract's `ts` was informal). Client impression dedupe key = (page_view_id, banner_render_id, slot_index, event_type). Adversarial-review checkpoints. |
| DEV-35 | **Adversarial-review cycle (Fix-P1):** verdict FIX-FIRST 0/5/16; all MAJORs + 13 minors fixed in-phase (`d731687`) with regression tests; three deliberate deferrals (m5 wording, m13 P5 cleanup, m16 scope note). Contract change from m2: `/lg/attempt` now MINTS a ko_sid when the cookie is absent, binds it into the v2 tuple, ECHOES `session_id` in the response (+best-effort Set-Cookie), and the engine adopts the echo for `/lg/auction` + beacons — cookie-blocked visitors complete funnels instead of 422ing at auction. Documented residual (M1 edge, beyond-finding): if the SAME `carrier_key` returns from BOTH placements of one Offer in one auction, `carrierMeta` identity `(offer, carrier_key)` is last-write — disambiguation would change carrier identity through auction-core + persisted `carriers_shown_json` semantics; revisit only if a real provider exhibits it. **Bundle headroom now 1,803 B (39,157/40,960)** — Phase 4 adds container behaviors to `runtime/render.ts` + a shared rebuild; budget pressure is REAL there (plan mitigation: property-name minification pass or es2020 target evaluation, never a gate raise). |
| DEV-36 | **Fix-P2 errata + judgment calls (all omitted-not-faked per `06` §6.10's own omit-principle; operator-flagged like DEV-24):** (a) `06` §6.2 "Postal code" member + the §6.2† Session members (`page_view_id`/`funnel_attempt_id`/`auction_instance_id`) are unimplementable without extending the FROZEN 32-macro projection / 12-key computed registry (`04` §§4.3–4.4 fix them; contract-internal conflict) — omitted from the picker; registry extension = future contract rev. (b) §6.10's parenthetical "AND rows supported (evaluator supports conjunction)" is factually wrong — `conditional` storage is a single object and the SHARED evaluator (payload + section dependencies + runtime parity) has no conjunction; extending it is a cross-cutting data-model change (§1.4) — omitted with in-UI help ("need AND/OR? create a second field"), operator extension candidate. (c) §6.6 "Unix timestamp" format is inexpressible via `formatDate`'s token set; §6.8 "Computed"/"Split string" array sources have no runtime support — all three proven by tests, omitted from pickers. (d) §6.3 Analytics-label/Notes columns render as read-only projections (no `value_map` storage slots). (e) §6.5 preset semantics pinned: letters = anchored `[A-Za-z ]+` post-sanitize, digits = anchored `[0-9]+`, custom = `RegExp.test` as authored. **[CORRECTED post-review — DEV-37]** the original "200-char cap + quantifier-bomb heuristic is safe" claim was FALSE: the cap is on the pattern, not the unbounded visitor input, and the heuristic missed the alternation-bomb class → ReDoS on the money path. Real fix = an unconditional runtime input-bound before any `.test()` (violation→fallback), + a widened save-time heuristic as defense-in-depth. See DEV-37. (f) Warning-only schemas are saveable AND auction-eligible (`validatePayloadSchema().ok` = "no blocking error"; §5.5-consistent; 25 blocking / 2 warning codes exported + compile-time exhaustiveness). (g) F-1: the G5 suite found the eq/neq snap-back live and it was fixed in-phase — the §6.10 example is now the regression. (h) Test-tab "Regenerate" = clear-draft + regenerate; last-Test chip = `last_test_at` (test-tool-scoped MAX(created_at)). |
| DEV-37 | **Fix-P2 adversarial remediation (verdict FIX-FIRST 1 MAJOR + 6 MINOR → all actioned in-phase).** MAJOR-1 money-path ReDoS: `applyFreeTextConstraints` (`payload.ts`) now bounds the regex INPUT unconditionally before any preset/custom `.test()` — effective ceiling = `free_text_max_length ?? FREE_TEXT_RUNTIME_INPUT_HARD_CAP` (4096); over-ceiling → invalid → the caller's fallback, so a custom pattern with NO explicit max can never `.test()` a huge input. Defense-in-depth: the save-time `isCatastrophicRegexShape` now also rejects the exponential alternation class (`(a|a)+`, `(a|ab)*`) the nested-quantifier screen missed. Regressions in `leadgen-payload-formats.test.ts` (5000-char input × `(a|a)+$` returns <250ms via fallback; max-length-UNSET path) + `leadgen-payload.test.ts` (alternation bombs save-rejected; linear patterns still accepted). MINORS: M2 boolean eq/neq writes the shown value into the model immediately (no silent is-empty); M3 non-boolean condition values stay strings (no runtime `===` drop); M4 draft `TEST_DRAFT_MAX_BYTES=65536` 400 + `TEST_DRAFT_TTL_SECONDS=259200`; M6 CSV parser quotes only at cell start; nano-7 value-map skips reserved keys with a notice. MINOR-5 (computed-ref shape discriminator) left as the documented byte-compat trade-off. **Process note:** two remediation subagents returned 0-tool-call flakes (environment dispatch issue) — the conductor executed the fixes by own hand; tsc caught a backtick-inside-island-template-literal typo before commit (verify-by-own-hand, not trust-the-report). |
| DEV-38 | **MAJOR-1 re-remediation (focused re-review reopened it).** The DEV-37 fix's premise — "the runtime input cap closes ReDoS" — was FALSE: an input cap bounds POLYNOMIAL cost only; EXPONENTIAL backtracking explodes at ~30 chars (far under 4096), and the two flat shape-regexes matched only single-level groups, so `((a)+)+$`/`((a+))+$`/`(([a-z])+)+$` were save-ACCEPTED and hung the auction Worker on a ~40-char visitor answer (admin-authored pattern + visitor-supplied trigger = config→visitor CPU-DoS on the money path). **Real fix:** `isCatastrophicRegexShape` is now a paren-depth-aware walker — per-group frames track hasQuantifier/hasAlternation, flags propagate UP on group close, and any unbounded quantifier (`+`/`*`/`{n,}`) governing a group whose body contains (at any depth) a quantifier or alternation is rejected. SOUND (never admits an exponential shape). Layered: (L1) runtime input hard-cap 4096 = polynomial bound; (L2) the same screen at BOTH save-time AND runtime (`applyFreeTextConstraints` refuses a stored bomb before `.test()`, so legacy/pre-fix/DB-edited data is also safe). Conservative over-rejection of safe quantified-alternations (`(cat|dog)+`) is the documented safe-side trade-off (valid_values or Advanced drawer; usability follow-up). This is exactly why the delegated loop runs a focused re-review of every remediation — the first fix was plausible and tested-green but wrong. |
| DEV-39 | **Fix-P3 execution + cross-slice seam catch.** Subagent dispatch degraded mid-Phase-3 (a content-filter block truncated the server slice to only `rules.ts`; a connection-drop lost the UI slice's report) — the conductor wrote the server slice (guarded hard delete + `buildOfferUsageReport` 12-kind inventory + duplicate Q3 copy-set + D3 `regionValueError` + S1 `offers_payload_explain`) BY OWN HAND, and hand-verified the UI slice (tsc + suites + diff). **Seam bug caught:** the UI's S1 simulate panel read `body.offers_considered` (only `{offer_id, placement_id}`) instead of the additive `offers_payload_explain` array carrying the redacted preview/parser/expected-fields — so §7.6 rendered EMPTY at runtime while the UI test (a field-name-string check) stayed green. Fixed the UI read + added a REAL endpoint→panel seam test (`leadgen-auction-simulate.test.ts` asserts the endpoint returns `offers_payload_explain`; `leadgen-auctions-ui.test.ts` asserts the island reads that key). Two contract-mapping notes: offer status enum is `active/paused/archived` (no `draft` — duplicate uses `paused`, and the untested new-v1 schema keeps it out of live auctions via the R4 gate); the §7.1 row-action rewrite moved actions into a kebab menu, so the existing `leadgen-offers.spec.ts ⑥` archive test was updated to open the kebab first. D3 region validators are shape-checks (ZIP 5-digit; country/state 2-letter alpha-2) behind the UI's closed dropdowns; a full ISO membership table is a follow-up. `rules.ts REGION_RULE_ACTION_LABELS` etc. are currently unused (the UI inlined an identical frozen-label copy) — harmless duplication, dedupe candidate. |
| DEV-40 | **Fix-P3 adversarial remediation (verdict BLOCK 2 BLOCKER + 3 MAJOR → all actioned in-phase).** **BLOCKER-1/2 (A1 delete-guard incompleteness — data-integrity crux):** `buildOfferUsageReport` did not count two reference classes, so an offer still pointed at by live config could be hard-deleted and orphan it — (1) `funnel_rules_targeting`: a `leadgen_funnel_rules.target_offer_id` reachable via `leadgen_funnel_variants`→`leadgen_funnels` (`LEFT JOIN … WHERE r.target_offer_id = ?`), (2) `auction_backfill_source`: the offer named as an auction's backfill source. Both now counted as BLOCKING kinds; regression "a funnel_rule target AND an auction backfill source block `?mode=hard` with the two new kinds" fails-before/passes-after. **MAJOR-3 (S1 preview answers-only):** `offers_payload_explain` built its preview from `sample_answers` alone, so `source:"macro"`/`"computed"`/`"placement"` fields were empty/wrong vs. what the engine sends. Fixed by building one `buildLeadgenRuntimeContext` from the request + the operator's simulated `context` overrides + resolved quote/funnel/variant, then threading `macros`/`computed` and the offer's PROVIDER-FACING external placement id (`leadgen_offer_placements.placement_id`, NOT the `lgpl_` public id — 04 §4.5, matching the Test-tool peer) into `buildPayload`; regression "the preview is the EXACT payload — macro + placement fields resolve" seeds a `source:"macro"`(utm_source)+`source:"placement"` schema and asserts both resolve. **MAJOR-4 (A3 inventory gaps):** added `quotes_indirect` (offers reachable through a quote's funnel; join via `fvs.variant_id`), `region_rules`, `revenue_attribution`, and provider-log latest-ts to `buildOfferUsageReport`. **A2 atomicity (minor):** `duplicateOfferHandler` now compensating-deletes the new offer row if placement creation fails (no half-created offer). **MAJOR-5 (documentation, not code):** "stays dry" in §7.6/traceability was imprecise — simulate performs NO writes and NO production call but DOES resolve carrier metadata via the provider **staging** endpoint (pre-existing P10 auction-core behavior); wording corrected to "no writes; staging-only carrier resolve" in the S1 row (no code change — the behavior is correct and pre-dates this mission). Post-remediation gates by own hand: tsc 0 · vitest 3605/3605 (307) · verify:all green · Playwright 104/104. |
| DEV-41 | **Fix-P3 focused re-review remediation (verdict FIX-FIRST 1 residual MAJOR + 1 MINOR → both fixed in-phase).** The re-review CONFIRMED the two A1 BLOCKERs and A3/A2, but PROVED my S1 MAJOR-3 fix incomplete: I built one `simCtx` outside the per-offer loop with NO offer slice, so `contextToMacros`'s three offer-derived macros (`offer_id`/`offer_name`/`placement`) resolved empty/wrong for `source:"macro"` nodes — the exact "misleading preview" class MAJOR-3 claimed to close. My regression had only covered `macro:"utm_source"` (request-scoped) + `source:"placement"` (which uses the explicit `offer.placement_id`, a different code path), so it passed green while the offer-scoped-macro path was broken — a textbook false-green from an under-scoped test. **Fix:** the runtime context is rebuilt PER OFFER inside the loop (`{...baseCtxOpts, offer:{offer_id, offer_name, placement_id: externalPlacement}}`), mirroring the Test-tool peer (`payload-builder-handlers.ts`); `offerCtx.macros`/`.computed` feed `buildPayload`. **MINOR (§7.6 "masked"):** `source:"token"` nodes were absent (a token node returns `undefined` without a `value`, falling to default) rather than present-but-masked. Fix: inject `REDACTED_VALUE` (`"[REDACTED]"`, the same sentinel `redactPii`/`maskPaths` use) as the token value with the offer's real `api_token_placement`/`request_execution_mode`, so a token node appears iff the engine would inject it (payload placement + server mode) and renders `[REDACTED]` — the REAL secret is NEVER read in an admin dry-run (strictly safer than the peer, which resolves then masks). The regression now seeds all six source classes (request macro, offer_id/offer_name/placement macros, source:placement, source:token) and asserts each resolved value. **Process lesson (recorded):** a regression that exercises only one instance of a claimed class is false comfort — the S1 test now covers every source class the claim spans. Gates by own hand: tsc 0 · vitest 3605/3605 (307) · verify:all green · Playwright 104/104. |
| DEV-42 | **Fix-P3 second re-review remediation (verdict FIX-FIRST 1 MINOR, introduced by DEV-41's token fix → fixed in-phase).** The second re-review re-CONFIRMED MAJOR-3 closed and the masking secret-safe, but proved the DEV-41 token injection was OVER-eager: it rendered the `[REDACTED]` sentinel whenever `api_token_placement==="payload"` && `request_execution_mode==="server"`, WITHOUT checking that a secret actually resolves — but the live engine (`fetch.ts`) and the canonical Test-tool peer (`payload-builder-handlers.ts`) both inject the token leg ONLY when `readEnvSecret(secret_ref)` resolves. Reachable state: an offer with payload+server placement but a NULL `api_token_secret_ref` (independently nullable — no coupling validation) or a secret_ref that isn't deployed to the Worker env (env-drift, flagged by `deploy-safety.md`). Such an offer's preview showed a spurious `tok: "[REDACTED]"` the live payload omits — worse, it HID a missing-secret misconfiguration in the very tool meant to preview what gets sent, and it REGRESSED the pre-DEV-41 behavior for that sub-case (token was correctly absent before). **Fix:** SELECT `o.api_token_secret_ref` and gate injection — `const tokenResolvable = secretRef !== null && readEnvSecret(c.env, secretRef) !== undefined` — injecting `value: REDACTED_VALUE` only when resolvable. `readEnvSecret`'s return is consumed ONLY as a boolean; the real secret value is NEVER placed in the payload (asserted: the preview JSON does not contain the seeded secret string). New negative regression "S1 token gate: a payload+server offer whose secret does NOT resolve shows NO token field" proves engine-parity. Also closed a test-coverage gap the reviewer flagged: the S1 test now seeds a `source:"computed"` node (`request_timestamp`) and asserts it resolves (the row claimed computed but the prior test didn't exercise it). Gates by own hand: tsc 0 · vitest 3606/3606 (307) · verify:all green · Playwright 104/104. |
| DEV-43 | **§6 preamble erratum + F-2 closure (Fix-P2 retro 1:1 audit, 2026-07-08).** (a) The `06` preamble's "(no route changes)" is contradicted by two ADDITIVE routes Phase 2 shipped: `POST`/`PUT /offers/:id/payload/sample-answers` (`payload-builder-handlers.ts`) — a NECESSARY deviation: §6.12.1 mandates generated-sample-answer draft persistence and no existing route could carry it. Additive-only; no existing route changed shape or semantics. (b) §6.4's "drag between groups" originally landed buttons-only (Mark-as-main / Move-to-Other) — superseded in the audit-remediation slice by HTML5 drag between the Main/Other groups on the value-map rows (`dragstart`/`dragover`/`drop` funneling into `vmDropOnRow` → `vmRegroupRow`, the SAME state mutation the buttons use); buttons kept as the keyboard-accessible path. |
| DEV-44 | **Retroactive-audit remediation detail (branch `leadgen/fix-p2p3-audit-remediation`; audit story in the section header above).** P3 UI (wave 1, EXECUTED seam regressions throughout — island code run against REAL server JSON): F1 usage island now consumes the server's ordered `kinds[]` array (`{kind,count,items[],warning_only}` per entry) and the client-side kind list is DELETED (the phantom-`payload_schemas`/missing-new-kinds/wrong-blocking-flag drift class is structurally dead); F2 duplicate reads `res.body.offer.public_id` + `copied.extra_placements_blanked` and the executed regression drives the REAL 201 → navigation URL → server-rendered banner loop; F5 defensive test-status chip; F9 note wording; F10 country/state paste validated against the same closed sets as the dropdowns; F11 region labels single-sourced from `leadgen/rules.ts` (inlined copy deleted; false comment fixed); F13-UI `__needs_value__` rows render an EMPTY input + "needs value" hint AND round-trip through `collectPlacements` so an unrelated Save neither blocks nor silently deletes the pending placement (replace-set hazard found during the fix). P3 server (wave 1): F3 `expected_response_fields = Object.keys(parsed.fields)` behind a shape guard (exact-name regression; fail-before probe reproduced `["carriers_path","fields"]`); F6 structured hard-delete log `{event:"leadgen_offer_hard_delete",…,cascaded:{per-table counts}}` + COMPANION FIX: `active_payload_schema_id` (an enforced FK) is cleared before the delete batch — pre-fix, hard-deleting any offer that ever saved a schema threw an FK violation (latent bug exposed by the mandated F12 fixtures); F7 duplicate is ONE `db.batch` (parent + children bound via `(SELECT id … WHERE public_id = ?)`; compensating delete removed); F8 `analytics_mirror_rows` sums ALL four offer-keyed 0037 mirrors and `revenue_attribution` sums revenue_raw+postback_log+conversion_log (`leadgen_revenue_unmatched` has no offer key — skipped, commented), every warning `count` is the TRUE row count; F13-server `cap_fallback_offer_id` copies under the cap flag + the new default placement id 400s when it equals ANY source placement id; F5-server list `test_status` via the canonical `LEADGEN_TEST_STATUS_SUBSELECT` (one query, no N+1; a NULL-status transport-error run maps to `untested` per the canonical derivation — cross-surface consistency chosen over the audit's "else failed" phrasing); F12 contract-named API tests added (cascade BEFORE/AFTER row counts ×5 child tables, copy-flag matrix, sentinel rows asserted in DB, DB-proven non-copy, all-13-kinds populated fixture). P2 (wave 2): F-1 typed condition inputs — linked-fields projection now carries `choices` (sample-answers pin updated 5→6 keys), enum condition values render a choices `<select>` storing the TYPED value, `in`/`not_in` render a chips editor storing `values[]` (byte-compatible with `conditionalMet`); F-5 the island's free-text screen is an ES5 port of the depth-aware `isCatastrophicRegexShape` walker (`((a)+)+$`/`(a|a)+$` now flag client-side; fail-before proven against the old flat regex); F-4 executed `renamePrefix` populated-subtree regression + value-map rows⇄storage round-trip via the extracted `vmApplyRowsToNode`; F-2 HTML5 drag (DEV-43b). F4 (§7.7): real Playwright browser flows added for duplicate-navigate-banner, clean delete, blocked delete w/ usage links + Archive-instead, usage dialog, region-rule UX incl. paste rejection, and the simulate trace panel. |

| DEV-45 | **Fix-P4 Slice D2 (§8.2 E1/E9 · §8.7 E2 · §8.9+09 §9.1 events panel · §8.12 flows) — judgment calls + browser-exposure catches.** (a) **Nested-button structural bug (D1-latent, browser-exposed):** the §8.3 library items were `<button>` wrappers around preset-rendered thumbnails that legitimately contain `<button>`/`<input>` markup — INVALID HTML; the parser closed the outer button early and shattered the studio out of the admin shell (drawer landed full-width at document level, its tabs under the fixed sidebar — every drawer interaction dead in a real browser while all vitest string-checks stayed green). Fixed: `div[role="button"][tabindex="0"]` + island Enter/Space activation + a served-markup regression (`<button …data-add-component` forbidden). The §8.12 suite's FIRST run caught it — exactly why the contract mandates browser flows. (b) **Empty-subheadline save 400 (D1-latent):** `collectSection` sent `subheadline_text:""`; the validator requires non-empty-or-null → every studio save with a blank subheadline failed in-browser. Fixed ('' → null) + fails-before vitest regression + the ③ flow covers it live. (c) **§9.1 events-document deviation (documented):** the preview iframe cannot `script-src /lg/runtime/{v}.js` on the ADMIN host — ALL `/lg/*` paths ride `publicSiteContextMiddleware` (site-by-hostname) and 404 there. The events document therefore INLINES `LEADGEN_RUNTIME_JS` — the BYTE-IDENTICAL export the versioned route serves (asserted by byte-containment in the endpoint test) — with the version as a data attribute; `data-lg-preview="1"` preview mode is engine-native (beacons→postMessage, auction disabled), runtime/* untouched, `verify:leadgen-runtime` byte-identical (39,157 B). Public routing NOT reordered for an admin-preview need. (d) **selected_offers persistence gap (D1 gap):** the studio save body omitted the explicit selection set, so a selected-but-unmapped Offer was silently DROPPED on the next save (`parseAnswerMaps` derives selection from edges when absent). The blob + save body now carry `selected_offers`; regression proves survival through PATCH. (e) §8.7 "value-map modal" = a DEEP LINK into the offer's §6.3 payload-builder table (`…/edit#payload`, new tab) — an in-panel scoped modal is a usability follow-up; "per-row test" = the per-offer validate-payload preview (notes unsaved edits are not reflected). (f) `ui-question-builder.ts` DELETED (last consumer was a test); its §12.11 cell-state/copy assertions ported onto the studio island's decode (`edgeMapState`/`mapStateNote`), which a SEAM matrix pins cell-for-cell to the REAL `rebuildDerivedIndexes`. (g) R5 integration: `#mapping` hash (the preflight fix-link target quotes-handlers already emits) opens the mapping drawer tab — flow ⑤ drives quote→blocked-preflight→fix-link→drawer end-to-end. Gates by own hand: tsc 0 · targeted suites green · verify:leadgen-runtime byte-identical (39,157 B) · FULL vitest 3787/3787 (308 files) · `test-ui/leadgen-section-studio.spec.ts` 6/6 (screenshots in `test-artifacts/leadgen-section-studio/`); full Playwright suite = phase-exit (operator-run per the slice contract). |
| DEV-46 | **Fix-P4 slice F acceptance suite as a defect-finder + F2 remediation.** Authoring all four §8.11 patterns through the REAL UI surfaced 4 product defects the vitest layers could not see (kept as red pins, then fixed — pins flipped green, never weakened): (1) **BLOCKER-class §8.13 hazard**: slice B enforced internal_field uniqueness over ALL nodes, but a `ValidationError` affordance legitimately carries `internal_field` as its error-slot binding (`data-lg-error-for`) — the Phase-1 seed 400'd and any live Section using the idiom could not re-save; fix scopes the duplicate check to answer-PRODUCING types (`catalog.produces !== null`). (2) Non-default preview sims were DEFEATED by §9.1 hydration — the engine boots with an empty answer store and re-hides server-revealed dependency components (engine preview mode skips snapshot restore by design; srcdoc has no storage origin) — fix: non-default sims render as STATIC script-free documents; the §8.9 events stream survives via a hidden events-probe frame; default keeps full hydration. (3) choiceDisplay-only edits silently lost (controls had no listeners — the exact silent-data-loss class); wired to the collect path. (4) Events-panel min-content stretched the studio page to ~3034px (min-width propagation); CSS containment. Plus 3 §8.5/§8.6 authoring gaps closed (TrustBar items/layout, LogoStrip logos, StepIndicator steps/current inspector controls — pattern 4 now genuinely authors a 4-step indicator). Process note: browser-level acceptance authored AS the operator would work is the defect-finder unit tests cannot replace — same lesson as the P3 audit, now applied proactively. |
| DEV-47 | **Fix-P4 adversarial review remediation (verdict FIX-FIRST 1 MAJOR + 2 MINOR → all fixed; re-review SHIP).** MAJOR-1 was a PRE-EXISTING (base `eb8b819`, not a P4 regression) stored XSS on the LIVE `/lg` funnel renderer: the `arbitrary_css_override` deny-list `CSS_ESCAPE_RE` — duplicated byte-identically in `content-schema.ts` + `sections.ts` — omitted `"`/`'`/backtick, so a curated `design_overrides` value (e.g. `buttonText: 'red" autofocus onfocus="alert`1`'`) passed both validators and broke out of the preset's `style="…"` into real `autofocus`/`onfocus` attributes that execute in VISITOR browsers (a paren-free tagged-template dodges the `()` block). Reachable by an authenticated admin via the Studio Advanced raw-JSON node editor or a direct `PATCH /sections/:id`. Fixed at both layers per fix-found-issues-now (not deferred to a hardening branch): widened BOTH regexes to include the quotes+backtick, AND `style()` now `escapeHtml`s every interpolated value so a hostile value can never terminate the attribute even if it bypassed validation. Only legit render change: font-family design tokens' single quotes render as `&#39;` (browser-equivalent computed style; no byte-identical/computed-style test regresses). MINOR-2: `answers.ts fieldsOf` returned an answer FieldSpec for `produces===null` nodes, so a `ValidationError` sharing a boolean question's `internal_field` re-coerced the answer to a string (breaking dependents + payload typing) — now skips non-producing nodes (matches the slice-B validator model; object-producers NameFieldsGroup/AddressAutocomplete unaffected). MINOR-3: the preview `postMessage` listener gated only on `data.type` — now also requires `event.source` to be the preview/events-probe iframe's `contentWindow`. Consequent cache-correctness bump: `LEADGEN_TEMPLATE_VERSION` 1→2 (preset markup + the `style()` escaping changed this phase) rolls all cached funnel shells/configs forward (the `/lg/runtime/{v}.js` URL moves to `/2.js`, old `/1.js` now 404s — asserted) + `LG_ENGINE_VERSION` bumped to match (bundle byte-identical rebuild). Convergence re-review independently attacked all four (11 breakout payloads rejected at both validators + entity-escaped past validation, no legit-token false-positive across all 8 curated keys, object-producer expansion intact, full version-axis grep + shell/config key-ETag asymmetry reasoned, inlined events bundle byte-identical) → SHIP. Gates by own hand: tsc 0 · vitest 3812/3812 (308) · verify:all green · Playwright 134/134. |
| DEV-48 | **Fix-P5 (T1/T2) — parity + analytics proof + QA honesty + the 52-row v2.3.7 re-audit (2026-07-09).** (a) **T1 preview↔runtime parity** (`test/leadgen-parity.test.ts`): §9.5 shared-renderer invariant proves the studio preview and the live funnel render through ONE authority (`presets.ts`) — the preview module authors no component markup — then asserts, per catalog + container type, that the preview subtree ≡ the live `renderSectionComponents` subtree (+ nested-container recursion + identical design tokens); §9.3 gives a dependency-operator parity table server≡client over all 9 ops. (b) **§11.6 anti-false-PASS 5/5**: Fix-P1 shipped 4 legs; the 5th (the visual-suite `setContent` static tripwire) was deferred to Fix-P5 per contract `12` and is now present — `test-ui/leadgen-visual.spec.ts` was re-pointed from the old static-injection harness to a real `page.goto('/lg/…')` + `data-lg-ready` wait, and `leadgen-live-funnel.spec.ts` asserts the visual suite source contains no `.setContent(`. (c) **T2 producer coverage** (`test/leadgen-analytics-producers.test.ts` §10.2): the producer map is lockstep over the frozen 31 event types across four proof routes (15 `server_emitted` / 2 `server_impression_row` / 12 `client_beacon` / **2 `deferred_no_surface`**) with no gap/overlap; every producer's emission-site `source_marker` is checked to still exist in source; the **DEV-32 opening-lander deferral is encoded as documented-conditional** (the deferred set is asserted to be EXACTLY `opening_lander_view`/`opening_lander_cta_click`, accepted at `/lg/track` so no dead-letter, per `10` §10.2 "only when a funnel is configured with an opening lander" — NEVER a fake producer). (d) **T2 attribution chain** (`test/leadgen-attribution-chain.test.ts` §10.3): the IDs persist across every server-owned leg + a coverage-honesty test naming the client-beacon legs. (e) **T2 re-audit** (`docs/leadgen/traceability.md`): all 52 v2.3.7 rows re-audited against LIVE behavior with the `11` §11.5 standard — the CP5 "ZERO code gaps" sign-off was FALSIFIED (2026-07-08 investigation: empty runtime plane), CP3 "rendered end-to-end" was REJECTED as-signed (empty shell), the P11 impression client-fire deferral (DEV-23 → "a later phase" that never came in v2.3.7) is documented and traced to its actual close in Fix-P1 R7 (#88) + DEV-29; each formerly-overclaimed runtime row is re-cited to its Phase-1..5 runtime test; operator-owned LIVE legs restated BLOCKED. **Honesty scope:** this was the docs-only T2 slice — no product/test code changed; no PASS was written without a named executing test; vitest citations were re-executed green 2026-07-09 (parity/analytics/attribution 115, runtime/gates 142, computed 14), Playwright citations collectable (`--list` 43, exit 0). The full-suite phase-exit gate + fresh-context adversarial review + PR remain the delegated loop's steps (not claimed done). |

## Manual QA hand-off (operator-owned — `11` §11.7; blank sign-off = FAIL state for Fix-P5)

| Scenario | Prepared (docs/leadgen/manualQA.md §) | Operator run | Result |
|---|---|---|---|
| MQA-R1 live `/lg` full completion (desktop+mobile) | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R2 banners render; impressions in analytics | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R3 banner click → resolved provider URL | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R4 payload carries real geo/ip/traffic/computed/placement | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R5 ineligible Offer blocked from auction | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R6 invalid Quote activation blocked w/ report | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R7 zero-JSON payload authoring + Test | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R8 Studio pattern §8.11-(4) start-to-finish | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R9 Maps field-level config + missing-key fallback | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |
| MQA-R10 desktop/mobile preview round-trip | authored (`docs/leadgen/manualQA.md`, §11.7) | BLOCKED(operator, `manual_qa_visual`) | |

## Operator-owned residuals (carried from v2.3.7 — unchanged by this mission; BLOCKED, never PASS)

Production deploy + post-deploy behavioral verification (per-phase or at mission end — operator's call) · manual-QA execution + sign-off (above) · CH secrets (`CH_URL`/`CH_USER`/`CH_PASSWORD`) + `infra/leadgen/{clickhouse,athena}-ddl.sql` apply + Athena→CH ingest · `GOOGLE_MAPS_BROWSER_KEY`/`_SERVER_KEY` · `LEADGEN_CONFIG_SIGNING_KEY` (mandatory before live auction — fails closed) · per-provider `LEADGEN_PB_TOKEN_*`/`LEADGEN_S2S_TOKEN_*` · live-tenant Quote activation · DEV-24 contract erratum (`lg_events_raw.continued_to_next_section`) in the v2.3.7 contract SSOT.
