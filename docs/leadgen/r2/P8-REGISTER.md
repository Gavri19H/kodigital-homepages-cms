# LeadGen P8 — Traceability Register (single recovery truth)

Seeded at P8-1 from `P8-DEFECT-CONTRACT.md` (v3) at item granularity. Anchors quote the owner
verbatim (`docs/leadgen/source-of-truth/SOURCE-OF-TRUTH.md`) or cite an owner image. Only the
conductor writes rows, and only after its own gate run + adversarial SHIP. Status vocabulary:
`PASS(evidence)` / `DEVIATES(finding)` / `BLOCKED(owner)` / `INCONCLUSIVE(step: …)`. Every row
starts as `DEVIATES(<the contract's measured finding>)` — the defect is the current truth —
and flips only on driven proof. Validator: `check_register.py` exit 0 at every phase merge and
at CLOSE. Evidence root: `docs/leadgen/r2/evidence/p8/`.

## Blockers

| ID | Anchor (owner verbatim) | Requirement | Phase | Status | Evidence |
|---|---|---|---|---|---|
| B1 | "if I want to auto fill only for street address and city and I want the user will insert the Zip by himself" | Address autocomplete alive on the default multi-field rendering: Autocomplete constructor ≥1, typing → suggestions, choice fills mapped fields; no Maps JS when no job enabled (contract §5 B1, R1-1) | P8-1 | PASS(certified by review #3's own drive: the wire converged to ONE flat shape and the nested emitter is deleted; the intercepted **Autocomplete constructor count went 0 → 1** with real args {id:"lg-addr-p8_addr_street", fields:[address_components, formatted_address]}, corroborated by Google's own .pac-container = 1; an all-manual address authored through the real PATCH route serves 0 data-lg-maps attrs, 0 SDK script tags and 0 SDK requests — the wasted Maps load is gone) — and the fill leg now works: a resolved place writes the VISIBLE inputs through the same path a keystroke takes, so the store can only hold what the visitor can see and correct) | b1/after.md, b1/probe-b1-after.txt, b1/multi-field-address-autocomplete-job-on--1280.png, b1/multi-field-address-autocomplete-job-on--375.png |
| B1-GOOGLE | "if I want to auto fill only for street address and city" | Google's own half: typing produces real predictions (needs a URL-whitelisted key) | P8-1 | INCONCLUSIVE(step: at cutover, on the live site with the URL-whitelisted browser key, type a street address and confirm predictions appear. Local drive recorded Google's OWN answer rather than assuming it: the JS bundle loads 9/9 HTTP 200 and the widget is constructed, but the first prediction request returns `gm_authFailure: true` — the owner key is IP-whitelisted to Cloudflare, not URL-whitelisted for 127.0.0.1. CORRECTION: the conductor originally folded the FILL leg into this row and blamed the key; the review proved the fill failure is OURS — carried as B1-FILL) | b1/after.md, b1/probe-b1-after.txt |
| B1-FILL | "if I want to auto fill only for street address and city and I want the user will insert the Zip by himself" | Choosing a suggestion must fill the mapped VISIBLE inputs — and must never stamp an answer the visitor cannot see or correct | P8-1 | PASS(certified by review #3 on the legs it drove — visible fill correct at 1280 and 375, the owner's manual ZIP empty on screen AND absent from the payload, street line only on the composite; the store-equality leg rests on review #2's capture plus a zero runtime diff since. Root fix: autofill enters via the real keystroke path (visible input + bubbling change), 'no box, no answer' — an unmapped/absent field is populated NOWHERE, and the composite street box gets the STREET line, not the whole formatted address. Driven at 1280+375: street/city/state/zip visible and store==screen; the owner's manual ZIP stays empty AND absent from the payload. Original finding:  maps.ts hooks.setAnswer -> engine writeAnswer records into the store, nothing writes the DOM input; the visible City box (326x52) keeps its placeholder at 1280 and 375 while the store holds 'Mountain View'. Money path: on the D3 default the store carries Google's ZIP 94043 stamped answer_source:user_selected — an answer the visitor never gave, cannot see, cannot correct, and which the buyer receives) | review-p8-1/b1-owner-street-city-zipmanual-afterplace-1280.png, review-p8-1/b1-owner-street-city-zipmanual-afterplace-375.png, review-p8-1/b1-fill-after-place-1280.png |
| B2 | "how the user defines a theme per funnel?" | A theme-record PATCH reaches the live page on a fresh ?_cb without any activation save; then the ThemeRecord keys re-verified (contract §5 B2, R2-2) | P8-1 | PASS(driven both legs: PATCH thm_p8-repro brand_primary #112233→#AB1234 (200) then ONE fresh ?_cb fetch of /lg/r2fix — old colour 20→0, new colour 0→20, content_version 109→110, with NO activation save, NO funnel-theme PUT and no restart (before: 3 unique ?_cb fetches all stale, version frozen). Leg 2 CORRECTED after review M-1 — the sweep's first table could not express MIS-TARGETED or DEAD and so certified a mis-target as alive; re-predicated and re-run: **ALIVE 17 / DEAD 2 / MIS-TARGETED 1 / UNMEASURABLE 5**. MIS-TARGETED: `roles.card` moves input.lg-input + .lg-btn-answer while its label's element .lg-question-card stays constant (contract R3's own example). DEAD (zero consumers by grep): `roles.success`, `spacing`. A proposal to also call `roles.error` DEAD was REJECTED on evidence — styles.ts:279 `.lg-tscard[data-error]` is a real consumer. The 3 non-ALIVE classes are carried into P8-3's honour-or-remove sweep, not closed here) | b2/after.md, b2/themerecord-sweep-conductor.txt, b2/live-theme-before-patch-1280.png, b2/live-theme-after-patch-1280.png, b2/live-theme-after-patch-375.png, b2/repro-before.md |
| B3 | "D. Theme picker per funnel name." | A theme edit from funnel C's chip changes funnel C only — storage + live pages for A/B/D unchanged; same for the Template chip (contract §5 B3, R6-1) | P8-1 | PASS(certified by review #5 driving the owner's verbatim acceptance end-to-end: a colour set from CHARLIE's chip produced ONE merged PUT to Charlie; storage — only C changed; RENDERED PAGES — C x8, A x0, B x0, D x0; the Template chip targets Delta on both panel headers and the hash; identity survives a plain tab round-trip AND a full reload; 0 console errors at 1280 and 375. Getting here took five review rounds because this row carries the wrong-funnel CLASS, which produced five instances: the chip read path, the tab round-trip, the colour rail display, the ungated arm-override read, and the override switch's own affordance. DISCLOSED RESIDUAL, named not swallowed (review #5 MINOR-1, carried as ADJ-P8-10): when an ARM is bound to a theme RECORD, the rail paints the funnel's inline palette while that arm renders the record's — display-only, the write is correct) | review-p8-1e/b3-colour-from-charlie-chip-1280.png, review-p8-1e/b3-colour-from-charlie-chip-375.png, review-p8-1e/b3-chip-roundtrip-reload-charlie-1280.png |
| B4 | "Add a real section to the canvas to the user could actually see the design he creates." | Choosing a site on Themes re-renders that canvas with the site's logo, branding and footer, matching Templates for the same site and moment (contract §5 B4) | P8-2 | PASS(certified by review #3 driving it end to end: Templates<->Themes parity on funnels A/C/E at 1280 AND 375 — CSS byte counts identical (71,934 / 68,501 / 68,496) and footer links identical on both canvases; the owner's Image18 dead-logo case renders the same honest chip on both instead of Image18's bare alt-text link; the site picker is reachable at 375 (x105 w240, in viewport) and CHANGING it re-renders the canvas (body 3755->3527); the 1280 three-pane is unregressed (982px, panes 280/326/340, one row, no document overflow). CARVE-OUT, named not swallowed: a funnel with frame_config_json NULL AND frame_template_id NULL — the state POST /quotes gives every new quote's first funnel — still shows Themes [] / 31,891 vs Templates 6 regions / 68,496, and there "match Templates" and "match what the visitor actually sees" point in OPPOSITE directions, so it is the owner's call: ADJ-P8-15. The positive site-logo leg is cited to review #1 (no fixture site carries a logo; the logo path is byte-unchanged since)) | review-p8-2c/c-b4-themes-A-1280.png, review-p8-2c/c-b4-templates-A-1280.png, review-p8-2c/c-b4-sitepicker-375.png, review-p8-2c/c-b4-sitepicker-changed-375.png, review-p8-2c/c-themes-1280.png, review-p8-2/b4-themes-A-seed-goodlogo-1280.png |
| B5 | "in the middle create different funnels side by side and drag sections boxes to the page of the wanted funnle" / "how he can see them side by side?" | ≥2 funnel columns fully visible at 1280 with 4 funnels; a section draggable into EVERY column incl. scroll-reachable ones or a visible reason; shared column genuinely pinned or the false comment corrected; keyboard + touch position stated explicitly (contract §5 B5) | P8-2 | PASS(certified by review #6 (SHIP) after FIVE prior rounds on one guard. Driven end to end on content the reviewer authored: fully-visible funnel columns **2 of 5** at 1280 with BOTH rails (216/275px) and zero document overflow; a real pointer drag auto-scrolled 0→880 and landed in the FURTHEST funnel, proven in storage; the dead sticky shared column (x 583→−1127 under a comment claiming it never scrolls) is now static at x 491 before AND after a full scroll, and the false comment is corrected; FIVE distinct silent-drop paths now each show a legible anchored reason with the columns unmoved; Enter opens a picker listing the shared page and all five funnels and landed a section in the furthest one; touch is grip-only and stated truthfully (card-body swipe scrolls, grip drag lands on the shared band, and the 375 rail↔board separation of 1853px against an 812 viewport makes the visible-reason fallback the honest position). THE GUARD, finally correct as an INVARIANT rather than a mechanism: armed iff a click will actually reach the card (`kind lib && !touch && releaseOverLibCard`). Verified 3×2 (mouse@1280, touch@1280, touch@375) × (release-on-source, miss): 0px popover overlap in every cell and tap #1 alive in every cell. The reviewer then found the STRUCTURAL reason it is exact — Chromium's touchmove-slop suppressor and its tap-slop click threshold are the SAME 15px constant, so a touch gesture can never be `moved` while a compatibility click is still coming — and a 12-rung slop ladder plus five gesture-shape attacks found no band where a refusal coexists with a delivered click. Conductor note kept deliberately: four of those five rounds traded one defect for another because MY briefs named where to set a flag instead of the property that had to hold) | review-p8-2f/f-b5-geometry-1280.png, review-p8-2f/f-b5-landed-furthest-1280.png, review-p8-2f/f-b5-sharedband-fullscroll-1280.png, review-p8-2f/f-b5-keyboard-pick-landed-1280.png, review-p8-2f/f-touch-grip-lands-shared-1280.png, review-p8-2f/f-inv-touch-375-release-on-source-tap1.png |

## Majors

| ID | Anchor (owner verbatim) | Requirement | Phase | Status | Evidence |
|---|---|---|---|---|---|
| M1 | "Add a 'I' 'funnel layout element' - progress bar - I clearly explained that I want different types of progress bars and to design them with a dedicated box!" | Progress as one coherent element: custom icon via existing media picker; Marker icon select only for icon_on_track; icon reaches style-picker + saved-template thumbnails; alignment→3 renders; position→2 renders; show_label honest on numbered; one label wording (contract §6 M1) | P8-4 | DEVIATES(contract measured: 6 hardcoded glyphs; select for all five styles; progress.icon reaches no thumbnail; alignment 3 values → 1 render f6a154c2; position 2 values == b5800cd2; show_label ON==OFF b44f6019; 3 label wordings) | — |
| M2 | "theme is only design language!!!! colors, fonts, sizes" | Every one of the ~80 authorable keys governs a measurable painted value on a VISIBLE element or is removed from the UI; guard enumerates all groups with a visible-computed-value predicate, empty allowlist (contract §4 R3, §6 M2) | P8-3 | PASS(all 34 authorable inline theme_json keys measured ALIVE by the CONDUCTOR'S OWN sweep, 3 identical runs, fixture verified restored — 0 DEAD / 0 MIS-TARGETED / 0 UNMEASURABLE, key list enumerated FROM SOURCE and matching the contract's 34 without forcing. The single root cause was the token layer freezing COPIES of role/scale values into component slots, so a theme wrote the role and the painted component never read it; fixed for card_defaults (4 keys), button_defaults.casing, scales.shadow, palette.success, palette.card_background, palette.error and palette.accent. Two defects the contract MISSED were found by the sweep and fixed in-phase. The R3 guard now enumerates 129 keys from the type declarations with a VISIBLE-COMPUTED-VALUE predicate AND a label->target invariant that fails a MIS-TARGETED key — residual exemptions empty — proven by sabotage red-proofs on real rendered elements, not by injection into nothing. Review #1 caught that the conductor's ALIVE verdict had concealed a page-flooding mis-target (card_defaults.background_role re-pointed the GLOBAL role token: the whole 1280x900 frame flooded and all four inputs went unreadable); fixed, re-driven, moved coordinates 3 -> 1) | review-p8-3/r-m2-cardbg-success-1280.png, review-p8-3/r-m2-cardbg-success-375.png, review-p8-3b/s5-A1-error-inputs-1280.png, review-p8-3b/s5-A2-success-inputs-1280.png, m2/inline-sweep-conductor.txt, m2/repro-before.md |
| M3 | "I should be able to create as many templates I want, to save them, and to use them as presets in different 'Quotes'" | Applying a saved template changes what the page paints (proven across 3 element groups); confirm dialog's enumerated promises true or removed; A/B templates arms actually differ (contract §6 M3, R2-1) | P8-4 | DEVIATES(contract measured: 45/46 comparable leaves shadowed after apply on a saved funnel; all four dialog claims false; A/B arms byte-identical 5,408 chars) | — |
| M4 | "the mapping of what is auto-filled per field should definatly be an option" | Address whole-feature beyond B1: studio shows autofill default truthfully; no all-four mode:manual materialisation; fills cannot collide; validation exposes {regex,message}; copy leaks fixed (contract §6 M4; R6-2/R6-4/R7) | P8-5 | DEVIATES(contract measured: studio shows 4× Manual for renderer default 4× autofill; one unrelated Required tick persisted mode:manual ×4; fills picker offered colliding siblings; validation None/ZIP-5 only) | — |
| M5 | "the rules you build are using jargon, have no actions, and just poor poor execution" | No `(§` clause strings reach an operator surface; humanizer never invents field names; warning path humanized; messages name the question/choice row; publish-blocker language register reused; copy check in place (contract §4 R5, §6 M5) | P8-5 | DEVIATES(ADDITIONAL SITE found by review #5 driving the P8-2 board, recorded here because M5 owns this class and P8-5 owns M5: a genuine 400 renders a RAW ULID as the operator's reason — `quotes-handlers.ts:2841` (and `:3151`) emits `section ${section.public_id} vertical '…' is not one of the quote verticals`, which the board paints as "section lgs_01KZ27D1Z55HWZ8F7WD5… vertical 'finance' is not one of the quote verticals" in a 190px alert where the ULID is itself cut off; the sibling uniqueness 400 correctly names the section instead. Original contract finding: 79+10 `(§` literals live on the real save route; 'Ks Nm' invented for ks_nm; renderSaveProblems applies no humanization; "1 field need attention") | — |
| M6 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | Studio canvas parity: authored defaults painted as selected, dependency-hidden questions hidden, injected editor chrome removed; already-correct aspects untouched (contract §4 R4, §6 M6) | P8-5 | DEVIATES(contract measured: default renders unselected/aria-checked=false vs live selected; dependency-hidden always painted; badges/ghosts/chips injected) | — |
| M7 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | No editor chrome inside native `<option>` elements (3 renderers); no + Add choice ghosts for dropdowns (contract §6 M7) | P8-5 | DEVIATES(contract measured: 5 option-borne × spans all 0×0 unclickable across DropdownQuestion/SearchableDropdown/otherSelectMarkup) | — |
| M8 | "the first page is shared by **all** the funnels" | Emptying a shared page stops serving it to visitors — no orphan variant-section rows kept alive by the by-page_id fallback; sibling delete paths checked (contract §6 M8) | P8-6 | DEVIATES(**REFUTED BY DRIVING — the contract's M8 does not reproduce at this HEAD.** Conductor-driven before dispatch: `PUT {"slots":[]}` returns 200 with `sections: []`, and the visitor page then serves the shared section **0 times** (`r2fix_shared_cont` count 1 → 0); restoring the slot brings it back. The described orphan cannot exist: `updateSharedPageHandler` (quotes-handlers.ts:2573) runs an UNCONDITIONAL `DELETE FROM leadgen_funnel_variant_sections WHERE quote_id = ?`, not a slot_id-filtered one, and `grep 'slot_id IS NULL'` across api/src returns 0 hits. The resolver fallback the contract names (resolver.ts:732 loadDirectPageSections) is real and correctly cited — it simply has no orphan to find. Sibling deletes cascade cleanly (deleteFunnelHandler:2134-2174, deleteSharedPageHandler:2618-2628). NO fix dispatched. One unchecked path named for the future: putVariantHandler's pages-replace branch (:3815-3816) — its variant_sections coupling was not traced. Fixture restored and verified) | m8/does-not-reproduce.md |
| M9 | "I clearly defined the difference between pages and sections" | Stale copy naming deleted things fixed at all 5 cited sites — no "Review slide", no "Slides" scope label, no help pointing at a deleted canvas, no "create one below/in the Themes tab" lies (contract §6 M9) | P8-2 (1 of 5 done) + P8-4 (rest) | DEVIATES(CORRECTED after review #4 MAJOR-B — my earlier cell asserted six live citations of which FOUR were stale and one described a sub-item THIS PHASE HAD ALREADY FIXED, which hid a completed deliverable. Every line below re-verified by grep at HEAD: **DONE in P8-2** — the scope label `shared.ts:427 PROBLEM_SCOPE_LABELS.section` now reads `"Sections"` (was "Slides"), driven live by review #2 as problem-group titles ["Theme","Sections"]. **STILL OPEN, allocated to P8-4** — canvas-help copy `templates.ts:156` (the contract itself says "not :144"); "Review slide" at `activation.ts:32` AND `funnel.ts:1106` (my ADJ-P8-14 first mis-cited this as 1100); "No presets yet — create one below" `funnel.ts:3926` (contract cited 3474); "No themes yet — create one in the Themes tab" `templates.ts:1953` (contract cited 1878). ADJ-P8-16 additionally inventories the live "slide" strings the contract never named) | src `shared.ts:427` (done) · `templates.ts:156`, `activation.ts:32`, `funnel.ts:1106`, `funnel.ts:3926`, `templates.ts:1953` (open) · review-p8-2/m93-scope-label-activation-1280.png |
| M10 | "to use them as presets in different 'Quotes'" | Saved templates get a real thumbnail; the board's Template chip names the funnel's template instead of matching numeric ids against string ids forever-"Template" (contract §6 M10) | P8-4 | DEVIATES(contract measured: thumbnail_html has no consumer; saved templates get a 3-key raw-enum pill; templateLabelFor numeric-vs-string mismatch) | — |

## Minors

| ID | Anchor (owner verbatim) | Requirement | Phase | Status | Evidence |
|---|---|---|---|---|---|
| N1 | "the rules you build are using jargon" | No raw tokens as visible labels (Button/Card corners [sm\|md\|lg\|xl\|full], shadow enums, "default-funnel") | P8-3 | PASS(driven at 1280 and 375: the rail's Button corners / Card corners / Card shadow now read Small/Medium/Large/Extra large/Fully round with ZERO raw enum tokens anywhere, and the fourth control the contract names — Base visual design — reads 'Default Funnel Design'. A scout found that fourth control lives in funnel.ts, outside the theme files, so it would have been silently dropped; it also turned out to offer TWO options for ONE registry object, a false choice, now deduped AT THE PRODUCER with the label map as the single source. The board's Theme chip no longer prints the raw KV id thm_p8-repro — it reads the record's name, renamed client-side from the catalog the page already fetches) | review-p8-3/r-n1-basedesign-1280.png, review-p8-3/r-n1-basedesign-375.png, review-p8-3b/s2-board-chips-1280.png |
| N2 | "the rules you build are using jargon" | Rules helper stops printing "Operators map to eq · neq · gt · lt · gte · lte." | P8-6 | DEVIATES(contract confirmed live at ui-rules-builder.ts:2206) | — |
| N3 | "the rules you build are using jargon" | fieldLabel() no raw-id fallback for absent fields | P8-6 | DEVIATES(contract: raw id printed whenever field absent from entryFields/answerFields) | — |
| N4 | "the order of the pages could be changed" | "— page 0" becomes 1-based at BOTH sites (SSR + island) or neither | P8-6 | DEVIATES(contract: zero-based at ui-rules-builder.ts:1966 + :2450 vs board "PAGE 1") | — |
| N5 | "the rules you build are using jargon" | A/B copy drops "(must sum to 100%; stored as basis points, per-test Σ == 10000)" | P8-6 | DEVIATES(contract confirmed live at ab.ts:157) | — |
| N6 | "add button of 'add funnel', user should be able to add as many funnel he wants" | New funnels get distinguishable default names (not all "New funnel") | P8-4 | DEVIATES(contract confirmed live in columns + rule Target-funnel select) | — |
| N7 | "theme is only design language!!!! colors, fonts, sizes" | Themes selects don't truncate their own default ("Inherit from base de⌄") | P8-3 | PASS(fixed AT THE BOX after three rounds of string-shortening each created the next overflow. Final measurement, review #5: manager 2 selects / 22 options over=0 at 282/282; rail 21 selects / 115 options with exactly 2 overflows, BOTH operator-typed data, both carrying the full text as a title. The qualifier moved into an <optgroup> heading so it is said once per group instead of on every option. A page-global clip-reveal — enumerated from rendered markup, not a container allowlist — covers 12 leadgen routes with 1 copy each and 0 on /admin, /admin/pages, /admin/articles. Its first version DEFEATED ITSELF (text-overflow:ellipsis collapses a select's scrollWidth to clientWidth, so the predicate flipped every second pass) and its test could not see that because the harness stubbed scrollWidth as a constant — the E11 shape. Both fixed: 17 readings -> 1 state, 30 rAF frames -> 1 state. Its second version DESTROYED a product-authored tooltip on routes it newly covered; now stashed, composed and restored with outerHTML byte-identical) | review-p8-3e/REVIEW.md, m2/n7-n11-after.md, m2/n7-select-zoom-3x.png |
| N8 | "The AB test can be also in the funnel level and not only in the page level." | Create A/B test gives visible confirmation instead of a bare reload (the test IS created) | P8-6 | DEVIATES(contract: reload to Funnel builder, no message) | — |
| N9 | "Add a 'I' 'funnel layout element' - progress bar" | Element letters read sensibly with the owner pins Progress=I, Footer=J (today A–F then H I J, G skipped mid-list) | P8-4 | DEVIATES(contract: letters skip G; nine tiles cannot fill ten letters — deliberate relabel required) | — |
| N10 | "there is no 'control' funnel!!!" | New Quote copy stops promising "A funnel + control variant are created automatically" | P8-6 | DEVIATES(contract confirmed live; contradicts A/B's own "Equal arms; no control") | — |
| N11 | "to use them as presets in different 'Quotes'" | "Apply to this funnel" / "A/B this theme" not enabled with zero presets while help says presets are shared | P8-3 | PASS(both branches proven. Zero presets: the two actions are visibly unavailable — 6 computed properties differ, cursor not-allowed, an amber dashed reason box on screen rather than a tooltip alone — and the panel gives ONE instruction naming a destination that resolves, verified by following its own link through the real router to a 200 with id=tm-new-theme. With presets: both enabled, descriptive titles restored. A late catalog at 13s un-sticks a previously latched failure and the poll settles to zero pending timers. The select's old 'create one below' text — the contract's own M9.4 example of a lie, rendered inside the panel this clause exists to make honest — is gone. This is not the hardening §1 forbids: nothing is blocked that could otherwise succeed, and the reviewer cleared it explicitly) | review-p8-3b/s2-preset-ZERO-crop-1280.png, review-p8-3b/s2-preset-READY-crop-1280.png, review-p8-3c/d7-zero-preset-panel-1280.png, review-p8-3c/d7-zero-preset-panel-375.png |
| N12 | "theme is only design language!!!! colors, fonts, sizes" | Alignment vocabularies deliberate: logo Left/Center vs progress Left/Center/Right resolved | P8-4 | DEVIATES(contract: two vocabularies, no rationale) | — |
| N13 | "I can define that only the number is sent, and I can define that the number will be sent as string" | Payload sample chip uses the JSON-literal formatter its sibling uses (one-call fix) | P8-6 | DEVIATES(contract: "Output preview" chip uses String(out) while sibling uses outputFormatJsonLiteral) | — |
| N14 | "You must give the user the option to pick his desired slider from sliders list!!!" | Radial slider first tap registers where pressed (contract marks UNVERIFIED — reproduce first) | P8-5 | DEVIATES(contract source-read: input.focus() before dialTo(ev) at engine.ts:1169-1171; 45° press recorded 15 expected 13 — UNVERIFIED, reproduce before fixing) | — |
| N15 | Image11 | dual_range pill values sit UNDER the handles per Image11; no duplicated value text (pills + end labels both "$0 \| $1,000") | P8-5 | DEVIATES(contract measured: pills y=1332 h=23 vs track y=1370 — 15px above; values duplicated. Open Image11 before fixing) | — |
| N16 | "Each one of this questions is answering **another field** for the offers payload" | Studio polish: selection badges don't occlude grid labels; no removeChild pageerror; inspector titles Phone/Address correctly; "1 field need attention" grammar; slider aria-label not raw id | P8-5 | DEVIATES(contract measured: all five symptoms live) | — |
| N17 | "free text (rich toolbar), links to legal pages (from the "pages" tab) that the user is choosing, Logo, company details" | The element list stops presenting "free text (rich toolbar)" and "company details" as separate things the product merges | P8-4 | DEVIATES(contract: A.2 list vs merged product) | — |
| N18 | "theme is only design language!!!! colors, fonts, sizes" | typography.display_size stops bleeding into the header logo (scales -m 1.1→2.53rem, leaves -s/-l untouched) | P8-3 | INCONCLUSIVE(step: on a site that HAS a logo, load the funnel at display_size m and at xxl and confirm the rendered .lg-logo font-size is identical, at 1280 and 375. The FIX is verified as far as this fixture allows: header.logoFontSize is removed from DISPLAY_FONTSIZE_PATHS and all three .lg-frame-header--logo-{s,m,l} .lg-logo rules are byte-identical across display_size in the stylesheet served by the real route, while the driven headline still scales 31px -> 71.3px — so the bleed is gone and display type still ramps. But the fixture site has NO logo: .lg-logo matched 0 nodes on every driven page, so there is no E10 visible-element proof and this is NOT recorded as PASS. Downgraded from PASS by the conductor after check_register's R3 rule correctly refused a behavioural PASS with no screenshot — the rule caught the conductor, which is what it is for) | review-p8-3/REVIEW.md |
| N19 | "why the user can choose the same page more than ones in the same funnel???" | A-4 uniqueness message wording + index honest (rule itself is sanctioned) | P8-6 | DEVIATES(contract: empty-funnel rejection says "'X' is already in this funnel" with field key sections.1) | — |
| N20 | "theme is only design language!!!! colors, fonts, sizes" | One font vocabulary across rail and manager (today Literata/Sora/System… vs Newsreader/Inter/Roboto Mono…) | P8-3 | PASS(rail and manager offer the identical 8 self-hosted families in the same order — the only families fonts.generated.ts actually vendors. A family already stored in someone's theme stays selectable and still displays correctly: driven, a funnel storing Literata paints its label with the option hidden from fresh choice. The first attempt labelled these '(legacy)', which reintroduced the owner's own jargon complaint and was caught ONLY because verify:all's jargon gate exits non-zero — the suite was 7992 green at that moment. Replaced with 'shows as default font', which states the visitor-facing outcome in the operator's language) | review-p8-3e/e3-manager-1280.png, review-p8-3e/e3-manager-375.png, review-p8-3e/e5-rail-themes-1280.png, review-p8-3e/e5-rail-themes-375.png |
## Cross-cutting requirements

| ID | Anchor (owner verbatim) | Requirement | Phase | Status | Evidence |
|---|---|---|---|---|---|
| REQ-R1 | "if I want to auto fill only for street address and city" | Every producer→consumer pair touched is proven by driving the consumer; two-reader wire shapes converged to one, other deleted (contract §4 R1) | all | DEVIATES(R1-1 open at seed; discipline applies to every P8 fix) | — |
| REQ-R3 | "theme is only design language!!!! colors, fonts, sizes" | The 80-key sweep table: every key honoured-or-removed with measured painted values; guard re-predicated (contract §4 R3) | P8-3 | DEVIATES(4 dead + 2 mis-targeted at seed; guard predicate wrong) | — |
| REQ-R5 | "the rules you build are using jargon" | The copy check exists: no `(§` string can reach a user-visible surface (contract §6 M5 last clause) | P8-5 | DEVIATES(no such check at seed; 89 literals reachable) | — |

## Found beyond the contract (surfaced for the owner's fix-or-defer ruling, NOT silently fixed)

| ID | Anchor (owner verbatim) | Finding (measured) | Phase | Status | Evidence |
|---|---|---|---|---|---|
| ADJ-P8-1 | "how the user defines a theme per funnel?" (same "authored value never reaches the live page" class, different producer) | `patchSectionHandler` (sections-handlers.ts) never bumps variant content versions — grepped: 0 calls to bumpActiveVariantContentVersions / invalidateOnVariantPublish in its body, while frame-handlers has 3 and themes-handlers now 1. Driven: a section headline PATCH (200) left `content_version` at 110 and the live page kept serving the old headline; section content edits reach visitors only when the 300s cache expires. Distinct from the owner-referred `caches.default` item (this is a missing bump, not the Cache-API layer) and OUTSIDE every enumerated P8 clause, so it is surfaced rather than fixed. | P8-1 | BLOCKED(owner ruling: fix-or-defer — a section edit is invisible to visitors for up to 5 minutes) | b2/after.md |
| ADJ-P8-2 | "if I want to auto fill only for street address and city" | When the Places SDK is attached and Google REJECTS the key, the street field stops accepting typing: 22 keystrokes landed **5** ("1600 ") and the input carries Google's `gm-err-autocomplete` class with warning glyphs painted over it. Same-page control with Maps off: street 22/22 and city 22/22, class plain `lg-input`. So a misscoped production key does not degrade to honest manual entry — it degrades the visitor's ability to enter an address at all, on the money path. (The contract's proven "keyless degrades honestly" case is a different state: no key → no SDK → plain input, measured clean.) | P8-1 | BLOCKED(owner ruling: fix-or-defer — recommend a key-failure fallback that detaches the widget and restores manual typing; not built, as no clause asks for it and §1 forbids unrequested mechanism) | b1/after.md, b1/multi-field-address-autocomplete-job-on--1280.png |

| ADJ-P8-3 | "Another poor execution. Here is the issues summary" (test-infrastructure hazard found while satisfying the contract's own p3a recapture ritual) | The p3a byte-pinned fixture captures a WALL-CLOCK value: `resolveTimeframe` (`ui-shared.ts:75`) uses `new Date()`, so `data-analytics-from` moved 2026-07-04 -> 2026-07-05 between two captures in one session. A byte-exact parity suite pinned to a rolling date fails on a date boundary with no code change — a latent daily CI flake. **CONDUCTOR CORRECTION (combined review, MINOR-7): THIS ROW WAS STALE AND ITS CLAIM IS FALSE AT HEAD.** The hazard was closed on 2026-07-23 by `6bfb05fe`, three weeks before I was still carrying it as an open owner decision. Verified by my own hand at HEAD: `test/leadgen-p3a-split-parity.test.ts:227` defines an `ANALYTICS_DATE_RE` matching both `data-analytics-from` and `data-analytics-to` and normalises them before comparing, and `:272-274` is a clock-faked durability proof asserting the window resolves to `2027-03-15` / `2027-02-14` and specifically NOT to a stale captured date. The suite therefore does not flake on a date boundary. It was proven again this phase: a recapture that crossed midnight moved `data-analytics-from/to` by exactly one day in two fixtures and the parity suite stayed green (12 passed). **No owner ruling is needed and none should be asked for.** This is the third false claim found in a conductor-authored artifact this mission — a register row rots exactly like a code comment. Original text, kept for the record: freezing the clock was judged a harness redesign §3 did not authorise. | P8-1 | DEVIATES(row was stale — the hazard was already closed by 6bfb05fe on 2026-07-23; no action and no owner ruling are required. **CONDUCTOR CORRECTION #2, from the final review:** my first correction spliced its new text onto a surviving fragment of the old status, producing the nonsense "no owner rulingture date" and an EIGHTH column in a seven-column table — a botched edit inside the very row I was correcting for being false. Rewritten cleanly here.) | `leadgen-p3a-split-parity.test.ts:227` (`ANALYTICS_DATE_RE` normaliser) + `:272-274` (clock-faked 2027 durability proof) — verified at HEAD by the conductor |
| ADJ-P8-4 | "I should be able to create as many templates I want, to save them, and to use them as presets" | `validateFrameConfig` is shared between the VARIANT frame-overrides document (where `theme_id` is legitimate per v3.1 §10.1) and the FUNNEL-level frame document (where the theme lives in `theme_json`). After the F8 convergence a funnel-level document carrying `theme_id` is accepted-and-ignored rather than rejected. CORRECTED after review #2 (F-11): it is not merely tolerated by validation — it is STORED and ECHOED BACK in `effective_frame.theme_id` (measured: `PUT /funnels/B/frame {…,theme_id}` -> 200, then GET returns it inside `effective_frame`), so any reader of `effective_frame` would believe a theme is bound. `winningThemeId` (theme.ts:969) never reads it and no product UI writes it, so the impact is latent — but the owner's ruling should be made on the accurate description. Every scope-precise alternative needed either callers edited outside the slice or an options knob no clause asks for (§1), so it was flagged, not guessed. | P8-1 | BLOCKED(owner ruling: fix-or-defer — scope-aware validation vs accepted-and-ignored) | src `public/leadgen/designs/frames.ts:1673 validateFrameConfig`; re-confirmed LIVE by the conductor: `PUT /funnels/<B>/frame {…,theme_id}` -> 200 and `GET` echoes `effective_frame.theme_id = thm_p8-repro` |
| ADJ-P8-5 | "there is no 'control' funnel!!!" | Behaviour change disclosed, not hidden: "A/B this theme" previously REFUSED when the EDITOR funnel had no running test, using a badge that could never speak for a different target funnel. That mismatched pre-check was REMOVED (not replaced by a new one), so the button now creates-and-starts the target funnel's test exactly as its sibling "A/B templates" always has. No gate was added; one wrong-target gate was removed. | P8-1 | BLOCKED(owner ruling: confirm this is the wanted behaviour) | src `admin/leadgen/quotes-tabs/funnel.ts:4140 abThisThemeOnTarget` — verified at HEAD |
| ADJ-P8-6 | "there is no 'control' funnel!!!" | `quotes-tabs/ab.ts:24-33 overriddenGroupLabels` skips non-object values, so a per-arm `theme_id` override is silently omitted from the arm's "Varies: ..." line — the operator sees no indication that the arm varies by theme. Display-only (nothing is rejected or lost), found during the F8 reader/writer audit, outside its owned files. | P8-1 | BLOCKED(owner ruling: fix-or-defer) | src `admin/leadgen/quotes-tabs/ab.ts:24 overriddenGroupLabels` — verified at HEAD |

| ADJ-P8-7 | "how the user defines a theme per funnel?" | Editing ANY theme control on a funnel bound to a theme RECORD silently dissolves the binding: `themes.ts:672-676 presetInlineOrAbort` turns `theme_json` from `{"theme_id":"thm_p8-repro"}` into a 14-key literal palette. Driven twice by review #2 (Funnel A via `field_defaults.min_height`; Charlie at 375). PRE-EXISTING and byte-identical at f240788 (sanctioned by an earlier round's "R2 P2 FIX-FIRST-2" inline-fork decision), so NOT a regression of this phase and NOT unilaterally reversed here — but it defeats in use the very capability B2 exists to prove: after one unrelated size nudge, later theme-record PATCHes no longer reach that funnel's live page, with no warning to the operator. | P8-1 | BLOCKED(owner ruling: fix-or-defer — reversing a previously sanctioned design decision is the owner's call, not the conductor's) | review-p8-1b/b3-drive-log.txt |

| ADJ-P8-8 | "theme is only design language!!!! colors, fonts, sizes" | CONDUCTOR ADJUDICATION of review #2's blocker F-3 ("a harmony step writes a colour derived from the WRONG funnel's palette"): **REFUTED on evidence.** Spec `docs/leadgen/redesign-contract-v2.5/09-theme-token-palette-contract.md:34` verbatim — *"curated harmonies derived from the base design (base value, wash, dark/light steps)"* — and the panel's own copy reads "Suggested from this design's base value". Measured live: Bravo and Delta (no authored theme) both resolve `brand_primary #1B3A5C`, i.e. that IS the shared base design; the disputed write `#142c45` is 25% black on `#1B3A5C`, which is NEITHER Funnel A's authored `#AB1234` NOR Charlie's `#0E7C3A`. So the harmony derived from the specified source, not from another funnel's palette. The implementer refused to enshrine the reviewer's reading and was correct to. REAL residual it surfaced instead: `base_tokens` is per-DESIGN and only the editor funnel's rides the wire (`GET /funnels/:id/theme` returns `{theme, effective_tokens}` with no base table), so a funnel on a DIFFERENT design would show the editor design's harmony chips. Unreachable today — `designs/registry.ts:36 FUNNEL_DESIGNS` contains exactly ONE design registered under two keys — so no machinery was built for a second design that does not exist (§1). | P8-1 | BLOCKED(owner ruling: fix-or-defer, and only if a second funnel design is ever added — the fix is to carry the target funnel's base tokens in `frame-handlers.ts themeProjection`) | review-p8-1b/harmony-charlie-1280.png |

| ADJ-P8-9 | "the mapping of what is auto-filled per field should definatly be an option, I clearly defined it, but not in this poor way!!!!" | **CONDUCTOR ADJUDICATION WITHDRAWN — I WAS WRONG.** I refuted review #2's F-13 on the basis that a lone `full_address` with `mode:"autofill"` is "a content shape the product's authoring UI CANNOT produce". Review #3 falsified that by DRIVING the real studio: the Mode select offers Autofill for every field kind whenever Maps is enabled and a key is configured (`ui-section-studio.ts:12225-12226`), the operator saved `props.fields=[{"field":"full_address","mode":"autofill","label":"Address"}]`, and the served page emitted `enable_autocomplete:true`, injected the SDK and called maps.googleapis.com. My error: I checked how the studio SEEDS that row ("Plain text address" -> manual) and never checked the Mode control the operator can then change it with — reasoning from the wrong evidence and calling a reachable state unreachable. THE REAL DEFECT, now being fixed in-phase rather than deferred: `presets.ts:3273-3277` gates the lone branch's `data-lg-maps` on the NODE-level maps flag ONLY and never on the row's `mode`, so the per-field Mode control was INERT. CORRECTED after review #4 (F-12): the fix gates the MANUAL direction only — `mode:"manual"` now emits no attribute even with the node job on (driven: 0 SDK requests, 0 ctor calls), while `mode:"autofill"` still additionally requires the node-level Maps job. That is coherent and the studio's own note says so, but my earlier wording 'inert in both directions ... now honoured' overstated what changed. That is an offered control the renderer cannot honour (contract R3's class) and it is the owner's clause above unmet. The owner's free-text scenario (contract §8) is preserved: "Plain text address" seeds mode MANUAL with Maps unchecked. | P8-1 | DEVIATES(my refutation withdrawn on review #3's driven evidence; per-field mode now honoured on the lone branch — DRIVEN AND CONFIRMED by review #5's truth table: manual+jobON = no data-lg-maps and no data-address-autocomplete, autofill+jobON = both, free-text §8 case unchanged. The wider per-field-mode work stays M4/P8-5) | review-p8-1c/b1-lonefull-autofill-1280.png, review-p8-1c/b1-lonefull-autofill-375.png |

| ADJ-P8-10 | "theme is only design language!!!! colors, fonts, sizes" | Two DISPLAY-only truths the rail still gets wrong, both found by review #5 driving, both outside every enumerated clause (no write is mis-targeted): (a) when an ARM is bound to a theme RECORD (exactly what "A/B this theme" creates), the rail paints Brand primary rgb(14,124,58) tagged "This funnel" while that same arm RENDERS `--lg-primary #AB1234` — a green swatch beside a maroon canvas 30px away; cause: `resolveRoleValue` falls back to the funnel's inline palette while `theme.ts:1106` suppresses that palette once a record wins. (b) A funnel bound to a theme record paints the record's colours while all 14 rows read "Base design" — pre-existing, byte-equivalent logic at f240788. Neither is a wrong-target WRITE; both are the rail describing state the product does not render. | P8-1 | BLOCKED(owner ruling: fix-or-defer — the rail's inheritance vocabulary is binary (09 §9.3) and has no word for "bound to a record", so honouring this needs a vocabulary decision, not just a code change) | review-p8-1e/d2-arm-record-rail-vs-render-1280.png |

| ADJ-P8-11 | "kick out all the stupid and unusable components from the 'Funnel builder'" | The section library offers cards the save REFUSES: dragging "P8 Address Repro v2" (vertical `finance`) onto any funnel returns HTTP 400 `vertical 'finance' is not one of the quote verticals`. A card that can never be dropped is contract R3's corollary ("a control that cannot be honoured must not be offered") on the board surface. CORRECTED after review #1 (F-11): the refusal is NOT silent — the operator is shown the RAW VALIDATOR STRING WITH A RAW ULID: `section lgs_01KZ27D1Z55HWZ8F7WD5H9396T vertical 'finance' is not one of the quote verticals`. That is contract R5's jargon class on the board surface, so this row is two defects, not one. CONFIRMED AGAIN in the P8-2 fix round (a drop onto two sections returned 400): the library feed offers 6 sections of which exactly 2 carry `vertical=finance` against an `r2fix_vertical` quote — those two are the undroppable cards, so the failure is taxonomy, NOT funnel-specific (a valid section lands in every column, including the furthest, proven by review #2). Conductor disclosure: both offending sections are MY OWN fixture residue from the early B1 reproduction, so the fixture exaggerates how often an operator meets this — the defect itself (offering a card the save refuses) is real and pre-existing. Found by S2.2 driving; the available-sections feed is outside that slice's files. | P8-2 | BLOCKED(owner ruling: fix-or-defer — filter the feed to droppable sections, or show why a card is not droppable before the drag) | review-p8-2/b5-drop-server-refusal-finance-1280.png |
| ADJ-P8-12 | "the order of the pages could be changed" | "Delete page" cannot delete a funnel's LAST page: `PUT /variants/:id {pages:[]}` returns 400 `pages must be a non-empty array`, and the operator is shown that RAW VALIDATOR STRING rather than an explanation. Two defects in one: an unreachable operation and jargon on the surface (contract §4 R5's class). | P8-2 | BLOCKED(owner ruling: fix-or-defer — is a funnel allowed zero pages? the answer decides whether this is a copy fix or a capability fix) | review-p8-2b/MEASUREMENTS.txt |
| ADJ-P8-13 | "the rules you build are using jargon, have no actions" | The routing-rules rail's `role="button"` spans still have NO keyboard activation. S2.2's keyboard work was scoped to the board surfaces, so the rail remains mouse-only — an accessibility gap on a surface the owner already called out for poor execution. | P8-2 | BLOCKED(owner ruling: fix-or-defer — belongs with the rules-rail work, not the board) | review-p8-2b/MEASUREMENTS.txt |
| ADJ-P8-14 | "I clearly defined the difference between pages and sections" | Contract M9.2's SECOND site drifted: the contract cites `funnel.ts:1084` for the "Review slide" fix-link copy; it now sits at **funnel.ts:1106** (my earlier '1100' was wrong WHEN WRITTEN — review #3 caught it in the very row whose purpose was to correct a stale cite; 1106 verified at HEAD by grep). Recorded so P8-4 (which owns M9) fixes the real line rather than the stale cite — the product has no slides. | P8-4 | DEVIATES(open, allocated to P8-4 by the approved plan; NOT fixed in P8-2 despite the file being open, because the plan's allocation is binding) | src `admin/leadgen/quotes-tabs/funnel.ts:1106` + review-p8-2/m93-slide-copy-templates-1280.png |

| ADJ-P8-15 | "Add a real section to the canvas to the user could actually see the design he creates." | B4's acceptance sub-clause *"matching Templates for the same site and moment"* is UNMET on a funnel with `frame_config_json` NULL **and** `frame_template_id` NULL — the state `POST /quotes` gives every new quote's FIRST funnel. Measured by review #1 on a quote it authored: Themes canvas = regions `[]` / CSS 31,891; Templates = 6 regions / 68,496 — the contract's own B4 numbers, reproducing verbatim. The reviewer independently confirmed the implementer's diagnosis that **Templates is the divergent side**: `POST /variants/:id/preview` on the STORED state also returns 31,891/`[]`, and the six regions come only from the Templates island injecting a `draft_frame_config`; the live `/lg` visitor page also legacy-shells there. So "match Templates" and "match what the visitor sees" point in OPPOSITE directions on this state, and only the owner can say which the canvas should show. | P8-2 | BLOCKED(owner ruling: should the Themes canvas mirror the live page (legacy shell, honest) or the Templates canvas (composed draft, aspirational) for a funnel with no frame and no template?) | review-p8-2/b4-residual-themes-NULLFRAME-1280.png, review-p8-2/b4-residual-templates-NULLFRAME-1280.png |
| ADJ-P8-16 | "I clearly defined the difference between pages and sections" | Contract M9's "no slides" class survives in LIVE operator strings beyond the two the contract cites — review #1 drove them: `templates.ts:894` and `themes.ts:200` (*"affects every **slide** of this funnel"*), `templates.ts:753` (*"Progress counts the **slides**…"*), `quotes-handlers.ts:6516` (*"**Slide** N '…' … on every **slide**"*), plus `shared.ts:997 scopeHead()` which carries the string with ZERO consumers (dead export). ADJ-P8-14 re-points only one line and misses these. Not fixed in P8-2 because the approved plan allocates M9 to P8-4 — recorded here as the full inventory so P8-4 fixes the class rather than the cited lines. | P8-4 | DEVIATES(open, plan-allocated to P8-4; full site list above) | review-p8-2/m93-slide-copy-templates-1280.png |

| ADJ-P8-17 | "Add a real section to the canvas to the user could actually see the design he creates." | At 375 the Themes 3-pane overflows horizontally by ~47px (`scrollWidth 390` vs `clientWidth 343`). Sole overflower, isolated by review #2: `#lg-theme-canvas-section-name` (`themes.ts:348`), a `form-help` span in a `justify-content:space-between` flex row with no `min-width:0` / `overflow-wrap`, so a long unbroken section name runs past the screen under `body{overflow-x:hidden}`. Pre-existing markup this phase did not touch, and B4's own picker-reachability clause is met — surfaced rather than fixed so the fix is not smuggled in under an unrelated clause. | P8-2 | BLOCKED(owner ruling: fix-or-defer — a one-line CSS constraint, but it is a mobile-layout change no clause asks for) | review-p8-2b/MEASUREMENTS.txt |
| ADJ-P8-18 | "Another poor execution. Here is the issues summary" (test-infrastructure honesty) | `leadgen-p3a-split-parity.test.ts`'s header still says the fixtures were *"captured once, before this phase touched ui-quotes.ts"* and that it *"does NOT re-derive expected content (that would just test the code against itself)"*. This mission has re-captured them repeatedly (each time classifying every changed line), so for the changed bytes the suite IS now a self-baseline while its own docstring denies it. Substance is sound — every recapture was line-by-line justified — but the file misdescribes what it does, which is the same class of defect the contract keeps finding in product copy. | P8-2 | BLOCKED(owner ruling: fix-or-defer — rewrite the docstring to describe a conductor-classified self-baseline, or restore a true frozen pre-split capture) | src `test/leadgen-p3a-split-parity.test.ts:14,16` (the two docstring sentences) — verified at HEAD |

| ADJ-P8-19 | "in the middle create different funnels side by side and drag sections boxes to the page of the wanted funnle" | Two board behaviours review #4 measured that no contract clause names, recorded so they are not mistaken for closed: (a) **Escape does not cancel an in-flight drag** — `funnel.ts:6061`'s Escape handler (my earlier :6003 drifted when a later commit added 58 lines above it — the recurring cite class, caught again) closes menus but never calls `endDrag()`, so the ghost survives (`ghostAliveAfterEsc = true`); §5 B5 says nothing about Escape-cancel and the guard is NOT defeated (the next tap still opens the picker). (b) At 375 a grip drag reaches the shared band only from the LOWER library cards — from the top of the rail the separation is ~998px against an 812 viewport; the register claims only that *a* grip drag lands there, which is true, and the acceptance's visible-reason fallback is measured at [29,742,317x70]. | P8-2 | BLOCKED(owner ruling: fix-or-defer — Escape-cancel is a conventional affordance the contract never asked for) | review-p8-2d/d-m1f-escape-then-tap-1280.png |
| ADJ-P8-20 | "Another poor execution. Here is the issues summary" (evidence hygiene) | Review #4 sha256-verified FOUR groups of byte-identical screenshots in `evidence/p8/review-p8-2c/` that are NAMED for distinct moments: `c-f2-swipe-cardbody-fingerdown-375.png` == `…-after-375.png`; `c-reg-failed-theme-get-1280.png` == `…-after-write-1280.png`; `c-builder-375.png` == `c-touch-cannot-reach-mid-375.png` (a "mid-gesture" file showing no drag ghost); and `c-b4-sitepicker-1280.png` == `c-b4-themes-A-1280.png` == `c-themes-audit-1280.png` == `c-themes-formhelp-region-1280.png`, the last of which is register-cited on B4. Review #4 re-drove every underlying claim and all hold, so this is not a false PASS — but an artifact that does not show the moment its name claims is the same family as citing a log that does not contain the measurement, and this mission has now hit that family five times. | P8-2 | BLOCKED(owner ruling: accept as-is given the claims were independently re-driven, or require moment-distinct artifacts) | review-p8-2d/d-b5-geometry-1280.png |

| ADJ-P8-21 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | **The visitor never sees the error border on a whole class of field, and the Studio preview does — the two diverge.** `runtime/render.ts:228 setFieldError` reaches for `fieldEl.querySelector("[data-lg-input]")`, so on the shape where the field block IS the input (FreeTextQuestion) NO `aria-invalid` is set and the `.lg-input[aria-invalid="true"]` border rule never fires live; `admin/leadgen/preview-sim.ts:256` DOES handle that shape, so the canvas paints an error state the live page cannot. Found by S3.9 while wiring the error role (its spec deliberately asserts only the ERROR_CLASS colour for that shape, so it does not paper over this). Inside M6's canvas-parity clause, scheduled for P8-5 — not an owner deferral. | P8-5 | DEVIATES(live vs canvas divergence on the FreeTextQuestion shape; not yet driven by the conductor) | — |
| ADJ-P8-22 | "the rules you build are using jargon" | **The Studio's required-field error writes its copy into an element that stays hidden, and a test calls that "visible".** `preview-sim.ts` fills the message slot but never removes `hidden` — measured output `<p class="lg-error lg-error-auto" … hidden …>This field is required.</p>` — while `test/leadgen-sections-api.test.ts:1098` asserts a "visible required message" via a substring check that a hidden element satisfies. That is the contract's own false-green shape (E10: existence is not behaviour) sitting inside a test that reads as coverage. Inside M5/M6, scheduled for P8-5. | P8-5 | DEVIATES(hidden element carries the message; the test asserting it is visible cannot fail) | — |

| ADJ-P8-23 | "theme is only design language!!!! colors, fonts, sizes" | **An entire stylesheet in the product can never match anything.** `banner-default/styles.ts:22` scopes the whole banner sheet to `DEFAULT_BANNER_SCOPE = '[data-banner-design="banner-default"]'`, and **no code anywhere in `src/` ever writes that attribute** (conductor-verified: the single non-stylesheet hit is an explanatory comment a P8-3 slice added). Compounding it, `registry.ts:45-47` hands `bannerChromeCss` the UNRESOLVED base `defaultFunnelDesign.banner`, so even if the scope were produced, no theme value would reach it. **CONDUCTOR CORRECTION (mine, caught by S3.11 and re-verified by hand):** I originally wrote that this is why the accent role's "Used by: … recommended" promise cannot be kept. That is FALSE. `default-funnel/styles.ts:1925` carries a LIVE `.lg-banner[data-recommended="true"]` rule reading `banner.recommendedBorder`, and `auction/banner.ts:303` really emits `data-recommended="true"` — so accent's recommended promise IS honoured, by a different sheet from the dead one. The finding that survives is narrower and still real: the `banner-default` sheet alone is unreachable, and it is the sole reader of `banner.recommendedCtaBackground`/`recommendedBadgeBg`. Also zero-reader, same family: `color.recommendedBorder`/`recommendedBg`/`recommendedGlow` (tokens.ts:30). This is a whole component's wiring, not one of R3's 80 authorable keys, so it is surfaced rather than fixed unasked — but it is the same root cause as every R3 defect: a token layer whose consumers cannot be reached. | P8-3 | BLOCKED(owner ruling: fix-or-defer — wire the banner scope + resolved tokens, or delete the sheet and its dead tokens. Neither is a theme-key fix and both change more than M2 asks for) | — |

| ADJ-P8-24 | "theme is only design language!!!! colors, fonts, sizes" | **The Themes manager offers 6 of the 7 semantic colour roles as swatches; `error` has no tile at all.** `THEME_RECORD_ROLE_KEYS` has 7 members but `ui-theme-manager.ts`'s `ROLE_META` omits `error`, so the operator can only reach it through an unlabeled Advanced hex row — on the one role whose colour the visitor sees at the moment a form rejects their input. Found by S3.11 while auditing the "Used by" text; not fixed because adding a rendered swatch tile is structural, not the text-only change that slice owned, and no contract clause names it. The role itself IS wired and measured ALIVE (P8-3 S3.9). | P8-3 | BLOCKED(owner ruling: fix-or-defer — add the missing error swatch to the manager, or accept Advanced-only editing for that one role) | — |

| ADJ-P8-25 | "theme is only design language!!!! colors, fonts, sizes" | **The same too-narrow-select shape N7 fixed survives on a second admin page, under a second definition of the same class.** P8-3 fixed N7 at the container: `quotes-tabs/shared.ts:622 .lg-scalars` became `repeat(auto-fit,minmax(220px,1fr))`. But `ui-auctions.ts:119-120` defines its OWN `.lg-scalars{grid-template-columns:repeat(2,1fr)}` + a 640px query and wraps five control groups in it (`:475,:624,:634,:642,:650`) — untouched, because the auctions page is outside N7's clause ("Themes selects") and outside every P8-3 slice's ownership. Conductor-verified by grep, NOT driven: whether any auctions select actually truncates its own value has not been measured. Surfaced now because the class is identical and the phase that learned it is this one. NOTE also confirmed while checking this: the FROZEN Playwright baselines cannot be moved by the N7 fix — `test-ui/leadgen-v31-gate1c-baselines.spec.ts` targets `/admin/leadgen/sections/:id/edit`, which emits ZERO `.lg-scalars`, and `test-ui/leadgen-visual.spec.ts` drives the visitor runtime, which no P8-3 admin change touches. | P8-3 | BLOCKED(owner ruling: fix-or-defer — measure and fix the auctions page's selects, or accept that N7 was scoped to the theme surfaces) | — |

| ADJ-P8-26 | "theme is only design language!!!! colors, fonts, sizes" | **Conductor ruling on the clip-reveal's scope, recorded because it is a deliberate boundary and not an omission.** F10 placed the reveal in `admin/templates/layout.ts` — the shell this worker SHARES with the Conversions product — so it added 5,477 bytes to every conversions admin page and turned `test/conversions-admin-shell.test.ts` red (expected 25789 to be 20312). F12 re-scoped it to `admin/leadgen/clip-reveal.ts`, emitted from the Themes rail (which also covers the whole quote-editor board) and the Themes manager; the conversions test passes 11/11 UNEDITED and the reveal is driven absent from /admin, /admin/pages and /admin/leadgen/quotes. That leaves three leadgen pages (quotes list, /quotes/new, quote-not-found) without it. **I ruled NOT to extend**, on measurement rather than preference: F12 drove all three filter selects and none clips (scrollWidth==clientWidth, 126/126 at 1280, 107/107 at 375), and N7's clause is scoped to the theme surfaces, so extending would be unrequested scope under §1. The clean superset — one include in `leadgenPageShell` (`admin/leadgen/ui.ts`, ~2 lines) — is identified and NOT applied. Noting the tension honestly: this phase's own root cause was universes bounded by convenience, and "those three do not clip today" is that shape of argument; what makes it acceptable here is that it is DRIVEN, and it is recorded so the owner can overrule it cheaply. | P8-3 | BLOCKED(owner ruling: fix-or-defer — leadgen-wide coverage vs theme-surface scope) **CONDUCTOR CORRECTION, review #3 MAJOR-2: the evidence I based this ruling on was WRONG.** Not three uncovered routes but ELEVEN select-bearing leadgen routes, and clipping DOES occur on them — quotes list `activity` +2px at 375 (my row claimed 107/107 clean), sections 4/4 at 375 (+31px), offers 7/7 at 375 (+79px), and Section Studio `#lg-preview-theme` +35px at BOTH widths on an operator-authored name. I accepted a slice's driven numbers without checking the route enumeration behind them — the same 'universe named by convenience' error this phase's root-cause pass is about, committed by me one paragraph after writing that pass. The ruling may still be right; the basis was not | — |

| ADJ-P8-27 | "how the user defines a theme per funnel?" (the Themes manager is the surface that answer lives on) | **A theme whose name is long can never be renamed or deleted — the product 500s and tells the operator "Internal Server Error".** Driven by review #3 on a live instance: an 83-character theme id returns HTTP 500 on a PATCH-that-changes-anything and 500 on DELETE (reproduced twice); a 25-character id returns 200/200 on both. The manager surfaces it as `#tm-error` = "Internal Server Error" (`review-p8-3c/d10-delete-500-*.png`). Pre-existing — `themes-handlers.ts` was not touched by P8-3 — and outside every contract clause, so surfaced rather than fixed. **Consequence already visible:** one orphan record the reviewer created (`thm_rv3c-reveal-oscillation-probe-theme-with-a-deliberately-long-operator-name-8f21`) is stuck in local dev KV because the product cannot delete it; no funnel references it. An operator who names a theme descriptively is permanently stuck with it. | P8-3 | BLOCKED(owner ruling: fix-or-defer — this is a data-loss-adjacent trap on the owner's own "theme per funnel" surface) | review-p8-3c/d10-delete-500-1280.png |

| ADJ-P8-28 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | **The Themes manager's font PREVIEW cannot show the font.** Measured by F13 while fixing N7: `document.fonts.size === 0` on `/admin/leadgen/themes` — the admin pages vendor NO `@font-face` at all — so whichever family the operator picks, the preview paints a system fallback. The operator chooses type by looking at something that is never the thing they are choosing. Same family as the contract's own R4 ("the canvas is not a faithful preview"), but on the theme surface rather than the section canvas, and named by no clause. Not fixed: vendoring the 8 self-hosted families into the admin shell is a real asset/bytes decision, and P8-3 has already been burned once for growing a shared shell. | P8-3 | BLOCKED(owner ruling: fix-or-defer — vendor the admin-side webfonts so the preview is honest, or label the preview as approximate) | — |

| ADJ-P8-29 | "theme is only design language!!!! colors, fonts, sizes" | **The clip-reveal's affordance is pointer-only, so at 375 a clipped operator-authored name has no way to be read.** The N7 clause itself is CLOSED — the theme surfaces' own text now fits (manager 282/282 over=0; the rail's only overflows at 375 are DATA-BEARING, i.e. names the operator typed). For those, the reveal supplies a `title` tooltip, which a touch device never shows. Review #4 measured the sharpest case: `/admin/leadgen/offers` at 375 paints seven 40px selects showing only a chevron. Beyond N7's clause (which is about the selects' own default text, now fixed) and beyond this phase's scope, so surfaced rather than fixed — but an operator on a phone cannot read their own funnel/section names on those surfaces. Related, pre-existing, also unsurfaced until now: `/admin/leadgen/themes` at 375 has `.tm-shell overflow-x:hidden` with 365/341, so 24px is unreachable and "+ New theme" sits at 381.6px, off a 375 viewport. | P8-3 | BLOCKED(owner ruling: fix-or-defer — give clipped operator data a non-pointer affordance at 375, or accept that the admin is a desktop surface and say so) | review-p8-3d/d10-mgr-375-fullpage.png |

| ADJ-P8-30 | "theme is only design language!!!! colors, fonts, sizes" | **Two latent holes in the clip-reveal, left UNFIXED on a deliberate conductor judgement.** Review #5 (SHIP) found: (a) a PRE-EXISTING `data-lg-title-own` attribute would be adopted as if it were the author's title and then written into `title` on withdrawal, so the element would gain an attribute it never had; (b) an author title of `""` composes a leading blank line (`"\n<option text>"`) because the duplicate rule covers `own === text` but not `own === ""`. **Both measured UNREACHABLE today**: `data-lg-title-own` exists nowhere in `api/src` outside `clip-reveal.ts`, the only two author-titled leadgen selects are titled pre-insertion or in SSR, neither is `cloneNode`d, no product code round-trips `outerHTML` into a save, and the attribute never appears in the p3a captures; an empty path is rejected at `sections-handlers.ts:2408`. Each would close with a one-line guard. **Why not fixed:** after five reviews and fourteen fix rounds on this phase, three of which introduced the next round's defect, the marginal risk of another edit to this file exceeded the benefit of closing a hole nothing can reach. Recorded so the next person finds it deliberately rather than rediscovering it. | P8-3 | BLOCKED(owner ruling: fix-or-defer — two one-line guards in clip-reveal.ts, or accept them as unreachable) | review-p8-3e/REVIEW.md |

| ADJ-P8-31 | "Add a 'I' 'funnel layout element' - progress bar - I clearly explained that I want different types of progress bars and to design them with a dedicated box!" | **The publish preview's per-step progress swap can never match the `numbered` style, so that style's preview never advances.** `advanceFrameProgress` (`quotes-handlers.ts:4669-4672`) rebuilds the progress region with `renderProgressBar`, while the real frame renderer uses `renderNumberedIndicator` for `numbered` (`designs/frame.ts`). The swap is an exact-substring replacement, so for that one style the substring never matches and the step never changes in the preview. Found by the P8-4 fix round while unifying the step label. **NOT FIXED deliberately:** the approved plan states §9.4's advance semantics stay owner-owned and that M1 touches only its listed items — `advanceFrameProgress`'s from/to swap is gated on `show_label`, and P8-4 has just REMOVED the `show_label` control for `numbered` on a conductor ruling, so changing the swap in the same phase would alter owner-owned semantics on the back of a UI ruling. | P8-4 | BLOCKED(owner ruling: fix-or-defer — align the swap with the renderer for `numbered`, or accept that the preview does not animate that style) | — |

| ADJ-P8-32 | "Add a 'I' 'funnel layout element' - progress bar - I clearly explained that I want different types of progress bars and to design them with a dedicated box!" | **A media reference whose blob no longer serves paints an empty white disc instead of degrading.** P8-4 shipped the custom progress marker (contract R7) and it degrades correctly for an ABSENT or CSS-unsafe url — driven: dot mark, no url emitted. It does NOT degrade for a well-formed `/media/<key>` whose R2 object 404s: `default-funnel/styles.ts:3211` puts `background:transparent` + `background-image:var(--lg-progress-icon-url)` on the `::before` over a white 26px `::after` disc, so a dead url leaves the disc bare. `designs/frame.ts` is a PURE renderer and cannot detect a 404 statically, so the fix must be an async existence check at the caller (`serve.ts`/`resolver.ts`/`branding.ts`), a layered CSS fallback, or a client error-watcher in the runtime bundle — all outside the slice's files and all beyond M1's clause. **The same class already affects `site_logo` and `header.logo_media_id`**, so this is not new with P8-4; the new control merely joins an existing family. | P8-4 | BLOCKED(owner ruling: fix-or-defer — pick one degrade mechanism for every media-backed image, or accept the bare-disc failure mode) | review-p8-4/d3-marker-missing-image-zoom.png |

| ADJ-P8-33 | "how do I define it????" (the owner asking how a template's look is defined) | **Unlinking a template leaves its look behind, and nothing tells the operator that.** Under P8-4's new materialise semantics, `POST /funnels/:id/apply-template {template_id:null}` clears the pointer (`frame_template_id` → null) but leaves the materialised `frame_config` intact — conductor-driven: after unlinking, funnel A still rendered `lg-frame--header-footer`, and only an explicit `PUT /frame {frame_config_json:{}}` returned it to the default. This is defensible (the values are the funnel's own now) and it is also defensible to expect a revert; the handler's own doc comment says only that it "clears the pointer" and is silent on the look. A semantics choice the owner should make, not one to infer. | P8-4 | BLOCKED(owner ruling: fix-or-defer — say what unlink does in the UI, revert the look, or leave it and document) | p8-4-conductor/drive.txt |

| ADJ-P8-34 | "theme is only design language!!!! colors, fonts, sizes" | **Two separately-offered controls write one surface, and one silently always wins.** `mobile.trust_strip_mobile` out-ranks `trust_strip.mobile` by specificity and source order — the guard measured `trust_strip.mobile`'s rule at (0,2,0) losing to `.lg-frame--m-trust-* .lg-frame-trust .lg-logo-strip` at (0,3,0) emitted later by `frame.ts:252`. Both are offered to the operator, so setting the one that loses does nothing and the UI gives no hint which governs. Found while correcting the paint predicate: the key only measured ALIVE once its sibling was explicitly omitted from the probe. Not the same defect class as a dead control (the surface IS painted — by the other control), so it is surfaced rather than fixed under R3. | P8-4 | BLOCKED(owner ruling: fix-or-defer — collapse the pair into one control, or make the precedence visible to the operator) | — |

| ADJ-P8-35 | "Another poor execution. Here is the issues summary" (delivery quality) | **One fifth of the served quote-editor page is developer commentary.** Measured by the conductor on the captured page: `editor-full.html` is **777,195 bytes**, of which **155,567 bytes across 2,119 lines are `//` comments** inside the emitted inline islands — they are shipped to every operator's browser on every editor load. P8-4 added **6,496** of them; the bulk is pre-existing and accumulated across phases. P8-3 already established the principle when it moved a 25-line rationale OUT of an island after discovering island comments both ship and are read by string scanners (that had produced a false green). The rationale is genuinely valuable and belongs in the repo — it just does not belong in the response body. NOT FIXED: stripping comments from emitted island bodies is a build-step mechanism no clause asks for, which §1 forbids me from adding unasked. | P8-4 | BLOCKED(owner ruling: fix-or-defer — strip comments from emitted islands at build time, or accept ~155KB of commentary per editor load) | p8-4-conductor/drive.txt |

| ADJ-P8-36 | "how do I define it????" (the owner asking how a template's look is defined) | **Honouring "Bare layout" visibly changes 3 of the 6 shipped templates, and a committed FROZEN baseline disagrees.** `section_slot.card` was a dead control — `bare` painted nothing — so P8-4 gave `--bare` a real rule. `header-footer`, `white-trust` and `minimal` all ship `card:"bare"`, so their question unit now computes `background rgba(0,0,0,0)`, transparent border, radius 0, no shadow (driven at 1280 and 375). The frozen pair `api/test-ui/__screenshots__/leadgen-v25/pattern-b-{desktop,mobile}.png` — seeded `template:"header-footer"` at `leadgen-patterns-v25.spec.ts:911` — captured the DEFECTIVE state (an opaque white card, 16px radius, shadow), so fixing the control necessarily moves those pixels. **Nothing was rebaselined and no `maxDiffPixels` was weakened.** CI runs no Playwright, so no gate could have caught this; a review found it by reading the fixture. This is manual visual QA, which is an owner-authority stop. | P8-4 | BLOCKED(owner: look at the three bare templates and either bless the new rendering — re-blessing the frozen baseline is yours alone — or say bare should not strip the surface) | review-p8-4b/j10-header-footer-AFTER-F8-1280.png |
| ADJ-P8-37 | "how do I define it????" | **A built-in template named "White + trust bar" renders no trust bar.** Driven: `trustRegion:false`. P8-4 fixed the THUMBNAIL so it stops drawing a band the config disables — which made the picture honest and left the NAME lying. Renaming a built-in template is an owner-facing vocabulary change, not a defect fix, so it is surfaced rather than done. | P8-4 | BLOCKED(owner ruling: fix-or-defer — rename the template, or enable the trust bar it is named for) | review-p8-4b/j7b-saved-chips-zoom.png |

| ADJ-P8-38 | "in the middle create different funnels side by side" (A/B arms must be able to differ) | **A leaf the operator authored on the funnel now outranks an A/B arm's own template for that leaf.** P8-4's prune fix preserves operator-authored values in the funnel column so an apply cannot silently discard them (review F-B). A side effect: `resolver.ts:1882-1887` layers that preserved leaf ABOVE a variant's own `frame_template_id`, so an arm cannot move it. This is a narrower cousin of the F-2 defect the same phase fixed (materialising once shadowed EVERY leaf; now it is only the leaves the operator explicitly set). Which should win is a genuine product question — an operator who set a value on the funnel may well expect it to hold across arms, or may expect the arm's template to govern that arm. **Source-verified only, NOT driven** — recorded at that strength deliberately rather than asserted. | P8-4 | BLOCKED(owner ruling: fix-or-defer — decide whether a funnel-level operator value or an arm's own template wins, then make the loser visible in the UI) | — |

| ADJ-P8-39 | "how do I define it????" (the owner asking how a template's look is defined) | **M3's headline number was a fixture artifact, and the correction is 14x.** Both the contract's *"45 of 46 comparable leaves shadowed"* and this phase's own *"28 of 29"* were measured against a saved funnel built by PUTting the whole `effective_frame` projection — a state the Quote Builder never writes. Driven at HEAD: a real Save PUTs the STORED column plus only the paths that session touched (`funnel.ts:1809`, `:1921-1933`), roughly three leaves. Re-measured against a realistic saved funnel: **comparable 29 · shadowed 2 · honoured 27** *(SUPERSEDED — that was the F11 fixture, which edited `header.tagline`/`back.label`; see the shipped number below)*. **CONDUCTOR CORRECTION (review #4 MAJOR-2):** I first justified those 2 as "exactly the operator's own edits (`back.label`, `header.tagline`)" — but NEITHER is authorable by any control (a live census of all six tabs finds 18 `[data-frame-key]` controls; `header.tagline` has 0 admin occurrences). The fixture edited leaves an operator cannot reach, so that sentence invited a conclusion the measurement does not support. Re-measured with two GENUINELY offered edits: **comparable 28 · shadowed 2 · honoured 26**. (**CONDUCTOR CORRECTION #2, from the P8-4+P8-5 re-review, finding 8:** this cell previously read *"27 · 2 · 25"*, taken from the reviewer's own throwaway census using `header.logo_align` + `header.logo_size`. The SHIPPED fixture uses `header.logo_align` + `background.image_media_id` — both grep-proven real controls — and the committed test measures **28 · 2 · 26**. A register row citing a number the shipped test does not produce is the same in-file-claims-rot this mission keeps punishing, in my own artifact. The shipped measurement wins.) The correction survives on operator-reachable leaves, which is the number that matters. The 2 shadowed are the operator's own choices, correctly preserved. So a POINTER-ONLY apply already moved 27 of 29 template-disagreeing leaves through the existing `frame_template_id` → `savedTemplateDefaults` resolution. **What remains true:** a review independently DROVE the confirm dialog making four false promises, and DROVE A/B arms rendering byte-identically — both real, both fixed. **What is not:** the framing that apply "changes nothing but an id". **Cost of the fix built on the inflated number:** it introduced review #1's F-1 (the dialog lying on every apply after the first) and F-2 (A/B templating permanently disabled), then its own fix introduced a BLOCKER (`--bare` moving 3 shipped templates and a frozen baseline) and ADJ-P8-38. I am not unwinding it at this stage — that is a large change with its own risk — but the owner should know the defect was 2 leaves, not 28, before deciding whether the materialise design stays. | P8-4 | BLOCKED(owner ruling: keep the materialise design, or revert to pointer-only now that the shadowing is measured at 2 operator leaves) | — |

| ADJ-P8-40 | Image11 (`docs/leadgen/source-of-truth/images/Image11.png`) | **Two tests both cite Image11 and assert opposite placements, and one of them is wrong.** P8-5 moved the dual-range value pill UNDER its handle per the image (driven fail-before: pill `y=411.9`, required `>=466.9`). The image's TOP slider — a SINGLE handle — also shows its value (`$37`) under the handle. But `renderRange` has no per-handle pill at all: the single type's value is `.lg-range-value`, a static block ABOVE the track, and `test/leadgen-slider-anatomy-r2.test.ts:60-73` asserts exactly that placement **while attributing it to Image11**. That attribution is false. NOT fixed: the contract's N15 row is scoped verbatim to `dual_range` pills, and moving the single readout under its handle means restructuring `renderRange`'s DOM and breaking a passing test in an unowned file — scope the owner did not ask for. Surfaced with the mis-citation named so it is corrected deliberately rather than discovered again. | P8-5 | BLOCKED(owner ruling: fix-or-defer — make the single-handle slider match Image11 too, or correct the test's citation and accept the single type reads differently) | n15/image11-reading.md |
| ADJ-P8-41 | "how the user defines a theme per funnel?" (activation is how a funnel goes live) | **Activation validation appears to be quote-wide, not scoped to the funnel being activated.** Driven incidentally by a P8-5 slice: the shared fixture quote `lgq_01KZ271383Y0MPV4BM2WKKCC4W` returns 409 on activation because an unrelated sibling funnel under the same quote ("P8-Charlie", zero sections) fails validation. An operator cannot activate a finished funnel while any other funnel in the same quote is incomplete. Not investigated further and no admin/activation file was touched — reported at the strength observed. | P8-5 | BLOCKED(owner ruling: fix-or-defer — scope activation validation to the funnel being activated, or confirm quote-wide is intended) | — |

| ADJ-P8-42 | "this is one of your worst executions… the mapping of what is auto-filled per field should definatly be an option" | **Renaming a choice label on a LIVE funnel now changes the value the buyer receives — when that value was never hand-edited.** P8-5 fixed R6-3 so a choice's value tracks its label while the value still holds the machine derivation (`value === slugify(label)` and `analytics_id === value`); the instant an operator types their own value it freezes forever (proved: `gco_2024_q3` / `partner_gco` survive a rename). Driven end to end: rename → save → visitor walk → `POST /lg/auction` carries `roadside_rescue_mutual`, and `other_option` appears in 0 of 22 sent bodies. **The trade-off:** on a live funnel, a label rename now moves the buyer-facing value and breaks analytics continuity for that choice. The alternative — freeze on first save and never mint `other_option` (seed the Other row label-only) — was NOT shipped because an empty value is a new save error on a bare enable+save (`validateOtherEditor` requires it), i.e. hardening §1 forbids. Correctness was chosen over continuity; the owner may prefer the reverse. | P8-5 | BLOCKED(owner ruling: keep value-tracks-label until hand-edited, or freeze on first save and change how the Other row is seeded) | s5-2b/visitor-1280.png |
| ADJ-P8-43 | Image11 | **The slider value is a filled navy chip; the owner's pin shows bold dark text on the page background.** N15's pinned requirements are all met and driven across 12 states at both viewports — the value sits UNDER its handle, travels with it, and the small grey `.lg-range-minmax` scale row is present in every state and was never deleted. But Image11 has **no chip behind** `$37` / `18%` / `78%`: the owner's values are bold dark text directly on the page, where the product paints white-on-navy rounded chips. Outside the clause's literal words (the clause pins placement and non-duplication, which pass), so it is surfaced rather than changed. The second enforcement leg for any design pin is a human side-by-side against the owner's image and is owner-owned; no pixel match against Image11 was automated, per the mission's design-pin rule. | P8-5 | BLOCKED(owner ruling: keep the chip, or render the value as bold text on the page exactly as Image11 shows) | review-p8-5/n15-dual-mid-1280.png |
| ADJ-P8-44 | "Each one of this questions is answering **another field** for the offers payload" | **The dual_range / from_to handle aria-labels are bare role words with no question caption.** `presets.ts:1196`, `:1217`, `:1220` emit `Minimum` / `Maximum` / `From` / `To` with no caption and no `aria-labelledby`, so on a page carrying two range questions a screen-reader user hears four indistinguishable labels and cannot tell which question a handle belongs to. N16-5's letter IS met — these are not raw ids, and the F2 fallback (`ariaLabel ?? label ?? internal_field`) is correctly applied at all 5 single-handle sites with no `?? node.internal_field` aria-label left anywhere in `presets.ts`. This is the accessibility gap the clause's letter does not reach. | P8-5 | BLOCKED(owner ruling: fix-or-defer — prefix each handle label with the question caption, or accept bare role words) | review-p8-5/n15-dual-mid-1280.png |
| ADJ-P8-45 | "the '+Add a question' button, for all the components, should be small, out of the component" (owner A.1 #1) | **The canvas still injects `+ Add choice` and `+ Add a question` ghosts, and two owner instructions pull opposite ways.** Contract R4's "injected editor chrome" row lists `2× + Add choice` among the things the canvas paints that the live page has none of — i.e. it asks for their removal — while owner A.1 #1 explicitly requires that the add affordance EXIST for all components. P8-5 read this as: keep the affordance, drop it only where it is nonsense (M7's dropdowns, driven and confirmed at 0). Measured on the reviewer's probe: 2 ghosts remain (choice groups + QuestionGrid). The reading is defensible but it is a resolution of a conflict between two owner sentences, so it is stated rather than assumed. | P8-5 | BLOCKED(owner ruling: keep the add affordances in the canvas per A.1 #1, or strip them per R4's chrome row) | review-p8-5/m6-studio-1280.png |
| ADJ-P8-46 | "this is one of your worst executions… the mapping of what is auto-filled per field should definatly be an option" | **A conductor reading of two contract sentences that pull opposite ways, which changed product behaviour — surfaced so the owner can overturn it.** Contract §4 R6-2 ends *"The picker deliberately offers exactly the siblings that collide"*, while the same contract's M4 requirement is *"fills cannot collide"* and the P8-5 reviewer DROVE the collision destroying a visitor's answer on the buyer payload (typed "Mountain View" into the box labelled City; the `POST /lg/auction` answers object carried only the sibling's text under the shared key, with no `…_addr_city` key at all). **Ruling: R6-2's sentence is DESCRIPTIVE** — it sits under a headline reporting that the picker *"can collide"*, and it states the shape of the defect (of everything the picker could offer, it offers precisely the harmful set). Read as a requirement it makes the contract self-contradictory. So the fix disables a sibling-owned key **only for a slot the Address actually renders** (where `fills` renames that slot's own visible box); a slot the Address does not render still offers siblings, preserving the real external-fill feature — driven and confirmed intact. A test asserting the opposite (`leadgen-p8-s52b-address-identity.test.ts:841-844`, *"city can still take an unclaimed field"*) was found to **contradict its own fixture**: `:803` seeds `town_field` as a sibling FreeTextQuestion's `internal_field`, so it was never unclaimed. The implementer STOPPED on that conflict rather than editing the check; the conductor ruled and authorised the change, re-founding the test's legitimate intent on a genuinely unclaimed key instead of deleting the claim. | P8-5 | BLOCKED(owner ruling: confirm that a colliding sibling key must not be offerable, or restore the old behaviour and accept the buyer-payload data loss) | review-p8-5/m4-fills-after-save-1280.png |
| ADJ-P8-47 | "this is one of your worst executions… the mapping of what is auto-filled per field should definatly be an option" | **The same buyer-payload data loss happens with NO `fills` involved at all — and every honest fix for it collides with a standing boundary, so it needs the owner.** While closing B-1 the slice measured a second route to the identical defect: a sibling question whose `internal_field` merely EQUALS the Address's derived sub-field key. Driven on section `lgs_01KZ7004ZKG1GAEZ4ET75EG34C` with a sibling `internal_field:"addr_city"`, an Address `addr`, and **`props.maps` null — the picker never touched** — the live page carries `data-lg-field` counts `{"addr_city":2,"addr":1,"addr_street":1,"addr_state":1,"addr_zip":1}`. Two visible inputs, one answer key, last writer wins, exactly as in B-1. This is **NOT** reachable through the fills picker (choosing `Create "addr_city"` would store the key the renderer already uses, so it cannot create the duplicate), which is why B-1's fix does not close it. **Why it is not simply fixed in-phase:** the three candidate fixes each hit something the mission may not cross — (a) refusing the colliding `internal_field` at save is a new validator, which §1's NO-HARDENING boundary forbids and the owner has already rejected once for activation; (b) changing how Address sub-field keys are derived is a breaking change to every stored funnel that already uses them; (c) a studio-time warning is the same blocker class as (a). Per §1 the slice stopped and reported rather than adding a gate unasked. Severity is high — it is silent, it is on the money path, and it needs only an unlucky field name. | P8-5 | BLOCKED(owner ruling: authorise a save-time refusal (a new validator, explicitly against §1), or a key-derivation change with a migration, or accept the collision) | p85-g1/ |
| ADJ-P8-47b | (same anchor as ADJ-P8-47) | **The duplicate-answer-key universe, now ENUMERATED rather than exemplified — 14 authorable shapes driven through the real renderer, 4 still collide, and every one of them has NO fill involved.** After the fills path was closed (BLOCKER-1, fixed: a stored fill is declined rather than renaming a box onto a key another node answers), a slice swept every authorable shape and measured which can still put two visible inputs on one `data-lg-field`: (i) a sibling whose `internal_field` equals the Address's derived `{base}_{slot}` — **with or without** a matching fill; (ii) two Address questions sharing one `internal_field`; (iii) a lone `full_address` beside a sibling named the base. **The decisive measurement:** driven stage C (`fills.zip = p8_addr_zip` + a sibling named `p8_addr_zip`) and stage D (**identical content, no fills at all**) produced **byte-identical** results — `census {"p8_addr_street":1,"p8_addr_city":1,"p8_addr_zip":2}` and one value in the auction body for both. So the collision is the authored key, not the picker, and **no rendering rule can resolve it** — declining a rename just falls back to the same name. That inverts an earlier guess that the `Create "<base>_<slot>"` option could cause a duplicate: it can sit on one, it cannot create one. The picker now names the sibling that owns the key, so the operator can at least see it. Fixing the class needs either renaming an authored key (breaking stored funnels) or a save-time refusal (§1 forbids), which is why it is here and not fixed. All fill-driven shapes are now distinct and the external-fill feature (an unrendered slot) is unchanged. | P8-5 | BLOCKED(owner ruling: same three routes as ADJ-P8-47, now with the full list of what is affected) | p85-g1/ |
| ADJ-P8-50 | "Each one of this questions is answering **another field** for the offers payload" | **Three smaller findings from the same driven sweep, none fixed, each measured.** (a) `leadgenAddressZipAnswerField` returns a single key, so it cannot carry the both-names answer the renderer now emits; with `fills.zip` pointing at a sibling and the zip row rendered, **the §9 ZIP facet reads the sibling's value** — closing it needs section context at `answers.ts` / `sections-handlers.ts`, outside the slice's files. (b) A stored `Create "<base>_<slot>"` value round-trips in the picker as `p8_addr_zip (missing)` because it is excluded from `others` by `ownRoleNames` — a pre-existing mislabel of a value that is in fact present. (c) `NameFieldsGroup` inputs carry `data-name-field` with **no `data-lg-field` ancestor** (`unkeyed-inputs=2` in the sweep), so they are invisible to every answer-key census — including the ones this phase used to prove the collision fixes, which means that shape was never covered by those proofs. | P8-5 | BLOCKED(owner ruling: fix-or-defer each; (c) also decides whether the answer-key censuses need to grow a second selector) | p85-g1/ |
| ADJ-P8-51 | Image11 (the `from_to` / `dual_range` control the owner pinned) | **A visitor types 40, watches it become 5000, and the buyer is billed on 5000.** Found while driving something else, then MEASURED CLEANLY on its own (real per-character keystrokes, no garbage, no `.fill()`, both viewports, real `POST /lg/auction`): with min untouched at 0, typing **40** into "To ($)" posts **`p8n_fromto_band_max: "5000"`**, and the number box rewrites its own displayed text to "5000" at blur. Declared bounds read off all four elements are `min="0" max="100000" step="5000"`, so this is **not** a bounds clamp. Root cause: `syncDualRange`'s gap clamp `if (b < a + step) b = fit(a + step)` (`runtime/engine.ts:572-573`) was written to stop two RAIL HANDLES crossing, but `handleInputEvent` routes every input inside `.lg-range` through it — including typing in the number box — so with `a=0, step=5000` any typed max below 5000 is silently forced up. Control cases that behave correctly: `20000/60000` posts `20000/60000`; the declared max posts intact. **Half the original report was killed by the same measurement:** two inputs sharing one `data-lg-field` is BY DESIGN (`renderDualTrackRange` emits a rail + a number box per key, synced deliberately), so only the value was ever the defect. **Being FIXED in-phase, not deferred** — unlike the other rows in this section, the fix crosses no boundary: it needs no new gate (§1 clean) and no migration, only correcting a sync path that misapplies a drag rule to typed input. | P8-5 | DEVIATES(FIXED in-phase by J1 — real `POST /lg/auction` now carries the typed 40 where it carried 5000, and a genuine ordering conflict corrects to the neighbour's EXACT value 20000 instead of the grid number 25000, identical at 1280 and 375; `dual_range` measured and CLEARED — 2 rails, 0 number boxes, no typed path, so it never had the defect. **One consequence the fix itself introduced, self-caught and named:** an off-grid typed value leaves the rail's INVISIBLE native thumb on the nearest step (measured Chrome sanitization 42000→40000, 40→0), so the drag hit-area can sit up to half a step from the painted handle — the painted fill, the handle, the pill and the posted answer all carry the exact value, only the invisible hit-area drifts. Bundle 53,063→53,124 of 53,248. **The 8 regression tests are written and MEASURED (3 red pre-fix → 8 green post-fix) but NOT YET LANDED** — importing `engine.ts` into the worker-program test file breaks typecheck; the verbatim block is preserved at `evidence/p8/p85-g1/j1-regression-block-unlanded.ts` and slice J2 is placing it) | p85-g1/ |
| ADJ-P8-48 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | **The structural reason the canvas keeps lying, stated once so the owner can decide the architecture rather than keep buying single fixes.** The studio canvas is an iframe whose `srcdoc` sets `script-src 'none'` **deliberately** (`ui-section-studio.ts:1274`, its own comment says so), so `engine.ts` can never run in it. `preview-sim.ts` is therefore a **hand-written mirror** of `runtime/render.ts` + `runtime/validation.ts`, and each of the six `LEADGEN_PREVIEW_SIM_STATES` (`preview-sim.ts:54-61`: `default`, `selected`, `error`, `dependency`, `validation_success`, `validation_error`) is mirrored separately. Nothing structurally keeps a mirrored state honest, so every state is an independent chance to drift — which is exactly what this phase measured: of the six, `error` and `validation_error` BOTH diverged and both were fixed on the canvas side (the address case marked 4 of 4 inputs where live marks 1, and hid the very sentence the visitor reads), while `default`/`selected`/`dependency` were confirmed correct by a reviewer's drive. The clause was satisfied state-by-state, by measurement, not by any mechanism. **Three honest routes, and picking one is an architecture decision, not a bug fix:** (a) let the engine run in the canvas — that means changing a CSP the product set on purpose, a security-posture call this mission refused to make unasked; (b) generate the mirror and the runtime from one source so drift is impossible; (c) accept the mirror and add a parity harness that enumerates the states — which this mission did NOT build, because a check that fails when a seventh state is added is a new gate and §1 forbids one no clause asks for. The evidence probe `api/scripts/p8/drive-g3-canvas-live-error.mjs` is the nearest thing that exists (mission tooling, deliberately not wired into CI). | P8-5 | BLOCKED(owner ruling: change the canvas CSP, unify the two renderers, or accept hand-mirrored parity and authorise a parity harness as an explicit exception to the no-hardening boundary) | g3/ |
| ADJ-P8-49 | "the canvas should include one section in the middle so the user could see a real reference of how is design is gonna look like in real life" | **The canvas paints a success state the live page can NEVER paint — and the two honest fixes are mutually exclusive, both owner-level.** Measured (probe, 2 new scenarios): on `validation_success` the canvas puts `lg-valid` on the field block (`input[q=note]`, `div[q=addr]`) while live paints it on **no element, ever, for either field shape**. `grep -rn "lg-valid" src/` has exactly two emitters — `preview-sim.ts:284 markValidInSlice` and the CSS rule `default-funnel/styles.ts:1926` (`color: validation.successColor`) — and **no `runtime/*` file emits it**. So the earlier hypothesis (*"marks the group instead of the ruled subfield"*) is **refuted**: relocating the class would close nothing. **Route A (canvas-only):** delete `markValidInSlice`, which makes `validation_success` byte-identical to `default` — that erases a contract-named sim (`08 §8.9` *"state sims: … Validation success …"*, `§8.12` acceptance *"dependency + validation-state previews visibly differ"*) and requires deleting the existing assertion at `test/leadgen-sections-api.test.ts:1209`, i.e. weakening a test. **Route B (live-side):** make the runtime emit the design's own success chrome — the design pack `08 §8.10` ships *"SuccessState (+ success styling on all inputs)"* and a `validation.successColor` theme token whose `.lg-valid` rule nothing matches, so under §4 R3 (*"a control that cannot be honoured must not be offered"*) this is arguably the correct route; but it is a **visible change to what every visitor sees**, in `runtime/render.ts`+`engine.ts`, with 185 bundle bytes left. The slice STOPPED rather than pick. **Two related facts found while measuring:** (i) `theme.ts:1593`'s comment claims `validation.successColor` has *"ZERO consumers in styles.ts/presets.ts"* — **false**, `styles.ts:1926` is a consumer; the conclusion (never counted as paint) survives only because nothing emits the selector, so the claim's REASON is wrong and would mislead anyone who later adds `.lg-valid`. (ii) the canvas's own target selection keys `answered` on the address BASE field, which live never writes, so with a real visitor's answer shape (`{note, addr_zip}`) the address is dropped from `targets` despite its ZIP being answered. Both fold into whichever route is chosen. | P8-5 | BLOCKED(owner ruling: Route A erase the success sim, or Route B ship success chrome to visitors and honour the successColor token) | g3/ |

| ADJ-P8-52 | "the rules you build are using jargon, have no actions, and just poor poor execution" | **What the jargon class cost, and the residual I stopped at — a scope decision, not a defect.** The contract named essentially one jargon string. Driving it out took **ten slices and roughly 70 sites**, because this codebase re-derives the same "show the operator a value" rule in many independent places: validators in `sections.ts` / `theme.ts` / `frames.ts` / `content-schema.ts`, the studio's own dropdowns, the theme manager's Advanced pane, the A/B and quote copy, and the FK-resolution family. Each slice's sweep found its class at MORE sites than the finding named — every single one, eleven times running. Two reusable seams now exist (a vocabulary-array-identity registry in `frames.ts` and one in `content-schema.ts`) plus centralised role labels in `theme.ts`, so the next vocabulary is one row rather than one more site. **I drew the convergence line here deliberately** and had the last slice report rather than chase. **Measured residual, for the owner to scope:** (a) **6 confirmed** operator-facing sentences still dumping camelCase/hyphenated vocabularies — `src/leadgen/payload.ts:448,499,888,944`, `src/admin/leadgen/payload-builder-handlers.ts:175`, `src/public/leadgen/designs/frames.ts:1896` (plus 3 in `listicles/validation.ts`, a DIFFERENT product and out of contract); (b) **9** unlabelled `options(X, null, …)` dropdowns, all in `admin/leadgen/ui-offers.ts`, most self-describing (currencies, timezones, statuses, DB-sourced names) and needing triage rather than blanket work; (c) 2 raw record ids in `admin/templates/ai-generation*` that are identifiers, not vocabulary, and are probably correct as-is. Also open and NOT collapsed, with the measurement: 3 label maps are word-identical across `frames.ts:1273` and `quotes-tabs/themes.ts:246-247` and could be deduplicated; the rest deliberately differ per surface and should not be. | P8-6 | BLOCKED(owner ruling: scope the residual — fix (a)'s 6, triage (b)'s 9, accept (c), and decide whether the 3 identical label maps get collapsed) | review-p8-combined/ |

| ADJ-P8-53 | "the rules you build are using jargon, have no actions, and just poor poor execution" | **A deliberate operator-visible change: a saved rule targeting an Address's or a slider's BASE key will now read "(removed field)".** Fixing the rules rail (final review MAJOR-1) meant making its field universe equal what the page actually records. Measured across 7 shapes with the real renderer: the rail previously offered an Address's base key (`p8_addr`) and a dual slider's base key (`budget`) while offering NONE of the four address roles, neither `given`/`family`, and neither `_min`/`_max` — so operators could build rules only on keys that record nothing, and could not build them on the keys visitors actually fill. Both base keys are wrapper hydration attributes and were **measured to record nothing**, so they are no longer offered. **The consequence:** any rule an operator already saved against such a base now shows "(removed field)" in the rail. That is ACCURATE — no input posts that key, so the rule could never have fired, and the product's own `deriveRuleCheckpoint` returns `unreachable: true` for it — but it is a visible change to existing configuration and the owner should know rather than discover it. It also matches the ruling already applied to the §6.2 payload picker, so the two surfaces stay consistent. **No migration was written**: the affected rules were already inert, so rewriting them would change behaviour rather than preserve it — but if the owner wants those rules re-pointed at a real sub-field instead of shown as removed, that is a data decision, not a code one. | P8-6 | BLOCKED(owner ruling: accept "(removed field)" on the previously-inert rules, or commission a migration re-pointing them at a real recordable key) | review-p8-final/ |

## Owner decisions (§9 — surface, never act)

| ID | Anchor (owner verbatim) | Requirement | Phase | Status | Evidence |
|---|---|---|---|---|---|
| OWNER-1 | "the first page is shared by **all** the funnels" | Shared-page publish gate (quotes-handlers.ts:5934): runtime skips an empty shared page, no R2 clause requires it non-empty. Contract recommends relaxing; NOT touched — gates a money path | §9 | BLOCKED(owner ruling: relax or keep — fix-or-defer) | contract §9.1 |
| OWNER-2 | "Here are some exmamples of botton of the page possible templates- Image28, Image29, Image30, Image31, Image45." | Five footer examples named, two built (Image29/Image30/Image31 unbuilt) | §9 | BLOCKED(owner ruling: build remaining three or defer) | contract §9.2 |
| OWNER-3 | "this template element could use different color, font and sizes then the main template." | Footer colours are palette ROLE references, so re-theming moves them — against A.2's independent-colour sentence | §9 | BLOCKED(owner ruling: literal colours vs role references) | contract §9.3 |
| OWNER-4 | "I clearly explained that I want different types of progress bars" | `percent` shows its number only with "Show label" on; always-on touches advanceFrameProgress whose from/to swap is gated on show_label | §9 | BLOCKED(owner ruling: keep gated or make always-on; naive change breaks per-step advance) | contract §9.4 |
