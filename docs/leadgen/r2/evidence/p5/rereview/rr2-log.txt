SETUP offer=lgo_01KYS1B1333YZ3KYVZWM92TKPN section=lgs_01KYS1B14K760VVGW2B8YM0YTJ
B1 after currency pick: type=string disabled=true
B1 attempt to force Type=number on a disabled select: refused
B1 after "As entered": type select re-enabled
B2 validation after contradictory raw JSON: Schema has 1 issue. || b2_amounttransform_invalidthe "formatCurrency" output format sends text, but the field Type is number — the runtime re-coerces after the format, so the value would be dropped. Re-pick the Output format to fix both at once.The transform is not a supported pipeline for this field — re-pick the Output format (it sets the transform and the sent type together), or fix it in the Advanced drawer.Jump
B2 output_format control shows=formatCurrency; type select shows=number
B2 Save attempt while flagged -> POST status observed = 0 (0 = no request fired)
B3 trim transform: output_format control shows ""; advanced-managed badge visible=true
B3 multi-step transform: output_format shows ""; advanced-managed badge visible=true
