# P8-3 p3a recapture — every differing line classified

`npx tsx src/scripts/capture-p3a-presplit.ts`, run by the conductor after all admin-markup slices
stopped editing. Pre-recapture fixtures were copied aside first and diffed line by line. The
standing rule: **one unexplained line stops the phase.**

## Files touched by the recapture

| Fixture | raw diff | real diff (ids + capture clock normalised) | classification |
|---|---|---|---|
| `quotes-list-empty.html` | — | — | sha256 identical |
| `quotes-new.html` | — | — | sha256 identical |
| `quotes-not-found.html` | — | — | sha256 identical |
| `editor-panel-analytics.html` | — | — | sha256 identical |
| `editor-panel-templates.html` | — | — | sha256 identical |
| `quotes-list-seeded.html` | +7 −7 | **0** | ULID capture drift only |
| `editor-panel-ab.html` | +6 −6 | **0** | ULID capture drift only |
| `editor-panel-activation.html` | +1 −1 | **0** | ULID capture drift only |
| `editor-panel-builder.html` | +14 −14 | **2** (1 pair) | intended — N1 + false-choice dedupe |
| `editor-panel-themes.html` | +55 −19 | **74** | intended — N1 / N7 / N11 / N20 |
| `editor-full.html` | +85 −44 | **84** | 74 + 2 above (it embeds both panels) + 6 intended island lines + 1 capture clock + 1 duplicated JSON line |

Normalisation used for "real diff": `lg[a-z]{1,2}_[0-9A-HJKMNP-TV-Z]{26}` → `<ULID>` and
`thm_…` → `<THM>`. NOTE for whoever repeats this: a first pass used `lg[a-z]_` (ONE letter) and
wrongly left 6 `lgpg_` page-id lines looking like real content. Two-letter entity prefixes exist.

## The three non-failing fixtures — why they changed at all

`quotes-list-seeded`, `editor-panel-ab` and `editor-panel-activation` never failed the parity
suite: it normalises ids before comparing. Their entire diff is the freshly-minted quote/variant
ULIDs of this capture run (e.g. `lgq_01KZ4AEAT97J8RHQ6ZDP4BG9F2` → `lgq_01KZ4H045PGSHYKQMTPK7Q9GSN`),
line-for-line, with no other token changed.

## The one non-obvious line in `editor-full.html`

The `<script id="lg-quote-data">` blob splits into **549 comma-separated fields on both sides**,
of which exactly one differs:

    "computed_at":1785777826689  ->  "computed_at":1785784701152

A capture wall-clock stamp, normalised by the suite. Not content.

## The intended changes, and proof each comes from real source

Every intended string was grepped in `api/src` — a fixture line with no source origin would mean a
hand-edited fixture, which is the one thing a recapture must never smuggle in:

| String | `grep -rF … src/` hits | Owner |
|---|---|---|
| `Default Funnel Design` | 1 | N1 label (S3.5 → moved to the producer by S3.7) |
| `Inherit from base<` | 1 | N7 shortened default option |
| `lg-theme-preset-help` | 2 | N11 help element |
| `Checking for saved presets` | 3 | N11 SSR default state |
| `refreshPresetAvailability` | 3 | N11 island confirmation |
| `des.selectedIndex = 0` | 1 | S3.7 stored-alias fallback |

Grouped by contract item:
- **N1** — the theme rail's option text (Button corners / Card corners / Card shadow) and the
  Base visual design select's option text.
- **N7** — the shared blank option is now `Inherit from base`.
- **N11** — the preset help element plus both buttons shipping `disabled` with a "Checking…"
  title, and the new island function that is the only thing that ever enables them. Its failure
  branch keeps them disabled and replaces the copy with a reason — unconfirmed never reads as ready.
- **N20** — the font `<option>` sets in the rail.
- **S3.7** — one option instead of two on the design select, plus the ES5 fallback that selects the
  canonical entry when a funnel stores the alias.

## Result

    npx vitest run test/leadgen-p3a-split-parity.test.ts
    Test Files  1 passed (1)
    Tests  12 passed (12)

Zero unexplained lines. No fixture was hand-edited; all eleven were regenerated by the capture
script from the real render path.
